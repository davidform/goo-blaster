# AGENTS.md — 接手這個專案之前必讀

> 這個檔案是**契約**，不是說明書。任何 AI（Claude / GPT / Gemini / Copilot / Cursor…）
> 在動 `index.html` 之前必須先讀完，並遵守「不可協商」那一節。
> 人類協作者也適用。
>
> 相容說明：本檔同時作為 `CLAUDE.md` / `.cursorrules` / `.github/copilot-instructions.md`
> 的內容來源。若你的工具讀的是那些檔名，請把本檔複製成該檔名，內容不要分岔。

---

## 0. 三十秒版本

**GOO BLASTER** 是一支**單一檔案**的 HTML5 Canvas 手機遊戲。
`index.html` 約 310KB，內含全部 HTML / CSS / JS / 11 種語言字典 / 50 關卡表，
**零外部相依、100% 離線可玩**。目標是上架 App Store / Google Play 付費下載。

- 線上版：https://davidform.github.io/goo-blaster/ ／ https://davidform.itch.io/goo-blaster
- 目前版本：見 `index.html` 裡的 `const BUILD='v0.9.xx'`
- 完整開發史與每一次決策的理由：**`docs-14-history.md`（v0.9.37 起，新版本寫這裡）**
  ＋ `docs-04-append.md`（v0.9.36 為止，**已凍結，只讀不寫**）

---

## 1. 不可協商（違反的話請直接拒絕，不要「先做再說」）

### 1.1 這個遊戲會給小朋友玩 —— 這是法律限制，不是口味問題

**禁止**任何形式的：轉蛋 / 抽卡付費 / 隨機付費寶箱 / 限時 FOMO 倒數 /
連續登入獎勵綁付費 / 誘導未成年消費的機制。

理由：多國兒童博弈法規與 COPPA。**唯一允許的變現方式是「一次性買斷解鎖」。**

（局內免費的隨機升級卡不受此限——玩家沒有付錢。界線是「錢」與「隨機」不能同時出現。）

### 1.2 單一檔案、零相依、可離線

不要引入 CDN、不要引入 npm 套件到執行期、不要拆成多個 `<script src>`。
玩家可能在飛機上玩，也可能被包成 WebView App。
（若要重構成多檔源碼，必須附一支 build script 把產物合併回單一 `index.html`，
並且產物要通過整套測試——見第 3 節。這件事風險高，動手前先跟專案負責人確認。）

### 1.3 任何字串改動都要同時做完 11 種語言

字典在 `const L10N={...}`，11 種語言 × 231 個 key。
顯示文字一律走 `T('key', 參數...)`，**不可以在程式裡寫死任何語言的字串**。

- 漏翻 → `py_i18n.py` 會抓到
- 英文模式跑出中日韓字 → `py_cjk_scan.py` 會抓到
- 翻譯對了但版面爆掉 → `py_layout_fit.py` 會抓到（**這是三件不同的事**）

預設語言固定英文：`const DEFAULT_LANG='en'`。**不要改成自動偵測。**
（曾經有人自作主張改成偵測系統語言，害使用者的手機連續三個版本都開成中文。）

### 1.4 一次只改一個變數

不要在同一版同時調「Boss 血量」和「卡池機率」。
玩家回報「變簡單了」的時候，你要分得出是哪一項起的作用，否則就沒有基準可以退。

改完在 `docs-14-history.md` 寫清楚**這一版只動了什麼**（v0.9.36 以前的在 `docs-04-append.md`）。

### 1.5 回報之前要自己測三輪

1. **第一輪**：跑整套測試，確認沒弄壞既有功能
2. **第二輪**：針對這次的改動找邊角案例（極值、0、上限、未定義欄位）
3. **第三輪**：最嚴苛條件（全部測試平行跑、CPU 節流、零永久強化的新手狀態）

**不要用讀程式碼代替實際執行。** 這個專案的測試是真的開瀏覽器跑遊戲迴圈。

---

## 2. 專案結構

```
index.html              ← 整個遊戲。唯一的產物。
docs-14-history.md      ← 開發史（v0.9.37 起）★ 新版本一律寫這裡
docs-04-append.md       ← 開發史（～v0.9.36，214KB，**已凍結**：只讀不寫，用搜尋不要整份讀）
docs-05 ~ docs-11       ← 各種專題評估（優化、獲利、語言市場、關卡數量…）
LICENSE.txt             ← 保留一切權利
tests/                  ← 50 支 Python(Playwright) + 8 支 Node 測試（run_tests.sh 預設批次跑 38 支 py + 8 支 js，其餘是診斷用）
run_tests.sh            ← 一鍵跑完整套測試（改完 index.html 一定要跑）
i18n/                   ← 產生/更新 L10N 字典的腳本（改字典用這個，不要手改 index.html）
store/                  ← 上架素材（封面、截圖、itch.io 用的 zip）
```

### index.html 裡面的地標

| 找什麼 | 搜尋這個 |
|---|---|
| 版本號 | `const BUILD=` |
| 多語系字典 | `const L10N=` |
| 關卡表（50 關） | `const LEVELS=` / `genExtraLevels` |
| 章節 Boss（10 隻） | `const BOSS_CHAPTER=` |
| 一般 Boss | `const BOSS_POOL=` |
| Boss 外觀特徵 | `const BOSS_SKIN_CH=` / `MID_SKIN_CH` |
| 升級卡池 | `const UPGRADES=` / `function rollCards` |
| 永久強化商店 | `const META_DEF=` / `function showShop` |
| 敵人 | `const ETYPE=` / `function spawnEnemy` |
| 主迴圈 | `function update(dt,rawdt)` |
| 玩家受傷 | `function hurtPlayer` |
| 存檔 | `function saveGame` / `loadSave` / `const SAVE_VER=` |
| **原生儲存** | `const NATIVE_STORE=` / `pickBetterSave` / `hydrateFromNative` |
| **免費/付費切點** | `const EDITION=` / `DEMO_LEVELS` / `isLocked` / `showLockBox` |
| **同伴（糖果援軍）** | `const ALLY_N=` / `spawnAllies` / `updateAllies` / `drawAllies` |
| 寶箱 | `const CHEST_TYPES=` / `const CHEST_W=`（整數權重表） / `buildChestPool` |
| **寶箱的關卡門檻** | `t.minLv` 欄位 / `CHEST_POOL`（第1~8關）vs `CHEST_POOL_LATE`（第9關起）/ `chestPool()`　⚠ `chestPool()` 讀的是 **`G.lvIdx`**，標題畫面就已經有一個 G 存在，只改 `LV_IDX` 沒有用 |
| **綠殼護盾** | `const SHELL_N=` / `SHELL_MIN_LV` / `spawnShells` / `updateShells` / `shellBlock` / `drawShells` |
| **存檔備份碼** | `saveCodeEncode` / `saveCodeDecode` / `importSaveCode` / `#codeBox` |
| **加速/核彈按鈕排版**（隨視窗較短邊縮放，手機/平板通用） | `function resize` / `BTN` / `#btnNuke` |
| 目前顯示中的 Boss（HUD 血條讀這個） | `G.boss`（`spawnBoss()` 裡有多 Boss 重疊時的交接保護，見 v0.9.30） |
| 長翻譯縮字級/折行的共用工具 | `function fitFont` / `function wrapText`（開場提示四行、大字特效都用這套） |

---

## 3. 測試 —— 這是多模型協作唯一的安全網

**沒有這套測試，第二個模型會無聲地推翻第一個模型的修正。**
所以規則很硬：**改完 `index.html` 就要跑整套，全綠才算完成。**

### 環境

```bash
pip install playwright --break-system-packages
playwright install chromium      # 若環境已內建 Chromium 可略過
```

測試會起一個本機 HTTP server 指向 `GAME_DIR`，預設是 `/home/claude/goo/game`。
**在你的環境請先設好這個路徑**（各測試檔頂端的 `ROOT=` 變數），
或直接把 `index.html` 複製過去。

### 一鍵執行

```bash
bash run_tests.sh          # 全部平行跑，最後印出失敗清單
```

### 必看的三個關卡

| 測試 | 驗收標準 |
|---|---|
| `py_test9.py` | **第 1 關那隻「笨 bot」必須通關。** 這是新手體驗的底線（零失誤只是參考值，見下方第 6 條） |
| `py_ab_base.py` | 證明「不該變的東西沒變」——改視覺時特別重要 |
| `py_bullet_density.py` | 第 5 關章節 Boss 一波 ≤ 5 顆；第 50 關維持 19/25 顆 |
| `py_v0927.py` | full 版一關都不能被鎖；demo 版第 16 關起鎖住 |

### 寫測試的鐵則（踩過的坑）

1. **不要用 `wait_for_timeout(固定毫秒)` 之後就直接檢查。**
   36 支平行跑時掉幀嚴重，實測 30 秒牆鐘只跑出 3.9 秒遊戲時間。
   一律改成「輪詢直到條件成立或逾時」。
2. **測量工具本身也要被驗證。** 有一支壓力測試連續錯了四版，
   每一版都給出看起來很像結論的數字（數存活子彈而非射出子彈、
   CPU 節流讓遊戲時間落後、bot 貼身被撞死、bot 站著不動被撞死）。
3. **沉默失敗的測試等於沒有測試。** 曾有 5 支測試因函式改名壞了好幾個月沒人發現。
   看到紅字不要當背景雜訊。
4. **`0/N 通關` 是紅旗，不是雜訊。**
5. 跨批次的數字不能互比（平行分頁數會影響成績）→ 要比就在**同一批**跑 A/B。
6. **機率性的指標不可以當硬性驗收標準。** `py_test9` 原本要求第 1 關「零失誤通關」，
   實測即使程式碼完全沒動也只有約 1/3 的機率會零失誤——每三次誤報一次的門檻，
   會訓練所有人忽略紅字。硬門檻是「一定要通關」，零失誤只是參考值。
7. **猜錯兩次就停下來裝儀器。** v0.9.27 有一項測試我連續猜了四次原因、
   每次「修正」都讓測試更複雜，最後量出來的真相是：被測的功能根本沒壞。
8. **效能測試不能跟別的測試搶 CPU**，而且**容器不是真實手機**，真機效能量不到。
9. ⭐ **「測試的輸入」也不能是機率的。** `hurtEnemy()` 內建 5% 爆擊（傷害 ×2），
   害 `py_boss_skin` 的盔甲門檻測試從 v0.9.22 起就有 5% 機率失敗、六個版本沒人發現。
   寫測試時要問：**我餵進去的東西，每次都一樣嗎？**（該測試已先 `G.P.crit=0`）

---

## 4. 多模型協作規則（重要）

`index.html` 是**單一檔案**，這代表：

- ❌ **不能兩個模型同時改。** git 對單一大檔的自動合併幾乎必定衝突或靜默覆蓋。
- ✅ **序列化作業**：一次一個模型 → 跑完測試 → commit + push → 下一個模型才開始。
- ✅ 每次接手前先 `git pull`，並確認 `const BUILD=` 是不是你以為的那一版。

### 交接時必須留下的三樣東西

1. `docs-14-history.md` 新增一節：**根因**、**改了什麼**、**量到的前後數字**、**教訓**
   ⚠ **不要再追加到 `docs-04-append.md`**（v0.9.36 起凍結）。原因是工具面的：
   這個環境沒有 `device_bash`、寫檔只能整檔覆寫，追加 5KB 就要重新輸出 214KB。
2. `const BUILD=` 版本號 +1
3. 測試：如果這次修的 bug 是既有測試沒抓到的，**要補一支測試把它釘住**
   （「修 bug 的同時要修『當初沒抓到的原因』」）

### 分工建議：按「區域」分，不要按「模型強項」分

模型排名每個月都在變，但檔案結構不會。建議切法：

| 區域 | 內容 | 會不會動到 index.html |
|---|---|---|
| A. 核心玩法 | 數值、關卡、Boss、卡池 | ✅ 會（**互斥，一次一人**） |
| B. 多語系 | `i18n/*.py` 產生字典 | ✅ 會（互斥） |
| C. 測試與稽核 | `tests/` | ❌ 不會（**可以平行**） |
| D. 上架與商業 | 商店頁文案、素材、定價、隱私政策、ASO | ❌ 不會（**可以平行**） |
| E. 原生封裝 | Capacitor / 儲存遷移 / 商店送審 | 另開 repo（**可以平行**） |

**C、D、E 三塊可以放心交給別的模型平行做，因為它們碰不到 `index.html`。**
這是目前最安全、也最有產出的協作方式。

---

## 5. 目前的已知缺口（接手的人可以直接挑）

- 🟢 ~~免費/付費切點~~ **v0.9.27 已完成機制**：`EDITION='demo'` 即可啟用
  （免費 1–15 關 + 解鎖面板 + `window.GOO_UNLOCK()`）。
  **還缺的是商店端**：`STORE_URL` 要填、原生內購要接、定價要決定。
- 🟢 ~~沒有原生儲存~~ **v0.9.28 已完成**：`NATIVE_STORE` 偵測到 Capacitor 就用
  Preferences 當持久層，localStorage 降級成快取；`pickBetterSave()` 保證玩家不倒退。
  骨架在 `native/`。⚠ **雲端沒有 Android SDK，真機打包與存檔持久性尚未實測**
- 🟢 ~~網頁版存檔可能被瀏覽器悄悄清掉~~ **v0.9.29 已緩解、v0.9.30 加強**：
  itch.io 玩家指出網頁版跑在第三方 iframe 裡，`localStorage` 不保證持久
  （跟 v0.9.28 解的「包成 App 之後」是不同的風險面）。加了「存檔備份碼」
  （`#codeBox`）讓玩家自己匯出/匯入一段文字碼救回進度。v0.9.29 當時留的
  待辦「久沒回來時提醒備份」，v0.9.30 已經做了一半：**第一次清掉第5關時**
  會在結算畫面主動提示一次（`codeNudge`，只出現一次）——這是「玩家有成就感
  的時間點」而不是「久沒回來」，兩者是不同的觸發時機，**「久沒回來才提醒」
  這個更完整的版本還沒做**（需要能偵測「距離上次遊玩多久」，目前沒有這個量測）。
- 🟢 ~~itch.io 升級卡永遠顯示 Lv.1 傷害數字~~ **v0.9.30 已修**：根因是
  `applyLanguage()` 只在開機/切語言時算一次描述文字、對局中不會重算，
  改成 `rollCards()` 每次現算。同時修正多 Boss 重疊時血條被搶走的 UI bug、
  補上核彈鍵盤快捷鍵（`E`）、修正平板大螢幕上加速/核彈按鈕過小擠在角落的問題。
  詳見 `docs-04-append.md` 三十三。
- 🟢 ~~開場移動提示只講觸控、桌機玩家會跳過~~ **v0.9.31 已修**：itch.io 兩位
  玩家的留言合起來拼出全貌——一位覺得操作「像在對抗」、另一位精準指出
  桌機滑鼠拖曳其實跟觸控共用同一套「相對方向」搖桿（已用真實滑鼠事件驗證
  屬實），但開場提示文字（`hintMove`）11 種語言全部只講「螢幕/手指/touch」，
  桌機玩家很可能因此完全跳過這句提示。改成裝置中立說法，並在過程中意外
  發現這個提示區塊（`hintMove`/`hintDash`/`hintHearts`/`hintChest`）從
  v0.9.21 起就沒被 `py_layout_fit.py` 測過（測試為了避免動畫閃爍主動跳過），
  好幾種語言早就超框，一併用 `fitFont()` 動態縮字修掉，並把測試補上。
  搖桿/移動邏輯本身沒有 bug，沒有改動。詳見 `docs-04-append.md` 三十四。
- 🟢 ~~戰報拍立得上的主角沒有臉~~ **v0.9.32 已修**：`makePolaroid()` 的照片區
  從第一版起就只畫一顆 42px 純色圓——沒有外框、沒有眼睛、沒有高光，跟
  `drawPlayer()` 完全不是同一隻角色。這張圖底下印著 `#GooBlaster`、是設計來
  分享到社群的，主角認不出來等於白丟品牌辨識度。新增 `drawHeroPortrait()`，
  比例全部照抄 `drawPlayer()`，並依勝負換表情（過關 `^ ^`／失敗 `× ×`）。
  ⚠ 根本原因是**整套測試沒有任何一支碰過 `makePolaroid()`**，已補
  `tests/py_polaroid.py`（讀真實畫素）並加進 `run_tests.sh`。詳見
  `docs-04-append.md` 三十五。
- 🟢 ~~🍬 軟糖再生可以無限回血~~ **v0.9.33 已修**：愛心上限固定 3 顆，但關卡
  長度一路加到 240 秒，而這張卡「每 40／25 秒回一顆」**完全沒有次數上限**。
  實測（對照版）第 50 關可白拿 **10 顆**愛心 = 實際血量翻 4.3 倍，一張卡就把
  難度曲線壓平。加上每關上限（Lv.1 = 2 顆、Lv.2 = 4 顆），滿血空轉不扣扣打。
  ⚠ 起因是使用者真機回饋，但他點名的是 🩹 急救棒棒糖（那張根本不回血）——
  **體感對、歸因錯**，要查的是他描述的效果而不是他點名的物件。
  已補 `tests/py_regen_cap.py`。詳見 `docs-04-append.md` 三十六。
- 🟢 ~~開機後主畫面永遠停在第 1 關~~ **v0.9.34 已修**：`SEL_IDX` 只有 `showMenu()`
  會設成 `PROGRESS-1`，但**開機路徑不經過 showMenu()**，所以玩家關掉遊戲再打開，
  進度與金幣都在、主畫面卻停在第 1 關（金幣對是因為 `applyLanguage()` 順手重畫了
  商店按鈕——同一份存檔、兩個地方更新，一個跟上一個沒有）。**網頁版一樣有這個
  bug**，只是網頁玩家很少「關掉再重開」。抽成 `syncSelToFrontier()` 並補在
  開機／`hydrateFromNative()` 合併完成／`showMenu()` 三個入口。
  已補 `tests/py_boot_sel.py`（含用假 Capacitor 模擬 App 重裝）。
  詳見 `docs-04-append.md` 三十七。
- 🟡 ~~第 7 關是一道 3.2 倍的難度懸崖~~ **v0.9.35 已大幅緩解，但沒有完全解決**。
  根因是 `nboss` 由 2 變 3 時，`BOSS_POOL[2]` 把 **ring（一波 19 顆）** 帶進一般
  Boss ——**正是 v0.9.26 判定「太密要縮」的那一種**，但當時的註解寫著
  「一般 Boss 不縮放（cross 8 顆本來就讀得懂）」，那個前提在第 7 關失效了。
  把 `cs` 縮放擴大到一般 Boss 的 ring（cross／spiral 與章節 Boss 都不動），
  ring 從第 7 關的半強度起跳、第 26 關回滿。
  **量到**（確定性工具、同批次）：第 7 關峰值 76→50（−34%）、平均 12.1→7.9（−35%），
  第 5、6 關完全未受影響；**懸崖由 +322% 降到 +178%**。
  🔴 **剩下的 +178% 仍是全遊戲最大一階**，下一個槓桿是「延後 `nboss:3` 登場」
  或「放慢第 7~9 關的攻擊間隔」——刻意分版處理（1.4），要先讓 −34% 上真機被玩過。
  ⚠ 使用者提議的「被打到後 3 秒無敵」機制**已存在**
  （`P.iframe = P.panic ? 2.1 : 1.45`），是延長不是新增；**刻意留到懸崖處理完之後**，
  因為它是全域鈍器，先動會直接蓋掉這一版的基準。
  詳見 `docs-04-append.md` 三十八。
- 🟢 ~~掉血時一直抽到「滿血才有用」的卡~~ **v0.9.36 已修**：`rollCards()` 後兩格
  原本是 `Math.random()*cp.length` **完全均勻亂抽、從不看玩家狀態**。實測（對照版、
  抽 4000 次）滿血 12.8%／非滿血 13.0%——**兩者一樣**，證實卡池無視血量。
  🔥 滿心怒氣的效果是「愛心全滿時 +45%」，掉血時抽到等於白卡，而掉血正是最需要
  有用選項的時候。改成加權抽，非滿血時 rage 降權至 0.25（**降權不是排除**——
  愛心糖與軟糖再生都能補回滿血）。量到：非滿血 13.0% → **3.3%**（4.06 倍差）。
  🟢 使用者更早提過的「血量低時救命卡機率高一點」是**反方向的加權**，
  **v0.9.40 已補完**（見下一條）。已補 `tests/py_card_hp_weight.py`。
  詳見 `docs-04-append.md` 三十九。
- 🟢 ~~復活只是「愛心突然變回 3 顆」，沒有任何演出~~ **v0.9.37 已補**：
  重生蠟燭的機制從 v0.9.17 起就是完整的（原地滿血、3.2 秒無敵、清空全場敵彈），
  缺的一直是**表達**——玩家不知道剛剛發生了什麼，也不知道自己還在無敵
  （唯一的提示是一圈**跟普通受擊一模一樣**的綠環）。加了全螢幕金色閃光、
  燭光柱＋向內收縮的金環、飄起的 🕯️、跟無敵同進同出的金色虛線環，
  以及專屬音效 `SFX.revive()`（原本借用武器進化的 `evo()`，那聽起來像「我變強了」，
  不是「差點死掉又被救回來」）。**一個數值都沒有動**——`hearts`／`iframe`／清彈
  三行逐字未改，所以 v0.9.35 留下的第 7 關基準沒有被蓋掉。
  ⚠ 根本原因是**復活這條路徑從 v0.9.17 到 v0.9.36 沒有任何一支測試走過**，
  已補 `tests/py_revive_fx.py`（機制回歸 + 演出驗收，並用 v0.9.36 對照版證明它抓得到）。
  🟠 專案負責人的原話也可以讀成「換一套復活機制」（例如復活只給 1 顆心 +
  更長無敵）——那是數值改動，**刻意留到真機回饋回來之後再單獨一版**。
  詳見 `docs-14-history.md` 四十。
- 🟢 ~~無敵狀態沒有表達，玩家不知道自己無敵~~ **v0.9.38 已修**：這個遊戲有**四種**
  來源會給無敵（受擊 1.45/2.1 秒、翻滾 0.42 秒、🌈 彩虹糖 **7 秒**、🕯️ 重生蠟燭
  3.2 秒），但它們從第一版起就共用同一圈薄薄的綠環。v0.9.37 只給復活做了金色演出，
  使用者看過之後仍說「我不知道自己無敵」——**要修的是這個狀態，不是他提到的那次事件**
  （跟 v0.9.33 的「體感對、歸因錯」同型，這次是「時機對、範圍錯」）。
  改成判斷 `P.iframe>0` 就讓角色本體走彩虹（瑪利歐無敵星星），四種來源自動全涵蓋。
  ⚠ 色相位移要用 **120° 均分不是 60°**——60° 的環在單一幀裡全是鄰近色，
  截圖出來就是「一隻綠色的角色」，完全讀不出七彩。
  同版一併加強核彈：白屏峰值 0.55→**0.88**、總長 0.32→**0.68 秒**，
  並加上 **2.5 秒淨空期**（引爆後不生一般敵人；擋在 Boss 排程之後，關卡長度不變）。
  **一個平衡數值都沒有動**，第 7 關基準未被蓋掉。已補 `tests/py_nuke_calm.py`（第 45 支）。
  詳見 `docs-14-history.md` 四十一。
- 🟢 ~~三個綠殼護盾還沒做~~ **v0.9.39 已做**：🐢 寶箱，三顆六角綠殼繞著角色轉，
  **只擋敵方子彈**（撞擊仍然照扣心，那是甜甜圈護盾 `P.shieldN` 的守備範圍——
  兩種護盾刻意分工，不然玩家看到兩圈東西卻不知道差在哪，難度影響也糊成一團）。
  沒有持續時間，只有「總共擋 15 發」，價值自動跟著彈幕密度走。
  **第 9 關（焦糖煉獄）才進池** → 第 1~8 關的寶箱池一份都沒變，
  v0.9.35 的第 7 關基準沒被蓋掉（`tests/py_shell.py` 用「前段池仍是 89 份」釘住這件事）。
  ⚠ **畫法踩過一次坑**：第一版畫成綠色圓形，跟軟泥怪（hue 140、r=15 的綠色圓形）
  幾乎一樣，看起來像三隻黏在主角身上的小怪。場上所有敵人都是圓形，
  所以殼**必須不是圓形**——現在是六角形 + 奶油色殼緣。
  詳見 `docs-14-history.md` 四十二。
- 🟢 ~~低血時抽不到救命卡~~ **v0.9.40 已補完 v0.9.36 欠的那一半**：
  v0.9.36 只做了降權（非滿血時把 🔥 滿心怒氣壓下去），加權那一半刻意留到這一版。
  `rollCards()` 的 `stW` 多一條規則：**只剩一顆愛心時，
  🍩 甜甜圈護盾 / 🍬 軟糖再生 / 🩹 急救棒棒糖 三張的權重 ×3**。
  量到（各抽 6000 次）：「三張裡至少一張救命卡」剩一心 **40.4% → 70.2%**，
  滿血不動（37.6% → 38.0%）；剩一心÷滿血由 **1.07 倍 → 1.85 倍**。
  ⚠ 三個刻意的決定，都有測試釘住（`tests/py_lifeline.py`，第 47 支）：
  ① 門檻是 **`hearts <= 1`**（再被打一下就結束）**不是 `< maxHearts`**（只是被打過一次）
  ② 名單只收「直接讓你不會死」的三張，**不收** 🧊/🌀 這種位移卡
  ③ 是**加權不是保底**（剩一心仍有 29.8% 抽不到救命卡）——
     保底會讓「低血」從危機變成資源，跟整體設計相反。
  **一個平衡數值都沒有動**，第 7 關（v0.9.35）與第 9 關（v0.9.39）基準都完好。
  ⚠ 難度 A/B 工具 `tests/py_ab_lifeline.py` 是**下界工具不是驗收工具**：
  笨bot選卡是三張隨機點一張，真人會刻意去點救命卡，所以它量到的差距
  只有真人效果約三分之一。詳見 `docs-14-history.md` 四十三。
- 🟢 ~~沒有隱私政策~~ `PRIVACY.md`（中英雙語）已寫好，**還要放到一個公開網址**
- 🟢 ~~定價還沒決定~~ **已定案 US$2.99**（2026-09-05，見專案現況文件的定價研究）；
  `STORE_URL` 仍是空字串——那要等 Google Play 商店頁真的存在才能填
- 🟠 **`py_v0927_perf` 的「有同伴時 FPS ≥ 30」對機器負載極度敏感**
  （2026-09-05 實測：容器有殘留程序時 23~25 fps 紅字，機器乾淨時 49.1 fps 綠字）。
  已用同批次 A/B 證明紅字時 **v0.9.31 未改版也一樣紅**，不是任何一版的迴歸。
  ⚠ **看到這支紅字的正確處置**：先 `pgrep -f "tests/py_"` 與 `pgrep headless_shell`
  確認沒有上一輪殘留的程序（被 timeout 砍掉的 `run_tests.sh` 會留下孤兒子程序
  繼續吃 CPU），清乾淨再單獨重跑一次。有意義的「同伴造成的 FPS 損失 < 20%」
  相對值在兩種情況下都是綠的。
  待辦（低優先）：把絕對門檻改成相對基準，讓它對負載免疫。
- 🟠 內容深度：只有 4 種敵人、3 種武器、1 個角色
- 🟠 `enemyTier()` 在第 9 關就封頂，之後 42 關的敵人分層完全相同
- 🟠 敵人分離迴圈是 O(n²)，需要空間網格
- 🟠 日 / 德 / 韓文沒有母語者潤稿過
- 🟠 git 歷史裡仍查得到已移除的內部文件

---

## 6. 專案負責人的工作習慣

- 用**繁體中文**溝通
- **只是推測就要明說是推測**
- **不要因為他是新手就迴避技術上的壞消息**——寧可講清楚代價
- **不要每次都請他二選一**，直接執行你推薦的做法，並說明為什麼
- 改動要**實際跑過驗證**再回報，UI 改動要附截圖

## 7. 精簡交接與回報（2026-09-08 確認）

- 每次新對話先完整讀本檔與 `HANDOFF.md`，再核對 Git 狀態、實際版本與本次任務。
  歷史紀錄不是現況證據；先看未提交變更，再安全同步，禁止用 reset/覆寫消除差異。
- `HANDOFF.md` 控制在約 40–80 行，只留目前任務、已確認決策、證據與下一步；
  詳細歷史放 `docs-14-history.md`，凍結歷史只按關鍵字搜尋。不得記錄金鑰或密碼。
- 只有評估對話長度過長、確實需要換新對話時，才先更新交接檔並提醒「建議換新對話」。任務階段完成或反覆回讀本身不構成換對話理由；一般階段完成只更新交接檔，繼續目前對話。
  請使用者在同一專案的新對話貼上以下原文，不自行宣稱已替使用者切換：
  > 依 AGENTS.md 與 HANDOFF.md 接手，先核對實際狀態，再完成目前任務。回報測試證據、未完成項目與 commit Summary。
- 遊戲改動仍須第 1.5 節三輪實跑；效能測試單獨跑，不與壓力測試搶 CPU。
  純文件/政策改動做內容、連結、雙語一致性與頁面驗證，不冒稱已跑遊戲三輪。
- 回報一律區分：已驗證、失敗、未執行；附測試命令/證據與未完成項目。
  原始碼正確不代表最終 APK/AAB 正確；封裝須核對實際產物及真機。
- 每次需提交時提供確切 commit Summary。遊戲版本用 `v0.9.xx: <單一改動>`；
  非遊戲改動用 `docs:`、`build:`、`test:` 等一致前綴，不為文件變更提高 BUILD。
- 已獲授權的同步可自動 commit/push；只提交本次檔案，先看 diff、測試及遠端。
  權限不足時走工具核准流程，不聲稱提交成功；push 可能觸發 Pages 公開部署。
  商店送審、正式發布、定價或敏感憑證操作不包含在一般 Git 同步授權內。
- 預設採單一代理、單一任務；需要多代理或付費工具前再確認，避免無謂 token 消耗。

## 8. 手機遠端測試流程（2026-09-09 使用者要求）

- 使用者經常以手機遠端操作電腦，不會維持 USB 連線；往後預設透過固定的 [Android 測試下載頁](https://github.com/davidform/goo-blaster/releases/tag/android-test) 提供測試版，不把接線當成提供新版的前提。
- 每次遊戲改動完成既定瀏覽器／邊角／CPU／離線驗證後，依 [native/MOBILE-TESTING.md](native/MOBILE-TESTING.md) 建置、稽核並更新此 Prerelease 測試通道。這是已授權的後續測試流程，不必重複問是否提供 APK。
- 固定 applicationId 與測試簽章、增加 Android versionCode；實際核對 APK 內容、簽章及公開下載 SHA256。不可用卸載／清除資料處理更新問題，不得把私鑰放入 Git 或 Release。
- 此授權僅涵蓋測試 APK 分發；全平行或真機尚未完成時必須明示，不能宣稱正式驗收。正式 Pages、商店送審、定價及更換簽章仍不在此授權內；itch.io 後續同步另依下列新授權。
- 不讓 Android 建置與效能測試同時執行。每次回報附固定入口、實際版本、驗證證據與仍需使用者在手機確認的項目。

## 9. 工作室共用流程（2026-09-09）

- 使用者要求未來多款付費遊戲沿用一致的開發方式；共用來源為 [studio/README.md](studio/README.md) 與 game-studio skill，本作設定為 [studio.project.json](studio.project.json)。
- 新遊戲可由自然語言想法開始；沿用工作流程，不複製本作 App 身分、簽章、售價、存檔或發布授權。
- 本作仍遵守單檔、11語言及既有驗證要求；共用架構不構成重構 index.html 的授權。
- 多 agents 仍為按需且先確認，預設單一代理；不用為了流程一致而常駐多個代理。

## 10. itch.io 同步與 USB 更新（2026-09-09 使用者新增授權）

- 使用者明確要求「往後直接更新itch.io」：每次遊戲改動完成既定驗證後，除 Android 測試通道外，直接同步既有 https://davidform.itch.io/goo-blaster，不必重複詢問。
- 使用固定 Butler 通道 `davidform/goo-blaster:html5`，流程見 [native/ITCH-PUBLISHING.md](native/ITCH-PUBLISHING.md)。只發布已驗證的 index.html，不上傳 repo、存檔或憑證；核對公開遊戲內容與實際啟動，不把上傳成功等同網頁已更新。
- 保留現有價格、公開狀態與專案身分；此授權不擴及其他遊戲、正式 Pages、Google Play／App Store 送審、定價或更換簽章。
- 本次使用者授權直接透過 USB 更新 Pixel；使用 `adb install -r`，核對既有App與簽章、版本及進度，不卸載／清資料。未連線時仍提供固定APK入口，不把USB當成發版前提。

## 11. 發版後 Devlog（2026-09-10 使用者新增授權）

- 使用者要求自動處理New devlog：完成本作itch.io公開遊戲驗證後，直接產生、核對並發布對應的中英更新日誌，不需逐次再問。
- 遵守[native/DEVLOG-PUBLISHING.md](native/DEVLOG-PUBLISHING.md)：只報已公開版本、同批交付一篇、先查既有文章與草稿、發布後核對Published與完整正文，記錄版本／網址防止重複。
- 本地工具native/devlog.py負責產文及收據；代理透過已登入瀏覽器執行發文。這是發版流程，不是背景排程；不擴及其他遊戲、其他社群或電子郵件。
