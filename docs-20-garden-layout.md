# 庭院操作與版面檢查（v0.9.76）

本次只改善操作位置與資訊分層，不更動產量、成本、倒數、存檔或戰鬥。

## 實際問題

以 v0.9.75 實際點選田地、廚房與村莊委託，發現操作列位於場景下方，選取物件會捲走原場景；廚房／委託也只是同一長頁的後半部。手機 390×844 的同條件量測：點第一塊田地後 scrollTop 增加 477px，種植按鈕落在 y787–831，而底部導覽從 y769 開始，按鈕遭遮擋。這是已重現的操作問題，不是玩家能力問題。

原始前後數據及截圖：`_private/test-artifacts/context76-comparison.json`、`context-v0.9.75.png`、`context-v0.9.76.png`。v75 基準取自遊戲 commit 3b76d1b；沒有重做其發布驗證。

## 研究與取捨

- [Apple Popovers](https://developer.apple.com/design/human-interface-guidelines/popovers/)：短暫、與來源物件相關的操作適合浮動面板；避免同時堆多張面板。本作採一次一張田地／小屋操作卡，保留選取外框與原場景。
- [Apple transient content](https://developer.apple.com/documentation/uikit/displaying-transient-content-in-a-popover)：只在需要時顯示操作。手機空間不足時將卡片限制在上下導覽之間，寬螢幕優先靠近田地旁邊。
- [W3C Dialog Pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/)提供 Escape、命名與焦點返回的參考；本作操作卡為可點外側關閉的非模態 popover，沒有宣稱實作模態背景鎖定或焦點圈限。

這些是設計原則，不能代替本遊戲的實際操作測試，也不能證明玩家喜歡新版。

## 完成的重排

| 流程 | v75 問題 | v76 行為 |
|---|---|---|
| 選田地 | 捲到下方原生選單 | 就近開操作卡，已解鎖植物以圖示按鈕直接列出，時間／產量同卡顯示 |
| 種植 | 按鈕可能在導覽後方 | 選植物後按種植；完成收起卡片，保持場景位置 |
| 作物成熟 | 更新可能取代正在操作的 DOM | 保留正在選的作物和鍵盤焦點；另一田成熟也不能重置選擇 |
| 小屋 | 修復控制在場景後方 | 小屋旁操作卡顯示等級、材料、修復與時間倍率 |
| 廚房／委託 | 任務、場景、配方、建設、裝飾長頁混排 | 各自工作頁，保留需要的材料／按鈕；寬螢幕配方及訂單並排 |
| 佈置 | 在長頁底端展開 | 專用工作頁，工具與佈置板集中 |
| 切換工作 | 必須捲回其他入口 | 固定分類列：田地、廚房、委託、佈置；頂端仍可回主畫面 |
| 村莊鏡頭 | 寬螢幕初始放大裁掉建築 | 初始比例配合場景寬高，保留拖曳與縮放 |

新版相同田地點選量測 scrollTop 增量 **0px**；種植按鈕 y617–661，位於底部導覽上方。小手機／橫向畫面仍可在操作卡內捲動較長翻譯，不承諾所有語言及所有裝置完全不用捲動。

另實際檢查了強化與設定頁：強化的效果、價格與購買按鈕已在同一列，設定只有聲音、語言及備份三個入口；此次沒有量到與庭院相同的「點物件再到遠處操作」問題，因此保留它們的既有結構。各主題繼續由首頁地標進入，不再在庭院分類列重複放主題入口。

## 驗證範圍

`tests/py_garden_context.py` 以真實按鈕操作驗證 11 語言×4尺寸、操作卡界線、44px按鈕、選取不捲頁、選擇保持、Escape關閉、工作頁隔離及離線。原庭院測試繼續驗證種植→收成→料理→交單→六建設、修屋、倒數、存檔與鏡頭。

`native/capture_garden_context.py` 保存真實 UI 圖及逐張 fixture／來源 SHA；高等級測試圖明列測試存檔，不冒稱真人進度。新手截圖用新存檔。

仍需真人確認：第一次操作是否理解、單手握持的舒適度、真機小螢幕閱讀與長時效能。自動化通過不代表這些項目已完成。
