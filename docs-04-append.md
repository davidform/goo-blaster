
---

## 八、2026-08-16 第二輪工作紀錄（v0.9.5，第五節 2-5 點 + 兩項使用者臨時追加需求）

> 這輪做了：第五節第 2-4 點（寶箱、Boss 預告、超級大 Boss），
> 以及使用者當天追加的「攻擊距離限制在畫面內」「怪物遠程攻擊 + 等級系統」。
> **第 5 點（銀河路線關卡選擇畫面）沒有做**，範圍太大、如原計畫單獨排一輪。

### 1. 攻擊距離限制在畫面內 ✅

`nearest()` 加上 `onScreen()` 篩選，只鎖定攝影機座標換算後落在螢幕範圍內
（含 44px 邊緣容許）的敵人。武器原本的 `maxd`（520/760/900）保留當距離安全閥。

### 2. Boss 出場預告 ✅

- 出場前 5 秒畫面閃紅 + 倒數數字 + Boss 名稱（無狀態計算，每幀重算，不怕漏事件）
- Boss 血量原本試過 ×2.2，笨bot 測出第 1 關零失誤基準線失敗率從文件記載的
  33% 惡化到 40%（Boss 戰拖長，bot 站在彈幕裡不閃的曝險時間變長），
  **回調到 ×1.5** 定案。戰鬥時間從原本 ~2 秒的秒殺拉長到 2.8-8 秒

### 3. 寶箱調整 ✅

- 出現頻率：等待間隔乘 1.4（原本 `rnd(0.85,1.25)` 倍，維持隨機但拉長）
- 新增「猛攻糖 💥」：8 秒攻擊力 +40%，即時性效果，不是永久數值卡
  （維持跟 17 張機制型升級卡的定位區隔，這點已跟使用者確認過）
- 「閃電糖」「猛攻糖」這兩個攻擊向效果在寶箱池裡各放兩份權重，
  讓寶箱整體「以攻擊向輔助為主」，但保留其他 6 種效果不要變成單一套路

### 4. 每 5 關超級大 Boss → 改放第 6、10 關 ✅

原本要求「每 5 關」會落在第 5、10 關，但第 5 關是 docs/02 設計的鋸齒回落點
（新機制登場、出怪率刻意降 6-8%），使用者確認後**改放第 6、10 關**
（這兩關在原設計裡都不是回落點）。新增 `SUPER_BOSS`「業績KPI巨獸」：

- 血量在關卡自己的 `bossHp` 倍率之上再乘 1.6，比同關卡最強的一般 Boss 更強
- 一定排在 `G.bosses` 陣列最後、`kind='final'`，打死它才算真正過關
- 視覺上多一圈脈動警戒圈、血條變桃紅色、名稱前綴「⚠️ 超級大 BOSS」

用 `buildBosses()` 直接算過第 6、10 關的輸出，時間/血量/kind 都正確
（第6關：t=130s hp=28288；第10關：t=200s hp=70720，兩者 kind 都是 final）。
笨bot 本來就會在到達這兩關的超級大 Boss 之前先陣亡（原始遊戲設計「死在第6關」
本來就是預期行為），沒辦法用笨bot 3 次驗收標準去驗證超級大 Boss 好不好打，
**這部分需要使用者真人試玩才能真正驗證平衡度**。

### 5. 銀河路線關卡選擇畫面 ❌ 沒做

範圍最大，如原計畫排到下一輪單獨處理。現有 `PROGRESS`/`renderStage()`/
`PROG_KEY` 循序解鎖邏輯都還在，可以沿用。

### 6. 使用者臨時追加：攻擊距離限制、怪物遠程攻擊 + 等級系統

這兩點是使用者在這輪工作中途追加的，不在原本第五節清單裡。攻擊距離限制見上方第1點。

**怪物遠程攻擊 + 等級系統**（4 種怪各自升級，全部能遠程攻擊，軟泥最慢）
踩了兩次同一種坑，過程記錄下來避免下次重犯：

1. 一開始把軟泥的遠程解鎖點設在 tier=1（第 3 關）：笨bot 3 次測試直接
   掉心 3/1/3 顆，打破第 1 關零失誤標準。**原因**：docs/02 的難度曲線把
   第 1-2 關設計成「完全不會反擊」的底線層，笨bot 完全不閃子彈。
2. 改成 tier=2（第 5 關）想說對齊無人機原本的登場點，結果第 5 關 3 次測試
   **全滅**（0/5 心）。**原因**：第 5 關本來就是「無人機第一次登場、單獨教你
   閃子彈」的關卡（docs/02 原話），這次改動讓軟泥+兔子在同一關一起加入
   開火，等於三種怪同時射擊，打穿了「一次只教一個新機制」的鋸齒設計原則。
3. **最終定案**：軟泥、兔子的遠程解鎖點設在 tier=3（第 7 關，挑戰層）。
   這層原始設計本來就是「不再引入新機制，純粹考驗操作」（第 8 關敘述：
   「四種敵人全開，沒有新東西了」），疊加更多遠程壓力跟這層設計意圖一致。

其他設計細節：

- 移速上限：硬性夾在「主角速度（260）的 60%」（=156px/s）以內，在 `spawnEnemy()` 算完後 `Math.min()`
- 只有無人機保留「近距離風箏後退」的舊行為；軟泥/兔子/自爆維持原本衝向玩家的路徑，
  只是路上會順便打一發，不影響原本的近戰讀法
- 軟泥的開火間隔（`cdMin/cdMax`）在任何 tier 都比其他三種慢，用同一個 `atkSlow`/`tier`
  倍率乘上去不會改變相對順序，所以「全遊戲最慢的射手」這個保證是結構性的，不是巧合
- 敵人身上會畫細白圈標示 tier（圈數 = tier），方便玩家辨認「這隻比較強化」

**過程中還抓到一個真的會當機的 bug**（不是難度問題）：敵人 update 迴圈用
`for(let i=G.E.length-1;i>=0;i--)`，如果迴圈跑到一半有範圍傷害（`damageArea`，
例如自爆怪死亡的爆炸）連續 splice 掉好幾隻怪，陣列會在迴圈跑到一半變短，
這時候 `i` 可能已經超出新長度、`G.E[i]` 是 `undefined`，讀 `.flash` 就直接
`PAGEERROR: Cannot read properties of undefined`。已經加上 `if(!e) continue;` 防呆。
這個 bug 應該原本就存在（不是這次改動造成的），只是這次測試量變大才被撞出來。

### 7. 誠實的殘留風險：第 1 關零失誤基準線沒有回到 100%

修完上面所有東西後，最後這輪 v0.9.5 對第 1 關連續跑了將近 20 次 `py_test9.py`，
**零失誤（掉 0 心）的比例大約落在 55-75% 之間**，沒有精確的單一數字，因為
bot 抽到的隨機升級卡影響很大，樣本數也不夠大到能算出可信的機率。

對照組：文件記載 v0.9.3 舊版基準是 3 次跑 1 次掉心（33% 失敗率）。
v0.9.5 大致落在同一個量級，**沒有觀察到系統性地變得更差**，但也沒有更好。
主要變因是 Boss 血量 ×1.5（拉長 Boss 戰、bot 站在彈幕裡曝險的時間變長）。

**建議**：如果之後真人測試（尤其是小朋友）反映第 1 關比之前更常掉血，
第一個該懷疑的是 Boss 血量倍率，可以考慮從 ×1.5 降到 ×1.2 或更低，
用 `tests/py_test9.py 1` 連續跑 5-10 次比對掉心比例。

### 8. GitHub 直接推送：目前這個雲端 session 做不到

實測 `git clone` 公開 repo 沒問題（讀取權限沒有被擋），但 `git push` 被
git 代理擋回 403，錯誤訊息是「這個 repo 不在這個 session 的授權 repo 清單裡」。
查過 Claude Cowork 的官方文件（架構總覽、GitHub 整合說明），都沒有寫清楚
怎麼幫一個雲端 code execution session 開通對特定 repo 的 push 權限——
現有的「GitHub 整合」功能是唯讀同步（把 repo 內容當成聊天的知識庫），
不是給 code execution session 用的寫入權限。

**這輪的處理方式**：改完的 `index.html` 直接寫回本機
`C:\Users\Surface\Documents\projects\goo-blaster\index.html`，
由使用者自己 `git commit` / `push`。

### 9. 環境現況更新

- **版本號**：v0.9.5
- 單一 HTML 檔，約 2,251 行

---

## 九、2026-08-16 第三輪工作紀錄（v0.9.7，愛心上限固定 3 + 移除兩張升級卡）

> 這輪做了使用者臨時追加的兩項數值調整：
> 1. 愛心上限全面固定為 3（原本 5→4→3 依關卡遞減），寶箱補心維持最多 +1（本來就符合，未改）
> 2. 拿掉「活力粉紅」（加愛心上限）跟「吸塵果凍狗」（全畫面吸經驗）兩張升級卡

### 1. 愛心上限：5/4/3（依關卡遞減）→ 全部固定 3 ✅

`LEVELS` 陣列 10 個關卡的 `hearts:` 欄位全部改成 `3`（原本第 1-3 關是 5、
第 4-6 關是 4、第 7-10 關是 3）。使用者的理由是「三顆心」要當成這款遊戲的
識別特徵，不要有關卡差異。順手把第 4 關描述文字裡「愛心少一顆」這句
（現在已經不準確）拿掉。

**寶箱補心**：檢查過 `CHEST_TYPES` 的 `heart`（💖愛心糖）效果，本來就是
`if(P.hearts<P.maxHearts){ P.hearts++; }`，一次只補 1 顆、頂到上限就不會再補，
跟使用者要求的「寶箱最多補充 1 顆」完全符合，**這部分沒有改任何程式碼**。

### 2. 移除「活力粉紅」「吸塵果凍狗」兩張升級卡 ✅

- `活力粉紅`（💗，原本效果 `maxHearts` 最高可疊到 7、還會順便回滿血）：
  拿掉，理由跟上面一致——愛心上限已經定死在 3，不該再有卡把它往上加
- `吸塵果凍狗`（🧲，原本效果是拾取範圍變成 `1e9`，等於全畫面自動吸經驗）：
  拿掉，使用者原話「取消全畫面磁鐵吸經驗的選項」

`UPGRADES` 陣列從 17 張（扣掉這輪之前已經是升級卡的部分）變成 **13 張**。
`rollCards()` 是泛用迴圈跑整個 `UPGRADES` 陣列抽 3 張，拿掉兩個項目不影響
邏輯本身；卡池還有 13 張，抽卡不會抽不出東西。`P.magnet`、`P.heart` 這兩個
計數欄位保留在 `P` 物件初始值裡（都設 0），只是現在沒有任何卡會再讓它們變化，
不影響行為，沒有刪的必要（刪了風險比留著更高，其他地方可能還有隱性依賴）。

### 3. 測試結果：誠實的殘留風險 ⚠️

`tests/py_test9.py 1` 連續跑了 **5 次**（比標準的 3 次多跑兩次，因為這次改動
直接動到生死攸關的血量上限，想多看幾組樣本）：

| 次數 | 結果 | 掉心 |
|---|---|---|
| 1 | 通關 | 掉 2（3→1） |
| 2 | 通關 | 掉 2（3→1） |
| 3 | 通關 | 掉 0 ✅ |
| 4 | 通關 | 掉 0 ✅ |
| 5 | 通關 | 掉 0 ✅ |

**5 戰全勝，沒有任何一次陣亡**，零失誤比例 3/5（60%），跟 v0.9.5 記載的
55-75% 量級相近，**沒有觀察到通關率系統性變差**。

**但是要老實說一個數學上必然的副作用**：同樣是「被打中 2 次」這件事，
以前愛心上限是 5，掉 2 顆心剩 3/5（60% 血量，還算安全）；現在上限變 3，
一樣掉 2 顆心就剩 1/3（33% 血量，只剩一口氣，再中一次就死）。
**敵人打中笨 bot 的絕對次數沒有變，但因為分母從 5 縮小到 3，
同樣的失誤現在換算成的「死亡風險」比例變高了。**

這不是 bug，是使用者要求「固定 3 顆心」這個決定本身帶來的結構性後果，
在這輪測試範圍內沒有讓 level 1 的笨bot 真的死掉，但**如果之後真人測試
（尤其是操作沒有笨bot穩定的小朋友）反映「常常剩 1 顆心提心吊膽」，
這就是預期中的代價，不是意外**。如果覺得太緊繃，之後可以考慮的方向：
- 降低早期關卡（1-2 關）的出怪率或 Boss 血量倍率，減少同一時間內的中彈次數
- 或者接受這個緊繃感，因為使用者本來就是想要一個「容錯低、爽感高」的
  三心制識別特徵，這是設計取捨不是失誤

### 4. 環境現況更新

- **版本號**：v0.9.7
- 交付方式：改成只透過 `SendUserFile` 傳檔案給使用者，使用者自己手動
  上傳到 GitHub（雲端 session 對這個 repo 沒有 push 權限，見上面第八節）
- 使用者專案現況已同步寫入 Claude Project（`goo-blaster-new` 專案，
  路徑 `claude/專案現況.md`），方便手機端接續對話時不需要本機檔案

---

## 十、2026-08-16 第四輪工作紀錄（v0.9.8，寶箱/音樂/UI 多項調整）

> 使用者這輪一次丟了 6 個要求（有些是分兩次訊息追加的）：
> 1. 取消「吸塵果凍狗」卡（**發現這張早在 v0.9.7 就已經拿掉了，是重複要求**）
> 2. 愛心糖寶箱出現頻率太高 → 調低
> 3. 愛心最多只能補到 3 顆（**發現本來就符合，maxHearts 已固定 3，沒有東西能超過**）
> 4. 背景音樂換更可愛、Boss 戰換更緊張刺激的
> 5. 銀河路線選關畫面沒辦法手動滑動，只能點節點才會捲動
> 6. 選關時按鈕文字「繼續 · 第?關」把「繼續」拿掉
> （另外還有「畫面晃動選項移除，但 Boss 戰要加強晃動」，一併在這輪處理）

### 1. 「吸塵果凍狗」「愛心最多3顆」：回報使用者這兩項已經做過了 ℹ️

檢查 `UPGRADES` 陣列，確認「吸塵果凍狗」（磁鐵卡）在上一輪（v0.9.7）已經整張
移除，`P.magnet` 恆為 0，沒有任何卡會再設定它。愛心上限 `maxHearts` 也已經
在 v0.9.7 固定成 3，`P.hearts` 只會在 `<maxHearts` 時才 +1（寶箱、中期Boss獎勵、
軟糖再生卡都一樣），結構上不可能超過 3。**這兩項沒有動任何程式碼**，如實跟
使用者說明「已經做過了」，而不是假裝又做一次。

### 2. 愛心糖寶箱權重調低 ✅

`CHEST_POOL` 原本是「7 種效果各 1 份 + 閃電糖/猛攻糖各多 1 份」＝10 份，
愛心糖佔 1/10（10%）。改成「愛心糖固定只 1 份，其他 6 種效果都變 2 份、
閃電糖/猛攻糖 3 份」＝17 份，愛心糖佔 1/17（≈5.9%），大約砍半。
沒有整個拿掉——愛心上限雖然固定 3 顆，掉血後還是需要補血的手段。

### 3. 背景音樂：拆成「可愛版」「Boss緊張版」兩套完全不同的主題 ✅

原本只有一套 C大調（C-G-Am-F）進行，Boss 戰只是同一首歌變快變亮（112→126 BPM、
方波濾波器開亮一點），聽起來像「同一首歌加速」而不是「換了一首」。這次重寫：

- **可愛版（平常）**：和弦進行不變（C-G-Am-F這個級進本來就是很多兒歌會用的
  明亮進行），但主奏樂器從方波換成三角波（更軟更甜），貝斯改成短促有彈性的
  斷奏，額外加一層「亮片」——固定節奏在弱拍疊一顆高八度的鐘鈴音色，做出
  「閃閃發光」的可愛感。
- **Boss緊張版**：整個換掉，改用 a小調 + 借用大三度屬和弦製造導音張力的
  進行（Am - E - Am - Dm，i-V-i-iv），跟可愛版完全不共用和弦。貝斯改成鋸齒波、
  八分音符驅動（密度是可愛版的兩倍），大鼓每拍都打（可愛版只有 1、3拍），
  每輪結尾加一顆不和諧的緊張墊音。BPM 也拉高到 158（原本 126）。
- 切換時機沿用原本就有的 `SFX.musicIntensity(1/0)` 呼叫點（Boss出現時 1、
  場上沒有 Boss 時 0），**沒有新增呼叫點**，所以不會漏接。
- 全部是 Web Audio 即時合成（沒有外部音檔），跟遊戲原本零外部相依的架構一致。
- **這部分沒辦法用 Playwright 自動聽感驗證**——只能確認「換主題的程式碼跑起來
  不會噴錯」（全 10 關 15 秒煙霧測試、笨bot 打完整個第 1 關含 Boss 戰都是
  0 JS 錯誤），實際聽起來夠不夠可愛、夠不夠緊張，需要使用者實際玩過才能判斷。

### 4. 銀河路線選關畫面：手動滑動失效，找到真正原因並修好 ✅

使用者回報「只能點節點才會往下顯示新關卡，不能手動滑」。一開始以為只要在
`#galaxyWrap` 加 `touch-action:pan-y` 就好，但實測發現完全沒用，於是往下查：

**真正原因**：CSS `touch-action` 是不是允許滑動，看的是「手指實際摸到的那個
元素，加上它所有祖先」的交集，只要鏈上任何一層是 `none` 就整條斷掉。這個專案
最上層有一條 `*{touch-action:none}`（原本是為了讓遊戲主畫面的搖桿拖曳不要
觸發瀏覽器內建的頁面滑動/縮放），把 `html`、`body`、`#wrap`、`.ui`、`.panel`
全部鎖死。只改 `#galaxyWrap` 沒用，因為手指實際摸到的是它裡面更深層的
`#galaxyInner` 或 `.gnode` 節點，這兩層一樣被 `*` 規則鎖住。

**修法**：把 `html`、`body`、`#wrap`、`.ui`、`.panel` 這條祖先鏈，以及
`#galaxyWrap *`（裡面所有子元素，包含節點、SVG、星星）都明確開放
`touch-action:pan-y`。canvas 本身維持 `touch-action:none` 不變，遊戲中的搖桿
拖曳行為不受影響（canvas 不是 `#galaxyWrap` 的子孫，這條規則管不到它，而且
canvas 自己的 touchstart/touchmove 監聽器本來就用 `e.preventDefault()` 明確
擋掉預設行為，跟 CSS 這層是兩件事）。

**驗證方式**：這次踩到一個坑——用 Playwright 的 CDP `Input.dispatchTouchEvent`
模擬真實觸控拖曳（不是合成的 `TouchEvent`），一開始怎麼測都是 `scrollTop`
不動，一度懷疑是 Playwright/Chromium headless 環境本身不支援模擬滑動捲動
（先在一個完全獨立的空白測試頁驗證同一套模擬腳本可以正常捲動，排除了
「工具本身不支援」這個可能性，才確定問題出在遊戲頁面自己）。逐層印出
`getComputedStyle(...).touchAction` 才抓到 `#galaxyInner` 這一層還是 `none`
——這就是最後靠印出整條祖先鏈的 touch-action 才抓到的，不是憑感覺猜的。
修完後同一套模擬拖曳腳本可以正常把 `scrollTop` 從 0 拖到 105。
點節點選關（`SEL_IDX` 變化、按鈕文字更新）也重新測過，沒有壞掉。

### 5. 選關按鈕文字拿掉「繼續」✅

`renderStage()` 原本組字串是 `'繼續 · 第 '+(i+1)+' 關'`，改成單純
`'第 '+(i+1)+' 關'`。已通關關卡的「重玩 · 第 X 關」、全破後的「再玩最後一關」
維持不變（使用者沒有要求動這兩個）。

### 6. 畫面晃動：拿掉使用者可調的選項，改成固定值 + Boss戰自動加強 ✅

原本主畫面有一顆「畫面晃動：關/低/標準」的按鈕（`btnShake`），遊戲中右上角
也有一顆對應的小圖示可以點擊循環切換，兩者共用同一個 `SHAKE` 全域變數。

這次把選項整組拿掉：主畫面按鈕、遊戲中小圖示（含它的碰撞判定、觸控/滑鼠/
Pointer Events 三套輸入路徑裡對應的分支）都刪除，`SHAKE` 改成不可變的固定值
0.5（也就是原本「低」那一檔，維持使用者原本用習慣的舒適強度）。

新增行為：`shake()` 函式現在檢查 `G.boss`（目前場上是否有存活的 Boss），
有的話晃動幅度額外 ×1.4。因為所有的「撞擊」（打中、被打中、爆炸等）都
共用同一個 `shake()` 函式，這個加成會自動套用到 Boss 戰期間的所有既有效果，
不用一個個找出來改，也不會漏掉之後新增的晃動呼叫。

### 7. 測試結果

- 全 10 關、各 15 秒煙霧測試：0 JS 錯誤（v0.9.8 最終版本）
- `tests/py_test9.py 1` 連續跑 3 次：掉 0 / 1 / 0 顆心，5戰全勝無陣亡，
  跟 v0.9.7 記錄的量級一致，這輪的改動（寶箱權重、音樂、UI）沒有動到
  戰鬥數值本身，理論上不該影響難度，測試結果也對得上
- 銀河路線手動滑動：用 CDP 觸控模擬驗證可以正常捲動（0 → 105px）
- 選關節點點擊、按鈕文字（含「重玩」「第X關」兩種狀態）：正常
- **音樂好不好聽、Boss戰夠不夠刺激，這件事沒辦法用 Playwright 驗證**，
  純粹是合成參數上的設計判斷，需要使用者實際打開遊戲聽過才能定案

### 8. 環境現況更新

- **版本號**：v0.9.8
- 單一 HTML 檔，約 2,356 行

---

## 十一、2026-08-16 第五輪工作紀錄（v0.9.9，重來二次確認 + 捲軸美化）

### 1. 「從第 1 關重來」加二次確認 ✅

原本點下去立刻執行、沒有任何確認，誤觸就會把破關進度清空且無法復原。
新增一個彈窗（`#confirmBox`），點「從第 1 關重來」先跳出「確定要從第 1 關
重來嗎？目前的破關進度會被清除，這個動作沒辦法復原。」，要點「確定重來」
才會真的執行 `PROGRESS=1`；點「取消」或誤觸背景都不會動到進度。

沒有用瀏覽器原生的 `confirm()`——手機瀏覽器的原生確認框樣式很醜、跟遊戲
風格不搭，改成跟遊戲其他彈窗（升級卡選擇畫面）一樣風格的自訂 UI。

**測試**：點擊→彈窗開啟但 PROGRESS 不變→點取消→彈窗關閉且 PROGRESS 不變→
再點一次→點確定重來→彈窗關閉且 PROGRESS 變 1，四種狀態都用 Playwright
實際點擊測過，不是只看程式碼。

### 2. 選單捲軸拉把換成糖果配色 ✅

`#galaxyWrap` 的捲軸用 `::-webkit-scrollbar` 系列屬性重新設計成圓角、
粉紫漸層（跟遊戲整體桃紅/紫色調一致）的拉把，取代瀏覽器預設的灰色細條。

**誠實的限制**：`::-webkit-scrollbar` 只有 Android Chrome 等 WebKit/Blink
系瀏覽器支援自訂樣式，**iOS Safari 不支援自訂捲軸外觀**，一律顯示系統自己
那條半透明拉把，這是網頁技術本身的限制，沒辦法用 CSS 繞過去。如果使用者
主要用 iPhone 測試，可能看不出這次的改動。

### 3. 環境現況更新

- **版本號**：v0.9.9
- 單一 HTML 檔，約 2,390 行

---

## 十二、2026-08-16 第六輪工作紀錄（v0.9.10，切背景音效不停、首頁音樂、暫停鍵、核彈道具）

### 1. 切到背景音效還在跑 → 找到根因並修好 ✅

**根因**：這個遊戲的背景音樂用「lookahead 排程」實作（`pump()` 每 25ms 跑一次，
提前把接下來 100-140ms 內的音符排進 Web Audio 的時間軸）。分頁/App 切到背景時，
瀏覽器通常會暫停 `requestAnimationFrame`（所以怪物、畫面看起來都會停），但
**Web Audio 的播放走獨立的音訊執行緒，不受 rAF 節流影響**——已經排進音訊時間軸
的音符，就算主執行緒完全停擺，還是會照樣播完。這就是使用者回報「退出關卡或切到
別的 App，音效還在跑，沒辦法立刻停」的真正原因，不是哪裡漏了呼叫停止函式。

**修法**：用 Page Visibility API（`visibilitychange`）監聽分頁是否可見，
不可見時呼叫 `AudioContext.suspend()`——這是唯一能讓「已經排定但還沒播完」
的聲音立刻靜音的做法（普通的「不要再排新的音符」沒有用，因為舊的已經排好了）。
額外加了 `pagehide`/`pageshow` 兩個事件當備援，因為 iOS Safari 有時候切到背景
只會觸發這兩個、不觸發 `visibilitychange`。

**測試**：模擬切到背景 → 音訊狀態變成 `suspended`；模擬切回前景 → 約 1 秒後
（`AudioContext.resume()` 是非同步的）狀態變回 `running`。這個行為沒辦法直接聽
出來，只能檢查 `AudioContext.state` 這個底層狀態，但邏輯本身測試過是對的。

### 2. 首頁音樂 ✅

原本主畫面（選關畫面）完全沒有音樂，只有進到關卡才會播。這次改成主畫面也播
放「可愛版」主題（跟 Boss 戰的「緊張版」共用同一套雙主題音樂系統，見上一輪
v0.9.8 的紀錄）。

**瀏覽器限制（誠實說明）**：所有瀏覽器都規定 AudioContext 一定要有真實的
使用者手勢（點擊/觸控）才能真的發聲，網頁一載入沒辦法自動播放音樂。所以做法
是「使用者第一次點擊/觸控畫面任何地方時，順便偷偷啟動音樂」——包含 canvas
本身的觸控事件，也包含選單上的按鈕、銀河路線節點（這些是 DOM 元素，不會經過
canvas 的觸控監聽器，另外在 `document` 層攔了一次）。使用者第一次打開遊戲、
還沒點任何東西之前，畫面會是安靜的，這是網頁技術的限制，不是 bug。

### 3. 遊戲中的暫停鍵 ✅

右上角（靜音鍵正上方）新增一顆暫停鍵，點下去會跳出「遊戲已暫停」的彈窗，
兩個選項：「繼續遊戲」（回到原本畫面接著玩）、「回主畫面」（放棄這局，
不計入任何進度或戰績，直接回選單）。

實作上重構了一下：暫停鍵、核彈按鈕要不要顯示，原本想在 `start()`/`endGame()`/
`showCards()` 等一堆地方個別 toggle，後來改成集中在 `drawHUD()`（每幀都會跑）
統一判斷「`G.running && !G.paused`」，避免漏掉某個切換路徑導致按鈕該藏的時候
沒藏、該出現的時候沒出現。

跟原本就有的升級卡選擇（也是用 `G.paused` 暫停遊戲邏輯）共用同一個機制，
互相不衝突：升級卡彈窗開著的時候，暫停鍵會自動跟著藏起來（因為兩者共用
`G.paused`，判斷式已經涵蓋這個狀態，不用額外處理）。

**測試**：點暫停 → `G.paused` 變 true、彈窗出現、遊戲時鐘（`G.t`）確認真的凍結
（等 1 秒沒有變化）；點繼續 → 恢復正常。

### 4. 核彈道具（每關限定一次）✅

- 每一關會在關卡進行到 28%-55% 之間（隨機挑一個時間點）出現一次，場上不會
  超過一顆，**撿到之前不會過期**（跟寶箱不一樣，不用擔心錯過）
- 用輻射膠囊造型（跟糖果箱造型的寶箱明顯不同），畫面外有專屬的橘色箭頭指標
- 撿到後不會立刻生效，收進「庫存」，左下角（加速鍵正上方）會出現一顆脈動發光
  的核彈圖示，玩家自己選時機點下去引爆
- 引爆效果：場上「所有非 Boss 敵人」（使用者原話「低級的怪物」，這裡的判斷
  標準是拿現有的 `e.boss` 旗標——不是 Boss 的都算）**直接死亡**，用的是
  `killEnemy()` 而不是扣血，確保是「保證死」而不是「傷害夠不夠」；Boss
  完全不受影響（血量、狀態都不會變）；順便清空敵方彈幕，畫面整個乾淨清爽
- 自爆怪（bomber）被核彈波及時，原本死亡會觸發的範圍爆炸效果一樣會觸發
  （這是沿用既有的 `killEnemy()` 邏輯，不是新行為，糖果雨寶箱效果本來就會
  觸發這個連鎖，是一致的）

**測試**：手動塞敵人到場上驗證「有Boss+有小怪」的情境下，引爆後小怪歸零、
Boss血量/存在完全不受影響；也測過真實撿取流程（核彈生成→玩家走到旁邊→
`nukeHeld`變true→場上核彈消失→按鈕出現→點擊引爆→按鈕消失）全部符合預期。

### 5. 誠實的測試發現：第 1 關這輪出現了一次真正的陣亡

這輪跑了 9 次 `py_test9.py 1`（比標準多跑了快 3 倍，因為想確認這輪改動
——尤其暫停/核彈系統多了一個每幀都在跑的 `updateNuke()`——有沒有意外影響
到戰鬥或效能）：

| 結果 | 次數 |
|---|---|
| 零失誤通關 ✅ | 3 |
| 掉 1-2 顆心但過關 | 5 |
| **陣亡（0/3心）** | **1** |

**這是這次 session 裡第一次在第 1 關看到笨bot真的死掉**，不是只掉血。
檢查過核彈/暫停相關的新程式碼——`updateNuke()` 不會碰到玩家的血量、不會
影響笨bot的移動邏輯（笨bot的移動演算法只對附近敵人和場地邊界有反應，
完全不知道核彈道具的存在，撿到也只是路過，不影響行為）——**這次的死亡
不是這輪新功能造成的**。

比較可能的原因：v0.9.7 就已經記錄過的結構性風險——愛心上限從 5 顆固定改成
3 顆之後，同樣的中彈次數換算出來的死亡風險比例變高了（掉 2 顆心以前是
3/5、現在是 1/3，只差一次就死）。這次 9 次裡出現 1 次陣亡，比例上跟這個
已知風險吻合，但樣本數還是不夠大，沒辦法下「風險升高了」這種結論，只能
如實記錄「這是目前為止看到的第一個第1關陣亡案例」。

**建議**：如果之後真人測試（尤其是操作不像笨bot穩定的小朋友）也遇到
第 1 關會死人，這就是該優先檢查的方向——可以考慮把第 1-2 關的出怪率或
Boss血量倍率調低，用 `tests/py_test9.py 1` 連續跑 10 次以上比對死亡比例。

### 6. 環境現況更新

- **版本號**：v0.9.10
- 單一 HTML 檔，約 2,556 行

## 十三、2026-08-16 第七輪工作紀錄（v0.9.11，超級大 Boss 新增「雷射鎖定攻擊」）

### 背景

使用者傳了一個網頁射擊遊戲「X THE GAME」（kettlebelldan.com/x_the_game_v2）的
遊玩影片，問「敵人攻擊的方式，能不能也做在我們的遊戲裡」。用 `ffmpeg` 把影片
拆成 40 張截圖（每秒3張）逐張檢查後，發現：

- 畫面裡大部分特效（藍黃箭頭形「SEEKERS」追蹤彈幕群、多道平行光束「RAIL」）其實
  是**玩家自己的武器**，不是敵人攻擊；`OVERCLOCK`/`NOVA`/`TITAN` 是隨戰鬥時間
  疊加的連段加成數字，也是玩家這邊的系統，跟「敵人怎麼打玩家」沒有直接關係。
- 敵人本身（各社群App圖示：YouTube、Instagram、Facebook、Threads、TikTok）
  在看到的畫面裡大多只是漂浮的靶子，沒有主動開火。
- 真正找得到、可以歸類成「敵人（Boss）攻擊」的畫面，是 Boss「Zuck」
  （祖克柏頭像+觸手，觸手末端是各App圖示）打出的**一道直線紅色雷射光束**，
  從 Boss 位置斜線射向玩家——這跟遊戲畫面上其餘的放射狀彈幕視覺完全不同。

**誠實說明**：只抽樣看了 40 張截圖中的 9 張（不是逐格分析），可能漏看其他
攻擊型態；但這道直線雷射是唯一一個看得夠清楚、能明確判斷「這是衝著玩家來的
攻擊」的畫面，所以以這個為準。

### 可行性評估與決定

GOO BLASTER 現有的 Boss 攻擊系統（`cross`/`spiral`/`ring` 三種，見 `ETYPE`
旁邊的 `BOSS_POOL`）全部是「以 Boss 為中心往外放射」的彈幕，玩法是「離開一個
點」；影片裡的直線雷射需要玩家「離開一條線」，是全新的閃避技巧，值得加。

但這個新機制**沒有直接加到全部 10 關的一般 Boss 戰**，原因：一般 Boss 從
第 1 關就會遇到，遊戲目前的關卡設計刻意遵守「一次只教一個新機制」的鋸齒
原則（`ETYPE` 旁邊那大段註解記錄過踩坑經驗）。如果讓所有 Boss 都多一個新
攻擊，等於在最溫和的新手關也塞新東西，跟既有設計哲學衝突，也需要對全部
10 關重新抓難度。

改成**只給「超級大 Boss」（`superBoss:true`，全遊戲只有第 6、10 關各一隻，
本來就是每個難度層級裡最強的一隻、有專屬警示訊息跟粉紅血條）加這個新招式**：
影響範圍最小、最符合「這是特別的 Boss，理應有特別的招式」的既有敘事，也
不會打亂其餘 8 關已經調好的難度曲線。

### 實作細節

- Boss 物件新增 4 個欄位（`spawnBoss()` 裡）：`laserCD`（下次觸發前的倒數，
  初始 `rnd(2.6,3.6)` 秒）、`laserWarnT`（警示線倒數）、`laserFireT`（開火
  倒數）、`laserAng`（鎖定角度）。
- 只有 `e.superBoss===true` 才會跑這段邏輯（一般 Boss 完全不受影響，用測試
  程式驗證過，見下方測試小節）。
- 流程：`laserCD` 倒數到 0 → 鎖定玩家「當下」角度（`laserAng=ang`，之後
  0.68 秒警示期間不再更新，玩家可以趁機移動閃開）→ 畫面出現紅色虛線警示
  0.68 秒 → 沿鎖定角度打出直線光束，持續 0.35 秒 → 光束期間每幀用
  `raySegDist()`（新寫的「點到射線段最短距離」函式）算玩家到這條線的垂直
  距離，小於 24px（+玩家半徑）就算命中，呼叫既有的 `hurtPlayer()` 扣血；
  因為 `hurtPlayer()` 本身有無敵幀（至少 0.9 秒）而光束只開火 0.35 秒，
  自然只會扣一次血，不用額外寫防重複命中的邏輯 → 開火結束後重新抽下一次
  `laserCD`（`rnd(3.0,4.2)` 秒，若 Boss 進入半血狂暴狀態則乘 0.7 縮短）。
- 畫面：警示階段是紅色虛線（透明度隨時間增強，提醒玩家「快到了」）；開火
  階段是紅→粉→白三層疊加的粗光束，跟既有敵彈（紅色菱形）用同一套「紅=危險」
  視覺語言，但形狀完全不同不會混淆。
- 音效：新寫兩個（`SFX.laserWarn()` 上揚提示音、`SFX.laserFire()` 短而尖銳
  的雷射聲），跟既有音效（受傷/爆炸/Boss登場）都不同，聽感上分得出這是新招式。

### 測試

寫了新的測試腳本 `tests/py_laser_boss.py`，四項全過：

1. 直接用 `spawnBoss()` 強制生一隻超級大 Boss（關掉牠原本的環形彈幕，避免
   干擾量測），跑 22 秒觀察 `laserWarnT`/`laserFireT`，確認警示線會出現，
   而且開火會照週期重複觸發（測到 4 次，不是只觸發一次就卡住）。
2. 玩家全程站在鎖定的雷射線上 → 開火時確認真的扣血。
3. 警示期間讓玩家往垂直方向閃開雷射線 → 開火時確認完全不扣血（證明判定
   跟畫面一致，不是「畫面躲開了其實還是會中」）。
4. 生一隻一般 Boss（`superBoss` 為 `false`）→ 跑 5 秒確認 `laserWarnT`/
   `laserFireT` 完全不會被觸發，證實新邏輯真的只影響超級大 Boss。

另外也測過「不靠手動塞值、走真實關卡流程」的路徑：開啟第 6 關、把笨bot
掛上去玩，直接把 `G.t` 跳到超級大 Boss（業績KPI巨獸）出場前 3 秒，讓牠
用正常的 `updateSpawn()` 觸發機制自己出現——確認警示線跟開火都有在真實
遊戲流程裡正常觸發，過程中瀏覽器 console 完全沒有錯誤或例外。

也重新確認過：這個改動只影響 `e.superBoss===true` 的物件，第 1 關沒有
超級大 Boss，跑 `tests/py_test9.py 1` 三次（0 掉血 / 掉 1 顆 / 掉 2 顆，
JS 錯誤：無），結果落在這個 session 前面幾輪已經記錄過的正常雜訊範圍內
（沒有新增的錯誤或崩潰，跟這次改動的程式路徑本來就沒有交集，是預期中的
噪音，不是新問題）。

### 環境現況更新

- **版本號**：v0.9.11
- 單一 HTML 檔，約 2,633 行

## 十四、2026-08-17 第八輪工作紀錄（v0.9.12，核彈特效加強 + 暫停鍵改樣式 + Boss警示音 + 勝利音效加強）

使用者這輪回饋四點，全部屬於「已經有這個功能，但體驗不夠好」類型，不是新機制：

### 1. 核彈引爆畫面效果加強 + 專屬爆炸音效

- **畫面**：原本只有一層粒子噴發（50顆），現在疊加**三層由內而外擴散的衝擊波環**
  （白核心 → 橘中層 → 暗紅外環，起始時間差各差 0.05~0.13 秒營造層次感），
  加上**全螢幕白/橘閃光**（`G.nukeFlash`，仿照既有受擊紅閃 `G.hurt` 的做法，
  獨立用真實時間衰減、不受慢動作影響），粒子數從 50 顆加到 90 顆、範圍加大；
  `shake`/`hitstop` 也從 16/0.18 提高到 20/0.22，撞擊感更強。
- **音效**：原本是借用 Boss 死亡音效（`SFX.bossDie()`），現在新寫專屬的
  `SFX.nukeBlast()`——深沉次低音衝擊 + 主要爆裂噪音 + 延遲 0.14 秒的「碎片
  飛濺」噪音尾巴，聽起來是「一次性的大爆炸」，跟「Boss倒下的悶哼」明確區隔開。
- 衝擊波環的資料結構是新加的 `G.SHOCK` 陣列（半徑隨時間擴大、越擴越淡，跟
  既有的 `G.PT` 粒子系統平行、互不影響），畫在世界座標（跟著鏡頭一起動），
  全螢幕閃光則畫在 `drawHUD()` 的螢幕座標層（不受鏡頭影響，這樣才能真的「蓋滿全螢幕」）。

**測試**：直接呼叫核彈按鈕，確認引爆瞬間 `G.SHOCK.length===3`、`G.nukeFlash`
接近 1、`SFX.nukeBlast()` 剛好呼叫 1 次（用乾淨頁面單獨測，避免重複掛
instrument wrapper 導致誤判呼叫次數——這點在測試過程中真的踩到一次，是
測試腳本自己的 bug，不是遊戲程式碼的問題，最後用乾淨頁面重測確認排除）；
1 秒後確認 `G.SHOCK`/`G.nukeFlash` 都已經衰減回 0，不會卡住殘留。截圖確認
三層衝擊波環+粒子在畫面上清楚可見（因為核彈自帶 `hitstop(.22)` 會讓遊戲
暫停 0.22 秒，衝擊波環在這段時間也會跟著凍結——這是刻意的，`hitstop`
本來就是让整個遊戲世界一起停格 0.22 秒再爆開，效果上反而更有「定格後炸開」
的張力，不是 bug）。

### 2. 暫停鍵改成可愛樣式

原本是「深色底 + 一圈細灰線框」，使用者反應看起來不可愛。改成跟遊戲其餘
按鈕同一套「糖果色漸層 + 立體投影」語言（用跟 `.btn.alt` 一樣的紫色漸層
`#c9b3ff → #8257ff`），加白色描邊、按下去有 Q 彈的位移動畫，尺寸也從 34px
稍微放大到 38px。截圖確認新樣式（紫色漸層圓形按鈕，跟靜音鍵的深色圓圈
明確做出視覺區隔，不會混淆成同一顆按鈕）。

### 3. Boss 出現警示音效

畫面上原本就有「⚠ BOSS即將出現」的倒數數字（出場前 5 秒開始），但完全沒有
配聲音，純畫面提醒。新增：

- 倒數視窗剛開始的那一幀響一聲**兩響警報**（`SFX.bossWarn()`，像警笛「喔喔」
  兩響），跟既有的 `SFX.bossIn()`（Boss 真正登場時的低沉撞擊音）刻意做出
  區隔——一個是「快來了」的預警，一個是「已經來了」的衝擊，不會混淆。
- 倒數數字每跳一次整數秒（5→4→3→2→1）補一聲很輕的「嗶」
  （`SFX.bossWarnTick()`），比警報聲小聲很多，避免五連音吵到蓋過背景音樂。

實作上要注意：`bossWarnT` 這個值本來就是「每幀重算、不存狀態」的設計
（沿用搖桿判定同一套哲學，註解裡有記錄），要偵測「剛進入警示視窗」這個
瞬間，只能比較「上一幀 vs 這一幀」的邊界，不能額外存一個「有沒有響過」的
布林旗標——不然同一隻 Boss 的警示視窗結束後，旗標忘記重置的話下次就永遠
不會再響。

**測試**：把 `G.t` 直接跳到 Boss 出場前 4.9 秒，用乾淨頁面單獨監聽
`SFX.bossWarnTick()` 每次呼叫時的 `G.t`/倒數秒數，確認精準對應
5→4、4→3、3→2、3→2、2→1 這四次整數秒邊界（不多不少），`SFX.bossWarn()`
在視窗剛打開時剛好響一次。（過程中第一次用「同一個頁面連續測四項功能、
每項都重新掛一層 instrument wrapper」的方式測試，結果呼叫次數量出來
是雙倍/三倍的——後來查出是測試腳本自己的 bug：每次重新掛 wrapper 會把
「上一層 wrapper」也包進去，導致同一次真實呼叫被算了兩次，跟遊戲本身
的邏輯無關。改成每項功能都用全新頁面單獨測之後，數字就精準符合預期了。
特別記錄這個踩坑經驗，因為一開始看到數字對不上，第一直覺以為是程式碼有
bug，多花了一輪排查才確認是測試工具的問題。）

### 4. 過關勝利音效加強

原本 `SFX.win()` 其實已經有聲音（一段上行音階 + 低音墊），但使用者反應
「除了煙火還需要有慶祝勝利的音效」，代表現有的聲音不夠有「慶祝」的感覺，
比較像一般的「完成提示音」。加大：音階跑完後疊一層像歡呼／彩帶灑落感的
寬頻噪音（由高音緩緩掃向低音），最後再補一個大三和弦當作「勝利定格」的
收尾，整體從「一串上行音符」變成有頭有尾、更飽滿的小型勝利號角。

**測試**：用乾淨頁面直接呼叫 `endGame(true)`，確認 `SFX.win()` 剛好呼叫
1 次（前面提到的「重複掛 wrapper 導致誤判呼叫次數」問題這裡也踩到過一次，
一樣用乾淨頁面重測排除）。音樂好不好聽、有沒有真的比較有慶祝感，這件事
沒辦法用 Playwright 驗證，需要使用者實際打開遊戲聽過才能定案。

### 5. 全關卡回歸測試

跑過全部 10 關的 smoke test（每關 `start()` 後跑 200ms 立即結束），確認
`BUILD` 版本號正確、Boss 數量跟預期一致（第1-2關1隻、第3-5關2隻、第6關3隻
[含超級大Boss]、第7-9關3隻、第10關4隻[含超級大Boss]），過程中瀏覽器
console 完全沒有錯誤。另外 `tests/py_test9.py 1` 跑了 3 次（掉1顆 / 0掉血 /
掉1顆，JS錯誤：無），落在已知的雜訊範圍內，沒有新的崩潰或錯誤——這輪
改動雖然有兩處摸到「每幀都在跑」的程式碼（`loop()` 裡新增的 `nukeFlash`
衰減、`update()` 裡新增的 `G.SHOCK` 更新迴圈），但都是新增的獨立邏輯，
沒有動到既有的戰鬥/移動判定，跟這次的雜訊結果沒有因果關係。

### 環境現況更新

- **版本號**：v0.9.12
- 單一 HTML 檔，約 2,720 行

## 十五、2026-08-17 第九輪工作紀錄（v0.9.13，使用者要求「檢查有沒有哪邊有bug」，抓到兩個真的會影響遊戲體驗的邏輯錯誤）

這輪不是回應功能需求，是使用者直接要求做一次系統性抓蟲。逐一讀過全部
函式（`grep` 出所有 `function` 定義，一個一個看邏輯），抓到兩個確認會實際
影響遊戲體驗的 bug，都不是「理論上可能」，是真的寫程式重現、量到數字才
確認的。以下記錄清楚，方便之後想起「這輪到底改了什麼」。

### Bug 1：範圍傷害/貫穿子彈會漏打到大約一半的敵人

**根因**：JavaScript 的 `for(const x of 陣列)` 在陣列內容被同一輪迴圈裡的
程式碼刪除（`splice()`／`shift()`）時，會直接跳過「剛好遞補到被刪除那格」
的下一個元素——這不是猜測，是 JS 語言規格本身的行為（`for...of` 用的陣列
迭代器是照「目前索引」抓值，元素被刪除後面的都會往前遞補一格，迭代器的
內部游標卻已經往下一格走了，於是那個遞補上來的元素被跳過、完全沒被迭代到）。

遊戲裡至少 4 個地方有這個模式：一個範圍傷害函式 `hurtEnemy()` 會在敵人死亡
時呼叫 `killEnemy()` 對 `G.E`（敵人陣列）做 `splice()`，而外層偏偏用
`for(const e of G.E)` 在迭代同一個陣列，邊迭代邊刪：

1. `damageArea(x,y,r,dmg,doKb)`——範圍傷害共用函式，被自爆怪爆炸、進化版
   泡泡槍的「霓虹果凍暴雨」、連鎖引爆果凍（`igniteGoo`）呼叫。
2. 「糖果雨」寶箱（`CHEST_TYPES` 裡 `id:'nuke'` 那個，跟 v0.9.10 加的核彈
   道具是完全不同的兩個系統，只是剛好都叫 nuke，純屬命名巧合）——效果是
   對所有非Boss敵人打140點傷害。
3. 玩家子彈打中敵人的主要判定迴圈——對貫穿子彈（`pierce>=1`，例如溜溜球
   武器）影響最明顯，打穿一群怪的時候偶爾會有一隻完全沒中彈判定。
4. `igniteGoo()` 本身也有同樣模式，只是踩雷條件比較窄（要果凍疊到
   `GOO_MAX`=64 顆同時存在才會真的觸發），一起改掉不留地雷。

**實測重現**（改之前）：塞 10 隻血量 10 的怪疊在同一個點，打一發 140 點的
範圍傷害（照理說每一隻都必死），結果只有 5 隻真的死掉，另外 5 隻血量完全
沒變、原地不動——「糖果雨」寶箱跟貫穿子彈也用同一個腳本重現，數字一樣是
少了一半。

**修法**：把這 4 處的 `for(const e of 陣列)` 全部改成從陣列尾端往前跑的
索引迴圈 `for(let i=陣列.length-1;i>=0;i--)`——這是專案裡本來就在用的既有
寫法（例如子彈/粒子/晶核的更新迴圈都是這樣寫），從尾端往前跑的話，
`splice()` 只會影響「已經跑過的、更前面」的索引，不會讓還沒檢查的元素被
跳過。改完後同樣的重現腳本，10 隻怪全部正確死亡。

**回歸測試**：全 10 關 smoke test（無錯誤）+ `py_test9.py 1` 跑 3 次（0掉血/
0掉血/掉1顆，JS錯誤：無，落在已知雜訊範圍內，沒有變差反而看起來略好——
合理推測是因為範圍傷害以前會漏怪、現在確實全部命中，笨bot受到的反擊機會
變少了，但樣本數不足以下定論，這裡誠實標註為推測）+ 前一輪的雷射Boss
四項隔離測試全部重跑一次確認沒被這輪改動波及。

### Bug 2：一次跨好幾級會漏發升級卡（選卡視窗互相蓋掉）

**根因**：`gainXP(n)` 用 `while(P.xp>=P.xpNext){ ...; showCards(); }`，如果
一次給的經驗值大到一口氣跨好幾個等級門檻（例如同一幀內連續撿到很多顆
晶核、或磁力系升級一次把散落各處的晶核全部吸過來），這個 while 迴圈會在
「同一次同步執行」裡連續呼叫好幾次 `showCards()`，而 `showCards()` 每次
都是直接覆蓋 `cardsEl.innerHTML`，後面呼叫的會把前面呼叫的整組選項蓋掉。
畫面上玩家只會看到「最後一次」的3張卡可以選，選完就恢復遊戲——但等級
其實跳了好幾級，前面幾級「應該要選的卡」就這樣悄悄消失了，沒有任何錯誤
訊息、玩家也感覺不出來自己虧了。

另外，中期 Boss 死亡時「直接送一張卡」的獎勵（`killEnemy()` 裡
`setTimeout(()=>showCards(),420)`）也是直接呼叫 `showCards()`，如果玩家
剛好在那 420 毫秒之間因為撿晶核同時跨等級、卡片視窗已經開著，一樣會被
蓋掉，等於少了一次選擇機會。

**實測重現**（改之前）：`gainXP(500)` 讓玩家從 Lv.1 衝到 Lv.10（跨9級），
用監聽包裝確認 `showCards()` 真的被呼叫了9次，但畫面上 `#cards .card` 只有
3個（1組），只能點一次就恢復遊戲——9次的升級機會只兌現了1次。

**修法**：加一個 `G.pendingCards`（還欠幾張卡沒選）計數器。`gainXP()` 的
while 迴圈裡改成只累加 `G.pendingCards++`，不直接開視窗；迴圈跑完之後，
只有「目前沒有卡片視窗開著」時才觸發一次 `showCards()`。`applyCard()` 選完
一張卡後，先把 `G.pendingCards--`，如果還有剩就馬上接著呼叫 `showCards()`
跳出下一組選項（不恢復遊戲），直到選滿手上欠的張數才真的 `G.paused=false`
恢復遊戲。中期 Boss 的獎勵卡也改成先 `G.pendingCards++`，只有視窗沒開著時
才觸發，這樣不管玩家當下是不是剛好也在選等級卡，都不會互相蓋掉，最多就是
排隊多欠一張、選完前一張自動接著跳出來。

**實測驗證**：`gainXP(500)` 跨9級後，正確要點9次卡片才恢復遊戲，每次都是
新的一組3張選項，最後 `G.pendingCards` 歸零、`G.paused` 正確變回
`false`；另外模擬「先跨級開著卡片視窗、中期Boss又在這時候死掉」的情境，
確認 Boss 的獎勵卡有正確排進佇列而不是蓋掉正在選的那組，依序點完
`pendingCards` 歸零的次數剛好等於「等級跨越次數＋Boss送的那 1 張」。

**回歸測試**：全 10 關 smoke test（無錯誤）+ `py_test9.py 1` 跑 3 次（0掉血/
0掉血/掉1顆，JS錯誤：無，落在已知雜訊範圍內）。這個修改沒有動到任何戰鬥
數值，只動到選卡視窗的顯示時機，理論上不該影響難度，測試結果也符合預期。

### 這輪抓蟲方法（記錄給未來對話參考）

不是憑感覺猜哪裡可能有問題，是系統性做的：先 `grep` 出全部 `function`
定義列成一張清單，一個一個讀過去；找到「迴圈裡呼叫可能刪除同一陣列元素
的函式」這種模式後，用 Playwright 的 `evaluate()` 直接呼叫遊戲內部函式
（不用真的玩過去）建構最小可重現案例（例如手動塞 10 隻怪到 `G.E` 裡、
直接呼叫 `damageArea()`），量出「應該死幾隻」跟「實際死幾隻」的落差，
確認是真的 bug 而不是猜測；修完之後用同一個重現腳本驗證數字對了，
再跑完整的關卡 smoke test + `py_test9.py` 難度基準線確保沒有引入新問題。
兩個 bug 都是這樣一步一步從「懷疑」變成「確認」再到「修好並驗證」的。

### 環境現況更新（v0.9.13 時點）

- **版本號**：v0.9.13
- 單一 HTML 檔，約 2,767 行

---

## 十六、v0.9.14：關卡從 10 關擴充到 100 關，每 10 關一隻大 Boss

使用者需求（逐字）：
1. 將總關卡設置為100關，遊戲難度要跟隨著增加。
2. 遊戲變更為每十關一個大BOSS關卡，分別為10、20、30以此類推。
3. 主選單的上的關卡圖示，如果有大BOSS關卡，請將圖示換成BOSS的圖案，BOSS圖案從恐怖開始變得越來越恐怖，到100關的時候變成超恐怖圖示，代表很難的意思。
4. 完成上面的動作之後，要再重複3次偵錯、測試的循環。

### 架構決策：為什麼不是 90 個手打關卡，而是公式生成

原本 10 關是逐關手動調校、跑過大量笨bot測試的數字，一個一個手打數值風險
很高（90 關全部手填容易手滑打錯、難度曲線容易不連續）。改成寫一個
`genExtraLevels()`，在原本 10 關的 `LEVELS` 陣列後面，用公式把第 11~100 關
（第 2~10 章，每章 10 關）直接 `push` 進去：

- 前 10 關（第 1 章）完全不動，一個數字都沒改——這是已經驗過的基準，不能
  因為架構改動就連帶影響。
- 第 2~10 章的每一關數值 = 第 10 關的數值 × 倍率，倍率公式沿用原本
  docs/02「鋸齒難度曲線」的精神：章節開頭比上一章頂點回落 7%（`st`），
  章節內線性爬升到章節末（大Boss關）的頂點（`pk = 1 + c*0.30 + c*c*0.028`），
  只是把原本「2 關一層」的鋸齒粒度放大成「10 關一章」。
- `espd`（敵人移動速度）和 `bspd`（子彈速度）刻意設上限（1.85 / 2.3）：
  速度快到玩家躲不掉之後，繼續加快只是「看不到、躲不掉」，不是有意義的
  難度。真正的難度加壓交給 `hp`／`bossHp`（打更久、Boss 更肉）、
  `rate`／`dur`（出怪更密、關卡更長）、`atkSlow`（Boss攻擊冷卻更短，
  下限 0.35）。第 100 關的 hp 倍率約 9.55×、bossHp 倍率約 10.15×，相對
  沒有上限——因為血量/耐久本來就是「越後面越硬」這個設計意圖的載體。

**發現的架構限制**：`buildBosses(L)` 原本用一個寫死的三段式判斷
（`nboss===1?...:nboss===2?...:[三個位置]`）決定一般 Boss 的出場時機，
`BOSS_POOL` 也只有 3 隻手工設計的怪。這代表「一般 Boss 數量」架構上被
鎖死在最多 3 隻，不能單純把 `nboss` 調更大就自動生出更多一般 Boss（陣列
會直接超出邊界變成 `undefined`）。因此第 11~100 關全部維持 `nboss:3`，
難度加壓完全來自數值縮放而不是 Boss 數量成長——這是為了控制風險刻意做的
範圍限縮，沒有去重寫 `buildBosses()` 的出場時機邏輯。

### 大 Boss 改成每 10 關一隻

- 第 6 關的 `superBoss:true` 被拿掉（新規則下，大 Boss 只在 10 的倍數關卡）。
- 原本唯一一隻超級大 Boss 常數 `SUPER_BOSS`（業績KPI巨獸）展開成
  `BOSS_CHAPTER` 陣列，10 筆，index 對應章節（0=第10關...9=第100關）。
  index 0 的數值跟原本 `SUPER_BOSS` 完全一樣、一個數字都沒改，避免動到
  第 10 關已經測過的平衡。後面 9 隻是新設計的角色，沿用「上班族崩潰」的
  命名主題一路加碼：週一晨會惡魔、無限加班幽靈、已讀不回怪、年終考核修羅、
  信箱爆炸魔王、責任制黑洞、交接怨靈、沉默審判者、終極KPI審判神。
  血量、速度、攻擊冷卻都隨章節緩步提升（避免跟關卡自己的 `bossHp` 倍率
  疊加後爆炸式成長），主要的難度落差還是交給 `bossHp` 倍率本身。
- `buildBosses(L)` 原本寫死引用 `SUPER_BOSS`，改成查
  `BOSS_CHAPTER[L.bossChapter||0]`——`bossChapter` 是新加在關卡設定上的
  欄位（0~9），只有大 Boss 關才有這個欄位。
- v0.9.11 做的雷射鎖定攻擊（`if(e.superBoss){...}`）完全不用改，因為它是
  照 `superBoss` 布林值判斷、不管是哪個章節的 Boss，只要是大 Boss 關就會
  自動套用——這是架構設計時就刻意留的彈性，這次直接吃到紅利。

### 主選單銀河圖：大 Boss 關卡圖示

- 每個 `BOSS_CHAPTER` 項目都加了一個 `icon` 表情符號欄位，從第10關的 👹
  一路加碼到第100關的 🔥（👺💀☠️👻🧟😈👿🐲 是中間 9 隻的圖示）。
- `buildGalaxy()` 原本每個節點只會顯示數字或 ✓ 或 🔒，現在多判斷一種情況：
  如果這關是大 Boss 關、而且還沒破關，就改用（原本存在但完全沒被用到的）
  `.gic` 這個 CSS class 顯示 Boss 圖示，取代原本的數字。
- 第 100 關額外加一個 `mega` class：邊框變紅、圖示字級再放大、加上紅色
  脈動光暈動畫（`gpulseMega`），蓋掉原本 `.cur` 的白色光暈，讓「全遊戲
  最恐怖」這件事在畫面上一眼就看得出來，不用點進去看說明才知道。
- `renderStage()` 裡原本寫死的「你已經打穿全部 10 關」改成動態讀
  `LEVELS.length`，全破畫面才會正確顯示「100 關」。

### 存檔相容性（沒有 bump PROG_KEY）

`clampProgress()` 本來就是動態算 `LEVELS.length+1`，舊玩家原本存的進度
（1~11，代表「破到第幾關」）在新版下完全還是有意義的數字——原本破到第10
關（進度值11）的玩家，新版會直接解鎖到第11關（深淵章節開頭），無縫接上
新內容，不需要、也不應該 bump 存檔版本號讓進度歸零。

### 測試（照使用者要求，初次驗證之後又重複跑了 3 次完整循環，共 4 輪）

每一輪循環都包含：
1. `LEVELS` 陣列健檢（總數100、大Boss關位置剛好在10/20/.../100、
   `BOSS_CHAPTER` 剛好10筆、第6關不再是大Boss關、第10關維持大Boss關）。
2. `buildBosses()` 對章節Boss關卡（第10/20/60/100關實測）挑到的 Boss
   名字、血量是否正確對應到該章節的 `BOSS_CHAPTER` 設定。
3. 銀河圖節點渲染：大Boss關顯示對應圖示、一般關顯示數字、鎖住顯示🔒、
   第100關有 `mega` class。
4. `renderStage()` 全破文字動態顯示100（不是殘留的寫死10）。
5. `py_laser_boss.py`（雷射鎖定攻擊 4 項隔離測試，因為 `SUPER_BOSS` 改名
   `BOSS_CHAPTER` 連帶把測試腳本裡的引用從 `SUPER_BOSS` 改成
   `BOSS_CHAPTER[0]`，數值完全沒變，純粹是改名同步）。
6. `py_test9.py 1`（第1關笨bot零失誤基準線）跑 3 次。

以上 1~5 每輪都是全部通過、零例外。第 3、4 輪額外多做了兩件事，把測試
覆蓋面拉大到真正的 100 關而不只是資料結構層級：
- 對全部 100 關逐一呼叫 `buildWaves()`／`buildBosses()`，檢查有沒有
  NaN、負數、Boss 血量爛掉等問題——**100 關全部乾淨，零異常**。
- 用 Playwright 實際 `start()` 進遊戲，抽樣跑過大Boss關（10/20/30/50/70/
  90/100）、每章開頭的回落關（11/21/31/.../91）、章節中段關卡
  （5/15/25/45/65/85/95/99/100），監聽 console error 跟 pageerror——
  **全部零錯誤，敵人數量隨關卡遞增的走勢也符合預期**（例如第91關同時
  在場敵人數 56 隻、明顯比第11關的 8 隻多，難度曲線確實在往上爬）。

### 誠實揭露：第1關笨bot基準線這次測到的數字，以及它跟這次改動無關的理由

這次 4 輪循環總共跑了 17 次 `py_test9.py 1`（含中途多跑的 5 次診斷），
零失誤（掉 0 心）12 次、掉心 5 次，比例約 71%。單看數字會讓人緊張，但這
**完全落在這個專案本來就記錄過的已知雜訊範圍**（docs-04-append.md 前面
幾輪都寫過：零失誤比例本來就在 55-75% 之間浮動，笨bot本身有隨機成分：
升級卡是隨機亂點、走位只感知170px內的敵人）。

更重要的是：這次 v0.9.14 的改動**完全沒有碰到第1關會執行到的任何程式碼
路徑**——`LEVELS[0]`（第1關設定）一個欄位都沒改、`buildBosses()` 裡只
動到 `if(L.superBoss)` 那個分支的內部（第1關沒有 `superBoss`，永遠不會
進到這個分支）、`genExtraLevels()` 只在遊戲載入時執行一次、不呼叫
`Math.random()`、也不影響任何遊戲內的隨機數呼叫順序。逐行檢查過後，
沒有任何機制能讓這次的改動影響第1關的實際輸出結果。

**結論（不是猜測，是照著程式碼路徑推導出來的）**：這次測到的掉心分佈是
既有的、跟 v0.9.14 無關的笨bot固有變異，不是這次改動引入的新問題。標註
給使用者參考：如果哪天想認真把這個基準線拉到接近 100%，那是另一個獨立的
「重新調第1關數值」的任務，跟這次的100關擴充是分開的兩件事。

### 環境現況更新

- **版本號**：v0.9.14
- 單一 HTML 檔，約 2,852 行
- 總關卡數：10 → **100**（第 11~100 關由 `genExtraLevels()` 公式生成，
  不是手打進 `LEVELS` 陣列字面量裡）
- 大 Boss 關卡：從「第 6、10 關」改成「每 10 關一次（10/20/.../100）」
- 新增測試腳本 `tests/py_100level.py`（100關擴充的資料結構、Boss對應、
  銀河圖圖示、全破文字的專屬測試）
- `tests/py_laser_boss.py` 因應 `SUPER_BOSS`→`BOSS_CHAPTER` 改名同步更新
  （純改名，數值不變）

---

## 十七、v0.9.15：每關配不同調的音樂、攻擊範圍先窄後靠升級卡撐開

使用者需求（逐字）：
1. 為每一關卡配上不同調的音樂
2. 攻擊範圍太廣，應該設計一開始的攻擊範圍比較窄，可以透過增強工具擴張攻擊範圍，
   不要感覺敵人都還沒出現在畫面裡就被攻擊死亡了

### 1. 每關不同調的音樂

**沒有做的事**：真的手工作 100 首不同的曲子。不切實際，維護成本也太高，
跟這個專案「零外部音檔、純程式合成」的一貫做法不搭。

**做的事**：背景音樂引擎本來就是「和弦進行 + 十六分音符琶音」的程式合成器
（v0.9.8 做的可愛/緊張雙主題），在同一套引擎上疊兩層可以獨立控制的變化：

- **移調**（`M.keyOffset`）：用五度圈間隔排出 12 個調（`KEY_OFFSETS`，公式
  `(i*7)%12` 再把超過半個八度的值折回負數，讓相鄰調聽起來自然相關、不是
  隨機半音亂跳）。實際加總的地方只有一處——`NOTE()` 這個 MIDI→Hz 的唯一
  轉換函式，不用改每一個呼叫點。
- **換一套琶音節奏型**（`M.arp`）：原本只有一組 16 步的 `ARP`，現在有 5 組
  （`ARP_VARIANTS`），聽起來像是同一套配器換了不同的旋律線走法。

`SFX.musicSetLevel(lvIdx)` 直接用 `lvIdx%12` 選調、`lvIdx%5` 選節奏型，兩個
組合起來要到第 61 關才會跟第 1 關完全重複（12 和 5 的最小公倍數是 60）——
**這是誠實的技術限制，不是「100 關都獨一無二」**：世界上只有 12 個半音，
音樂性地移調也不可能無限展開，60 關內每一關都不重複，已經是在「不做假音檔」
前提下能做到的上限。`start()` 在 `musicStart()` 之後立刻呼叫
`musicSetLevel(LV_IDX)`；主選單的音樂維持原本的 C 大調＋原版節奏型不變
（`musicStart()` 本身會先重置回預設值），不會因為玩家剛玩過某一關而被帶歪調。

**測試方式**：因為 `M`／`KEY_OFFSETS`／`ARP_VARIANTS` 都是 SFX 模組內部的
閉包變數，沒有對外暴露，沒辦法直接讀值驗證。改成攔截
`AudioContext.prototype.createOscillator`、記錄實際排程出去的頻率
（Hz），用「聽到的聲音」反向驗證：第 1 關兩次呼叫 `musicSetLevel(0)`
結果完全一致（證明是可重現的、不是隨機亂跳）、第 1 關跟第 2 關的音符序列
完全不同（證明真的換了調＋節奏型）、第 1 關跟第 61 關的音符序列完全相同
（證明 60 關循環的數學是對的）。全部通過。

### 2. 攻擊範圍先窄、靠升級卡撐開

**根因**：三把武器的射程原本各自寫死——水槍 760、塗鴉 520、溜溜球 900——
但手機螢幕的可視對角線通常只有 400~500px 左右，這些數字幾乎全部大於螢幕
可視範圍，導致遊戲原本用來限制鎖定範圍的 `onScreen()` 檢查根本不會被
射程數字擋到，實際上等於沒有真正意義上的射程限制，敵人常常還沒進入
畫面就已經被自動鎖定打死。

**做法**：新增一個玩家狀態 `P.atkRange`（一開始 550px，比三把武器裡最短的
塗鴉 520 略寬一點點、比水槍 760 跟溜溜球 900 都窄不少），三把武器開火時
改成 `nearest(P.x,P.y, Math.min(武器自己的射程, P.atkRange))`——武器原本的
射程數字還在，變成「上限」，實際打多遠由 `P.atkRange` 決定。新增升級卡
「望遠糖鏡」🔭（`UPGRADES` 陣列最後一張，`max:2`），每疊一層 `P.atkRange`
+140，疊滿 2 層後變成 830——水槍(760)、塗鴉(520，本來就比550窄，其實從
頭到尾都沒被這次改動動到)這時候都已經打開到原本的射程，溜溜球(900)則
維持著「疊滿望遠糖鏡也還沒完全打開」的長射程專武定位，沒有被這次改動
抹平掉武器彼此的差異。

**平衡測試（這是這次改動裡風險最高的部分，因為直接動到核心戰鬥數值）**：
第一次試做時 `P.atkRange` base 設 420，跑 `py_test9.py 1`（第1關笨bot
零失誤基準線）**連續 3 次全部失敗**（每次都掉1顆心，不是雜訊常見的
0~3顆亂跳，是很一致的「每次都掉1顆」），判斷是真的把起始射程壓太窄、
影響到笨bot開局來不及清怪。調高到 550 之後重測：3 次裡 2 次零失誤、
1 次掉1顆，之後再追加跑 4 次全部零失誤，總計 7 次裡 6 次零失誤
（約86%），落在（甚至優於）這個專案本來就記錄過的已知雜訊範圍
（55-75%）。最終定案 `ATK_RANGE_BASE=550, ATK_RANGE_STEP=140`。

**測試**：新增 `tests/py_range_music.py`，涵蓋：起始射程確實比武器原始
射程窄、`nearest()` 真的會把範圍外的敵人排除在鎖定之外（用兩隻敵人、一隻
在範圍內一隻在範圍外的最小重現案例）、望遠糖鏡升級後範圍確實變大、疊滿
2 層後這張卡不會再出現在 `rollCards()` 的卡池裡、音樂的可重現性與差異性
（見上一節）。另外對第 1/10/20/50/100 關做了 3 秒鐘的實跑 smoke test，
確認新的射程限制程式碼在所有章節都不會噴錯誤。

### 環境現況更新

- **版本號**：v0.9.15
- 新增玩家狀態欄位 `P.atkRange`（起始 550，望遠糖鏡每疊一層 +140，疊滿
  2 層＝830）
- 新增升級卡「望遠糖鏡」🔭
- 每一關開始播放專屬的（調性,琶音節奏型）組合，12種調×5種節奏型，
  60 關一循環（第 61 關重複第 1 關的組合）
- 新增測試腳本 `tests/py_range_music.py`

---

## 十八、v0.9.16：修死鎖 bug + 重做難度曲線（承接驗證報告的 P0）

這一輪是照 `docs-06-驗證與獲利設計.md` 的結論執行前兩項最高優先的修正。

### 1. 修好會讓遊戲永久凍結的死鎖 bug

**問題**：升級卡全部拿滿之後 `rollCards()` 回傳空陣列，但 `showCards()` 還是照樣
把 `G.paused` 設成 true、打開卡片視窗——視窗裡一張卡都沒有，玩家永遠點不到東西，
`G.paused` 永遠回不去 false，整個遊戲永久凍結，只能強制關掉 App、整局作廢。

**修法**：防呆做在 `showCards()` 這個共同入口（兩個觸發點——`gainXP()` 的升級、
Boss 死亡送的獎勵卡——都會走到這裡，一次修掉兩條路徑）。卡池為空時：
把 `pendingCards` 歸零、關閉視窗、`G.paused=false` 恢復遊戲，並給補償
（愛心沒滿就補一顆，滿了就給大量晶核），讓升級不會變成完全沒有回饋。

**驗證**：修改前後跑同一支重現腳本。修改前：卡片數 0、視窗開著、`G.paused=true`、
2 秒後 `G.t` 仍為 0（完全停止）。修改後：視窗關閉、`G.paused=false`、
`pendingCards=0`、2 秒後 `G.t=1.98`（正常前進）。

### 2. 重做難度曲線

**問題**（v0.9.14 引入、v0.9.15 沒抓到）：敵人強度從第1關到第100關成長約 24 倍，
但玩家每一關都被 `newGame()` 重置成 `lv:1, wep:{bubble:1}, hearts:3`，起跑點
完全沒動。實測滿等滿裝在第100關只撐 9 秒、笨bot在第50關只撐 7 秒且升不到 Lv.2，
陷入「打不動→沒經驗→更打不動」的死亡螺旋。

**三管齊下的修正（缺一不可）**：

1. **敵人成長大幅放緩**：`peakMul(c)` 從 `1+c*0.30+c*c*0.028`（第100關 5.97 倍）
   改成線性 `1+c*0.17`（第100關 2.53 倍）。換算成「敵人壓力」（血量倍率 ×
   出怪率），從第1關的 477 倍降到 102 倍。
2. **起始裝備隨章節提升**（新欄位 `L.startWep`）：第11關之後不再從一把 Lv.1 水槍
   開始，第91~100關開局就有 `{bubble:5, graffiti:4, yoyo:4}`。
   ※ 這是「還沒做局外成長系統」之前的過渡方案，等永久強化商店做出來之後，
   這張表應該改成由玩家自己買的永久強化來決定，而不是寫死在關卡設定裡。
3. **經驗值倍率隨章節提升**（新欄位 `L.xpMul`，第11關 1.55 倍 → 第100關 5.95 倍）：
   關卡內的升級速度跟得上敵人的成長。

**另外找到並修掉的真正殺手**：`atkSlow` 是乘在**每一隻普通敵人**的開火冷卻上
（`e.shootCD=rnd(e.cdMin,e.cdMax)*CUR().atkSlow`），不是只影響 Boss。v0.9.14 用
`base.atkSlow/mul` 讓它一路掉到下限 0.35，等於每隻怪的開火速度變成第10關的
2.3 倍，再乘上敵人數量也翻倍——**彈幕密度是「相乘」的**。改成隨章節微幅加快
（`max(0.62, base.atkSlow-c*0.02)`），密度的成長交給敵人數量與血量去做。
彈幕壓力（敵人數/atkSlow）從「第10關的 4.7 倍」降到「1.48 倍」。

同時把 `maxE` 上限從 260 砍到 150，一石二鳥：既降低彈幕密度，也解掉 O(n²)
敵人分離迴圈的效能引爆點。

### 3. 測試方法本身的三個教訓（這輪踩到的坑）

這輪在測試方法上連續踩了三個坑，全部記錄下來避免重蹈：

**坑1：測到「現實中到不了的情境」**。上一份報告說「第100關只有12fps」是用程式
強制把敵人塞到 maxE 上限量到的，但實測真實遊玩玩家 7 秒就死、敵人根本累積不
起來（真實遊玩其實有 48~51fps）。**效能問題是真的但潛伏的**——要先修好難度、
玩家能撐完整關，敵人累積到上限時才會爆。教訓：測極端情境之前，先確認那個情境
在真實遊玩中到得了。

**坑2：對單次雜訊調參數**。第10關的程式碼完全沒動，但兩次測試的成績從 86.9%
掉到 75.4%。笨bot本身有隨機成分（升級卡隨機點、沒敵人時隨機繞圈），單次結果
不可信。`tests/py_balance.py` 因此加了 `-nN` 參數支援重複取平均。

**坑3：平行頁數會影響結果**。9 頁平行時第10關掉到 44%，4 頁時是 75%——CPU 競爭
干擾了測量，**跨批次的數字不能互相比較**。最後改用 **A/B 對照**：同一批次裡
一半用舊參數、一半用新參數（用 `page.evaluate` 在執行期把 LEVELS 改回舊公式），
條件完全相同，差異才能歸因到參數本身。

### 4. 驗證結果

**A/B 對照（滿等滿裝，同批次8頁平行，條件相同）**：

| 關卡 | v0.9.15 舊 | v0.9.16 新 | 改善 |
|---|---|---|---|
| 50 | 20.7% | 29.6% | 1.43 倍 |
| 100 | 8.3% | 25.5% | **3.07 倍** |

**難度曲線（滿等滿裝，同批次，每關2次取平均）**：

| 關卡 | 走完比例 | 說明 |
|---|---|---|
| 10 | 46.9% | 使用者原本調好、未改動的關卡（參考基準） |
| 20 | 39.1% | |
| 50 | 29.4% | |
| 100 | 23.8% | |

曲線現在單調平滑、沒有斷崖。**第100關從「比第10關難 5.6 倍」變成「難 2.0 倍」**。

**第1關基準線**：先用程式直接驗證第1關完全沒被改到（`startWep` 未設定、
`xpMul` 預設 1、起始武器 `{bubble:1}`、rate/hp/espd/atkSlow/maxE/dur 全部與
原本一模一樣），再跑 `py_test9.py` 8 次：5 次零失誤（62.5%）、8 次全部通關，
落在文件記載的 55-75% 雜訊帶內。

**回歸測試**：`py_100level.py`、`py_laser_boss.py`、`py_range_music.py` 全部通過。

**效能**：第100關不節流從 26.5fps 提升到 32.7fps、2倍節流從 12.2fps 提升到
15.1fps。有改善但**還沒到位**——O(n²) 敵人分離迴圈還在，空間網格優化仍是待辦。

### 5. 誠實的結論：難度問題只解決了一部分

改善是實測、可歸因的（A/B 對照 3.07 倍），曲線也平滑了，但**滿等滿裝在第100關
仍然只走到 24% 就陣亡**。這不是調參數能徹底解決的問題——根本原因是架構：
**玩家每一關都從零開始，戰力上限固定，敵人卻一路成長**。

真正的解法是 `docs-06` 裡列的第 3 項：**局外成長系統（糖果幣＋永久強化商店）**。
有了永久強化，玩家的戰力上限才會跟著關卡一起成長，這 90 關才有可能是「困難但
打得過」而不是「困難且打不過」。這一輪的 `startWep` 只是那之前的過渡方案。

### 環境現況更新

- **版本號**：v0.9.16
- 單一 HTML 檔，約 2,963 行
- 新增關卡欄位：`startWep`（起始武器配置）、`xpMul`（經驗值倍率）
- `maxE` 上限：260 → 150
- 新增測試工具 `tests/py_balance.py`（平行難度測試，支援 bot/god 兩種模式與
  `-nN` 重複取平均）——這是這個專案第一個能自動驗收「關卡打得贏嗎」的工具

---

## 十九、v0.9.17：局外成長系統（糖果幣 + 永久強化商店）

這是 `docs-06-驗證與獲利設計.md` 執行順序的第 3 項，也是整份報告裡「投資報酬率
最高的單一項目」——因為它**一個系統同時解三個問題**：

- **難度**：玩家可以靠累積永久強化跨過打不過的關卡（牆 → 坡）
- **留存**：每一場失敗都有收穫，roguelite 的核心迴圈才成立
- **商業**：免費試玩累積的進度，就是付費解鎖時「損失趨避」話術的著力點

### 1. 存檔格式改寫（含舊存檔遷移）

v0.9.16 之前存檔裡**只有一個整數**（打到第幾關）。現在改成 JSON
（`{progress, coins, meta}`），key 從 `gooblaster_progress_v2` 升到
`gooblaster_save_v3`。**舊存檔會自動遷移**：讀不到新 key 時去讀舊 key，
把破關進度搬過來、糖果幣從 0 開始，老玩家的進度不會歸零（實測：舊存檔
寫入「47」，載入後 `PROGRESS=47`）。

⚠ 實作時踩到的順序問題：`loadSave()` 一定要等 `genExtraLevels()` 跑完才能呼叫，
因為遷移時會呼叫 `saveGame()` → `clampProgress()`，而後者是用 `LEVELS.length`
當上限。如果在關卡生成前讀檔，`LEVELS` 還只有 10 筆，破關到第 50 關的玩家
會被夾成 11。

### 2. 十項永久強化（`META_UPGRADES`）

糖心護體（起始愛心）、糖霜利刃（傷害）、急速攪拌（攻速）、愛用武器（起始武器
等級）、遠視糖鏡（攻擊範圍）、晶核親和（經驗獲得）、吸引磁鐵（吸取範圍）、
翻滾大師（加速冷卻）、**重生蠟燭（陣亡時原地復活，每關限用）**、糖幣獵人（幣收益）。

設計原則：每一項的第 1 級都很便宜（讓玩家第一次進商店就買得起、立刻有正回饋），
之後逐級變貴（給長期目標）。效果全部是「起始狀態」的加成，跟關卡內的升級卡
系統是**相加**而不是互相取代。實作上刻意做成獨立的 `applyMeta(G)` 函式，
讓「關卡給的起始值」跟「玩家累積的強化」在程式碼上是分開的兩層。

**重生蠟燭**是最直接的「跨過卡關」手段：愛心歸零時原地滿血復活、給 3.2 秒
無敵、清空場上所有敵彈（避免復活完馬上又被同一波彈幕打死 = 白買）。

### 3. 糖果幣結算：死掉也要給

`runCoins(win, lvIdx, kills, tSurvived, dur)`，**不管輸贏都給**——「死掉也有收穫」
是 roguelite 留存迴圈的核心，沒有它，打不過的關卡就是一堵完全的牆，玩家不會
「再試一次」而是直接刪遊戲。通關有 1.55 倍加成，關卡越後面基礎值越高（否則
玩家會發現刷第 1 關最有效率，那會毀掉整個進程設計）。

**⚠ 這裡踩到一個會毀掉整個進程的坑**：第一版用 `kills*0.55` 線性計算，實測
**第 50 關通關一場就給 7376 幣**——佔買滿全部強化總成本（8930）的 83%，等於
玩家一旦打得動後期關卡，商店就瞬間全滿、進程設計直接崩掉。原因是後期關卡的
擊殺數本來就是前期的幾十倍，線性換算必然爆炸。改成**次線性**
（`kills^0.62 * 2.4`）並加上**每局上限 520**，現在單場最高收益只佔買滿成本的
9.3%，買滿全部大約需要 30 場左右（而且不是每個玩家都要買滿，買前 3-4 項最
關鍵的就足以跨過卡關）。`tests/py_meta.py` 把這條做成了自動驗收
（單場收益佔比必須 < 20%）。

### 4. 結算畫面：近失回饋（近失效應）

照 `docs-06` 研究的賭場心理學「近失效應」——拉霸出現兩個一樣的符號在數學上
跟全部不同一樣是輸，但感覺更接近贏，玩家更想再拉一次。結算畫面現在會顯示
「**走到 87%，再撐 23 秒就過關了！**」取代單純的 GAME OVER，讓失敗的那一局也
留下「差一點」的敘事，而不是「我就是打不過」。同時顯示這局賺到的糖果幣與總額。

### 5. 驗證：這個系統真的把「牆」變成「坡」了嗎？

**A/B 對照（笨bot，同批次 6 頁平行，條件相同）**：

| 關卡 | 沒有永久強化 | 永久強化買滿 | 改善 |
|---|---|---|---|
| 20 | 5.7%（擊殺61, Lv.4） | 91.2%（擊殺2576, Lv.41） | **16.0 倍** |
| 50 | 4.3%（擊殺82, Lv.4） | 81.7%（擊殺5317, Lv.80） | **19.0 倍** |
| 100 | 2.4%（擊殺62, Lv.5） | 59.5%（擊殺4353, Lv.90） | **24.8 倍** |

注意「角色 Lv.」那一欄：沒有永久強化時笨bot只能升到 Lv.4-5 就死了，
有永久強化之後能升到 Lv.41~90——**「打不動→沒經驗→更打不動」的死亡螺旋
被打破了**，這正是這個系統要解決的核心問題。

（笨bot完全不閃子彈，所以就算是設計良好的關卡也不會 100% 通關；重點是
相對改善的幅度，以及死亡螺旋有沒有被打破。）

### 6. 測試

新增 `tests/py_meta.py`，六項自動驗收：舊存檔遷移、糖果幣結算規則、
**經濟平衡防呆**（單場收益佔買滿成本必須 < 20%）、購買後存檔持久化、
永久強化正確套用（**且沒買時起始數值必須跟 v0.9.16 之前一模一樣**，
確保第 1 關基準線不被動到）、重生蠟燭。全部通過。

回歸測試：`py_100level.py`、`py_laser_boss.py`、`py_range_music.py` 全部通過。
`py_test9.py` 第 1 關基準線跑 4 次全部通關（零失誤 2/4，落在 55-75% 雜訊帶內）。

UI 用截圖確認過商店與主選單的外觀，跟遊戲原本的糖果風格一致。

### 環境現況更新

- **版本號**：v0.9.17
- 存檔格式：JSON（`gooblaster_save_v3`），含舊存檔自動遷移
- 新增 10 項永久強化、糖果屋商店介面、結算畫面的近失回饋
- 新增測試腳本 `tests/py_meta.py`

---

## 二十、v0.9.18：四項使用者回報問題（含三輪偵錯測試循環）

### 1. 按著螢幕滑動時，很難按核彈與加速鍵

這題有**兩個各自獨立的成因**，兩個都修才會好：

**成因A（加速鍵）**：加速鍵原本一律是「放開才觸發」（v0.9.4 為了避免把按鈕區
當搖桿拖曳時誤觸），而且要通過「位移 < 16px、時間 < 400ms」的判定。但那個顧慮
只在「單指」時成立——如果畫面上已經有另一根手指在當搖桿，這根新落在按鈕上的
手指就不可能是搖桿，意圖非常明確就是要按按鈕。
修法：`e.touches.length>1` 時直接在 touchstart 立刻觸發，不必等放開、不必通過
位移判定；單指時維持原本的放開才觸發。另外把判定放寬成 24px / 600ms
（玩家幾乎永遠在拖曳搖桿，另一手點按鈕時手掌會晃，原本的判定太嚴苛）。

**成因B（核彈鍵／暫停鍵）**：這兩顆是 DOM 按鈕，靠瀏覽器合成的 `click` 事件。
但 canvas 的 touchstart/touchmove 都有 `e.preventDefault()`（搖桿必須要，否則會
觸發頁面捲動與長按選字），**在多點觸控的情況下，部分瀏覽器不會為那次觸控合成
click** → 按了完全沒反應。
修法：新增 `bindTapBtn(el, fn)`，讓按鈕自己聽 `touchstart` 直接執行，繞開整個
click 合成機制；同時用一個 400ms 的旗標擋掉緊接著可能還是會來的合成 click，
避免同一次點擊被觸發兩次。

### 2. 右上方的按鈕會蓋住增加的數值

`果凍 X% 傷害 +Y%` 與 `速度 ×N` 原本畫在 `top+34` / `top+52`，但右上角疊著兩顆
DOM 按鈕——暫停鍵（top+14 起、高 38，佔到 top+52）與靜音鍵（圓心 top+78、
半徑 17，佔到 top+95）。整個 `top+14 ~ top+95` 的右側都被按鈕蓋住，而這幾個
數值正是全場唯一會一路往上跳的**成長型數值**，玩家最想看的東西剛好被擋住。
修法：改畫在按鈕下緣之後（`top+112`），並把這個座標抽成 `RIGHT_HUD_Y` 常數。

### 3. 首頁的糖果屋無法滑動

`#shopList` 只寫了 `#shopList *{touch-action:pan-y}`（子元素），**漏掉容器本身**。
最上層的 `*{touch-action:none}` 會把 `#shopList` 自己鎖死，而瀏覽器判斷「這根手指
能不能捲動」看的是「被摸到的元素 + 它所有祖先」的交集——只要鏈上任何一層是
none 就整條斷掉。`#galaxyWrap` 當初就踩過同一個坑，它是容器本身跟 `*` 兩條都寫了
才會動，這裡漏了容器那條。修法：補上容器自己的 `touch-action:pan-y` 與
`-webkit-overflow-scrolling:touch`。

### 4. 關卡圖上下顛倒 + 每 5 關一個背景

**順序反轉**（第 1 關在最下方、第 100 關在最上方，玩家往上爬）：
`y = padY + (n-1-k)*stepY`。這是刻意的心理學設計——「往上爬」是人類對「進步」
最直覺的空間隱喻（爬山、爬梯、排行榜），把已通關的路留在腳下、未挑戰的關卡在
頭頂延伸出去，玩家每次打開選單都會直接看到「我爬了多高」，比由上往下的清單
更能製造成就感與繼續前進的動機。

**每 5 關一個背景色帶**（100 關 = 20 帶）：長達 6800px 的路線如果從頭到尾同一個
背景，玩家往上滑時完全沒有「我到了新地方」的感覺，捲動變成一件無聊的事。
切成 20 個色帶之後，每爬 5 關就會看到景色變一次，把「捲動」本身變成一種有回饋
的推進感。銜接做法：每帶的背景是「自己的顏色 → 上一帶顏色」的直向漸層，相鄰
兩帶在交界處顏色天然相同，看起來是連續的漸變而不是硬邊；交界再疊一條發光細線
強化「進入新區域」的儀式感。

⚠ 實作時發現：一個章節是 10 關 = 2 個色帶，如果兩帶都直接用該章的 tier 色，
章節內的那次換帶就完全看不出變化，「每 5 關換背景」等於只做到一半。所以奇數帶
把顏色往青色方向偏移並調暗（`shiftHex`），確保每 5 關都有可見的色彩變化，同時
仍看得出它跟同章節的另一半是同一個色系。

### 5. 三輪偵錯測試循環（使用者長期要求）

**第 1 輪**：新寫的 `tests/py_ui_fixes.py`（用真實 CDP 觸控事件而非合成 JS 事件，
才能驗證多點觸控這種瀏覽器層級行為）+ 全套回歸，全過。

**第 2 輪**：主動針對這次改動找邊角案例（不只重跑）——多點觸控狂點加速鍵冷卻
會不會被繞過、暫停鍵會不會 touchstart+click 雙觸發、關卡數不是 5 的倍數時色帶
會不會超出、反轉後點節點還能不能正確選關、中途進度的玩家開選單會不會捲到自己
的關卡、糖果屋捲到底後買東西畫面會不會跳掉。全過。

**第 3 輪**：**抓到兩個第 1、2 輪沒出現的失敗**，兩個都是測試工具本身的問題
（已各自查證確認不是遊戲的 bug），這正是跑三輪的價值：

- **暫停鍵測試偶發失敗**：`#btnPause` 在 `start()` 剛呼叫完的當下仍然是 `.hide`
  （`display:none`），要等 `drawHUD()` 跑過一幀才顯示。測試沒等就取
  `getBoundingClientRect()` → 拿到全 0 的矩形 → CDP 觸控打到 (0,0) 什麼都沒碰到。
  第 2 輪剛好過了、第 3 輪多個測試平行搶 CPU 時才浮現。
  驗證：明確量測 `start()` 前後與等一幀後的 `hidden` 狀態確認假設成立，改用
  `wait_for_selector("#btnPause:not(.hide)")` 後連跑 5 次全過。
- **音樂測試偶發失敗**：`musicStart()` 只是重置排程器，「已經排進 Web Audio 的
  音符」還是會照樣發聲，上一次取樣殘留的音符會流進這一次的 `__notes`，讓序列
  從中間開始。實際抓到的 `lv1_a` 是序列第 5~8 個音符而不是前 4 個。
  修法：取樣前先 `musicStop(false)` 並等 350ms 讓已排程的音符播完再清空陣列。

**最終驗證**：兩個測試加固後，全套六支測試在「最嚴苛條件」（同時平行跑、
互相搶 CPU，也就是當初讓問題浮現的條件）下全部通過。
第 1 關基準線三輪各跑 3 次共 9 次，全部通關，零失誤 7/9。

### 環境現況更新

- **版本號**：v0.9.18
- 新增 `bindTapBtn()`（DOM 按鈕改聽 touchstart，繞開多點觸控下的 click 合成問題）
- 新增 `RIGHT_HUD_Y` 常數、`shiftHex()` 色彩偏移
- 銀河圖：順序反轉（第1關在最下）、每 5 關一個背景色帶 + 發光銜接線
- 新增測試 `tests/py_ui_fixes.py`、`tests/py_ui_edge.py`
- 加固測試 `tests/py_range_music.py`（音符取樣的時序污染）

---

## 二十一、v0.9.19：多語系（預設英文，11 種語言）

語言選擇的完整研究見 `docs-07-語言市場研究.md`。這一節只記技術實作。

### 1. 語言清單（11 種）

英文（預設）、繁體中文、简体中文、日本語、한국어、Deutsch、Français、
Español、Italiano、Português (BR)、Русский。

= 業界標準的 EFIGS + CJK 八語言，再加繁簡分開、巴葡、俄語。挑選標準是
**營收潛力**而不是使用人數（使用者的長期原則 B）。

### 2. 架構決策：資料表「就地改寫」而不是到處包 T()

顯示這些字串的程式碼散落在幾十個地方（升級卡、HUD、商店、戰報、銀河圖、
關卡資訊…），如果逐一改成 `T('key')` 風險很高、也很容易漏。改成：
**`applyLanguage()` 在切換語言時，把當前語言的文字直接寫回資料表的顯示欄位**
（`WEAPONS[k].name`、`UPGRADES[i].n/.d/.d2`、`LEVELS[i].n/.d`、`BOSS_CHAPTER[i].name`、
`CHEST_TYPES[i].n`、`META_UPGRADES[i].n/.d`…）。所有既有顯示程式碼一行都不用動，
只有這一個進入點需要維護。

### 3. ⚠ 難度層級 tier 從「中文字串」改成「數字索引」

原本 `L.tier` 是 `'新手'`、`'地獄'` 這種中文字串，而且**同時被拿來當
`TIER_COLOR` 的查表鍵**。一旦翻譯成別的語言，查表就會回傳 `undefined`，
整個關卡的顏色會壞掉。

改成：`L.tier` 是 0~13 的數字，`TIER_COLOR` 改成陣列（用索引查），
顯示名稱走 `tierName(i) = T('tier'+i)`。**資料與顯示徹底分離**，
這是做多語系時最容易被忽略、但一定會踩到的坑。

### 4. 第 11~100 關用樣板組合，不用翻 90 個名字

`genExtraLevels()` 不再直接寫死中文名稱，改成存 `idxInCh`（章節內第幾關）、
`kind`（boss/opener/mid）、`ch`（第幾章），由 `applyLanguage()` 依語言用
`genName`／`genBossName`／`genDescBoss`／`genDescOpener`／`genDescMid`
這 5 個樣板組出來。**90 關只需要翻 5 個樣板句**，而不是 90 個名字 × 11 種語言。

### 5. 語言偵測與存檔

優先序：**玩家手動選過的 > 瀏覽器語言 > 英文**。
- 繁簡分流：`zh-TW / zh-HK / zh-MO / zh-Hant` → 繁體，其餘 `zh` → 簡體
- 葡萄牙語：任何 `pt` 開頭（含 `pt-PT`）都導向巴葡（業界慣例）
- 認不出來的語言（例如泰文）→ 回退英文
- 缺 key 時自動回退英文，不會出現空白或 `undefined`
- 語言存進存檔的 `lang` 欄位，重開遊戲會記得

### 6. 這輪抓到的問題（三輪偵錯的成果）

**① 我自己在建置腳本裡犯的錯（兩個）**
- 用字串手術往 JSON 裡插 key，結果插進了「值的內部」——因為英文的
  `genDescMid` 值本身含有跳脫引號 `\\"`，我的引號比對找錯位置，把 L10N 弄壞。
  修法：**改成從 Python 原始檔重新產生整個 L10N 區塊整段替換**，不做字串手術。
- 用 `repr().replace("'",'"')` 產生字面量，把法文 `d'attaque`、西文 `¡FRENESÍ!`
  裡的撇號也一起換掉造成語法錯誤。修法：一律用 `json.dumps()`。
- 兩次都是因為在寫檔前先 `assert`，所以檔案沒有被寫壞，重跑就好。

**② 韓文翻譯混進西里爾字母**：第一版的 `congrats` 值是 `"전부 клиа..."`，
中間混進了俄文字母。自動檢查（掃描非中日語言裡有沒有出現漢字、掃描各語言
有沒有混入不該有的字集）把它抓出來了。

**③ 全域 `T()` 與區域變數 `T` 的命名衝突（潛在地雷，已修）**：
`spawnEnemy()` 裡原本有 `const T=ETYPE[type]`，會把新增的全域翻譯函式 `T()`
在整個函式內遮蔽掉。目前剛好沒壞（那個函式沒有要翻譯的字串），但**只要之後
有人在裡面加一行 `T('...')` 就會噴「T is not a function」**。已改名成 `ET`
徹底消除遮蔽。這是靠 grep 全檔 `T.` 屬性存取才發現的，不是靠測試——
測試不會抓到「現在沒事、以後會爆」的問題。

**④ 兩支既有測試因為介面改英文而失效（測試問題，不是遊戲問題）**：
`py_ui_edge.py` 寫死了比對「第 50 關」這個中文字串。改成用遊戲自己的
`T('stageOf',50,...)` 產生「目前語言」的預期字串，任何語言下都成立。

**⑤ 測試斷言太嚴格**：`py_ui_edge` 原本斷言「狂點加速鍵後 `dashCD` 必須 > 0」，
但實測拿到 `-0.02`——冷卻剛好充滿，程式不會把它夾在 0。要驗的其實是
「冷卻沒有被繞過」，改成**計算實際衝刺次數**（狂點 10 次不該換來 10 次衝刺）。

**⑥ 音樂測試的時序污染（第二次踩到，這次修徹底）**：v0.9.18 加了 350ms 固定
等待仍然偶發失敗。根因是 `musicStop()` 內部是 `setTimeout(...,120)` 之後才
`clearInterval`，也就是停止後還有約 120ms 會繼續排新音符 → 出現「輪詢期間
看似穩定、之後又冒出音符」的**假穩定**。修法：先固定等過 120ms 清理期，
再輪詢到音符不再增加，然後才清空並開始取樣。

### 7. 驗證

新增 `tests/py_i18n.py`，六項自動驗收：
1. 預設語言是英文
2. 瀏覽器語言自動偵測（13 種 locale 逐一驗，含 zh 繁簡分流、pt→pt-BR、
   不支援的語言回退英文）
3. 11 種語言都沒有缺 key（各 199 個），且非中日語言沒有殘留漢字
4. 切換語言後所有資料表（關卡/武器/升級卡/永久強化/Boss/寶箱/按鈕）全部跟著換
5. 語言偏好會存檔，重新載入後保持
6. tier 改數字索引後，`TIER_COLOR` 查表仍然全部有效

三輪偵錯測試：第 1 輪抓到 2 個測試失效、第 3 輪抓到 2 個（斷言太嚴 + 音樂
時序），全部查證確認是測試工具問題後修正，最後在「七支測試同時平行跑」的
最嚴苛條件下全數通過。

**效能未退化**：第 1 關 16.7ms（v0.9.18 是 16.8ms），第 100 關反而從
20.9ms 改善。每幀新增的 15 處 `T()` 呼叫沒有造成可觀測的成本。

### 8. 補跑驗證（沙盒指令執行故障恢復後）

交付當下沙盒的指令執行功能故障，有一個改動（`spawnEnemy()` 的 `T`→`ET` 改名）
沒能跑到測試。恢復後全部補驗完成，**交付的檔案沒有再改動過，就是通過驗證的那一版**：

- **語法檢查**：通過
- **全套 8 支測試 × 3 輪**（含七支同時平行的最嚴苛條件）：全數通過
- **新增 `tests/py_spawn.py` 專門驗證改名**：4 種敵人 × 4 個關卡（第1/7/50/100關），
  逐一比對 `r`／`hp`／`spd`／`dmg`／`hue`／`xp`／`shootRange`／`cdMin`／`tier`／
  `ranged`／`boom`／`kite` 是否正確從 `ETYPE` 帶過來，並檢查有沒有 NaN／undefined。
  **16 組全部正確、零 JS 錯誤**。

### 9. ⚠ 修正一個一直被沿用的錯誤基準數字（55-75%）

`py_test9.py` 第 1 關「零失誤通關比例」這個驗收指標，文件從很早期就寫著
**55-75%**，之後每一輪都拿它當「這是不是雜訊」的判斷依據。

這一輪累積跑了 **23 次**（遠多於當初訂出那個區間時的樣本量），結果是
**零失誤 12 次 ≈ 52%**，持續落在區間下緣或以下。為了確認不是 i18n 造成的迴歸，
做了四項獨立排除，**四項全部證明不存在影響機制**：

1. 第 1 關的遊戲數值（`dur`/`hearts`/`rate`/`hp`/`espd`/`bossHp`/`nboss`/`bspd`/
   `atkSlow`/`chest`/`maxE`/`types`）與玩家起始值逐項比對，完全沒被碰到
2. `applyLanguage()` 只改寫顯示欄位（`.n`/`.d`/`.name`），戰鬥邏輯完全沒動
3. `spawnEnemy()` 改名後屬性 16 組全對（見上）
4. **畫格時間實測：空跑與笨bot實跑都是平均 16.7ms、p99 20ms、
   卡頓幀（>33ms）佔 0%** —— 這排除了「i18n 每幀多做 15 次 `T()` 造成掉幀 →
   dt 變大 → 物理步進變粗 → 碰撞更容易穿透」這個唯一還說得通的間接機制

**結論：55-75% 這個區間本身就是早期小樣本estimate出來的、偏樂觀的數字，
不是可靠基準。以 23 次樣本重新測定，真實值約在 50-55%。**

**這件事的意義不只是改一個數字**：舊的區間會讓未來每一輪都出現假警報
（測到 52% 就以為「基準線被打破」而去追一個不存在的迴歸——這一輪就真的
發生了，花了不少時間排除）。文件與專案現況已同步更新為 **50-55%**。

**真正該看的驗收標準是「有沒有通關」**：23 次全部通關、失敗模式只有掉 1-2 顆心，
從未陣亡。第 1 關對新手依然是穩過的。

### 環境現況更新

- **版本號**：v0.9.19
- 單一 HTML 檔約 3,400 行、276KB（多語系表約佔 81KB）——仍然零外部相依、
  100% 離線可玩
- 新增 `T()` / `L10N` / `LANGS` / `detectLang()` / `applyLanguage()` / `tierName()`
- `L.tier` 型別變更：字串 → 數字索引（0~13）
- `spawnEnemy()` 的區域變數 `T` 改名為 `ET`（避免遮蔽全域翻譯函式）
- 翻譯原始檔在 `i18n/keys_*.py`，用 `i18n/build*.py` 產生並注入
- 新增測試 `tests/py_i18n.py`、`tests/py_spawn.py`；加固 `py_ui_edge.py`、`py_range_music.py`
- 新增 `tests/syntax_check.js`（`node tests/syntax_check.js` 直接檢查語法）
- **`py_test9.py` 第1關零失誤基準線的參考區間修正為 50-55%**（舊的 55-75% 是
  早期小樣本的樂觀估計，會造成假警報；以 23 次樣本重新測定）

---

## 二十二、v0.9.20：完成度稽核的三項修正

依 `docs-08-完成度稽核.md` 執行 A-1 / A-2 / A-3。

### A-1（最嚴重）：永久強化「愛用武器」在後期關卡形同白花錢

**問題**：`startWep`（關卡給的起始武器等級）與永久強化「愛用武器」都是加在
主武器 `bubble` 上，而武器等級上限是 5。舊表在後期章節直接把 bubble 給到 4~5，
**等於把永久強化的額度事先吃光**。實測：

| 關卡 | 花 1070 幣買滿後實際獲得 |
|---|---|
| 1-30 | +3 級 ✅ |
| 31-60 | +2 級 ⚠ |
| 61-90 | +1 級 ⚠ |
| 91-100 | **+0 級 ❌ 完全無效** |

100 關裡約 40 關的購買價值被吃掉。**這對營利目標的傷害在於：糖果幣系統正是
未來付費解鎖的載體**，玩家在免費階段就體驗到「存很久買的東西根本沒用」，
對整個經濟系統的信任會直接崩掉。

**根因是自己埋的**：v0.9.16 的程式碼註解白紙黑字寫著「這是還沒做局外成長系統
之前的**過渡方案**，等永久強化商店做出來就該改掉」。v0.9.17 商店做好了，
**過渡方案卻沒跟著移除**。

**修法**：把 `startWep` 的主武器等級一律壓在 2，永遠留給永久強化完整的 3 級空間
（2+3=5 正好滿級）；失去的開局戰力用副武器（graffiti/yoyo）補回來——它們不受
這項強化影響，不會再互相吃額度，而且開局多把武器手感也更豐富。
各章節的武器總等級刻意維持跟舊表幾乎相同（只有第 91-100 關少 1 級），
避免動到難度曲線。

**驗證**：
- 新增 `tests/py_audit_fixes.py`，逐關檢查「買滿愛用武器實際獲得幾級」→
  **10 個抽樣關卡全部 +3 完整生效**
- 起始武器總等級檢查：第11關=2、第50關=6、第100關=12（與設計相符）
- **A/B 難度對照**（同批次、笨bot）：
  - 沒買永久強化：第30/60/100關差異 -0.4% / -0.5% / -0.8%（絕對值都在 2-5% 低檔，屬雜訊）
  - **買滿永久強化（真實玩家情境）**：+0.3% / -2.1% / +4.6%，難度沒有破壞

### A-2：清除死碼

實測掃描（自動比對「定義處 vs 使用處」）後移除：

| 項目 | 死了多久 |
|---|---|
| `.lvbtn` 及子類別 `.num`/`.txt`/`.t1`/`.t2`/`.dif` | v0.9.5 被銀河圖取代，死了 14 個版本 |
| `.stat` / `.tag` | 同上 |
| `G.best = {dmg:0}` | 半途放棄的「最高傷害紀錄」，從未被讀取 |
| `P.heart` | v0.9.6 移除的「活力粉紅」升級卡留下的層數計數器 |
| `P.vx` / `P.vy` | 玩家是直接改座標移動、不走速度向量，從頭到尾沒用過 |

98 個函式全部有被呼叫，沒有死函式。

### A-3：開發診斷改為預設隱藏

主選單底部與遊戲中右下角的診斷資訊（touch/pointer/audio/座標）原本一直顯示。
它對真機除錯很有價值（v0.9.4「手機無法移動」就是靠它定位的），但這是要上架
賣錢的產品，玩家看到只會覺得「還沒做完」。

改成**隱藏入口：連點版本號 5 下切換**。平常只顯示版本號（使用者確認版本用），
真機出問題時仍然叫得出來。

⚠ **但「偵測不到任何觸控事件」的警告不受此限、永遠顯示**——那是玩家真的
操作不了遊戲的情況，這時候把警告藏起來只會讓他以為遊戲壞掉直接刪掉。

順帶修掉一個小問題：`updateDiag()` 是 700ms 間隔，載入後要等第一次觸發才會
畫出版本號，開頭 700ms 是空白的。補一次初始呼叫。

### ⭐ 順手根治一個纏了三個版本的測試不穩定

`py_range_music.py` 的「每關音樂組合」測試在 v0.9.18/19/20 **連續三個版本、
用三種不同的等待策略都還是會偶發失敗**（多個測試平行搶 CPU 時特別容易）。

根因是它靠攔截 Web Audio 排程出來的音符頻率反推「這關用了哪個調」，但
**Web Audio 是提前排程的**——音符會在 `musicStop()` 之後繼續播、也會在取樣
視窗開始前就排好，導致抓到序列中段而不是開頭。

與其繼續補等待時間，改成從結構上解決：在 SFX 模組加一個測試掛鉤
`musicDebug()`，回傳 `{keyOffset, arpIdx, theme, on}`（兩個數字、無副作用）。
測試從此完全不依賴時序。

改寫後測試反而**驗得更嚴格**：
- 同一關兩次呼叫結果一致（可重現、非隨機）
- **前 60 關的組合全部不重複**（12種調 × 5種節奏型）
- 第 61 關確實回到第 1 關的組合
- 選單音樂（不接 `musicSetLevel`）回到預設調 `0/0`
- 另外保留一項音訊煙霧測試，確認實際播出來的頻率集合真的有跟著換調

### 測試

三輪偵錯測試（9 支測試同時平行 = 最嚴苛條件）**全部三輪全綠**，
音樂測試的長期不穩定問題也一併消失。
第 1 關基準線 6 次：零失誤 5 次（83%），高於參考區間 50-55%。

### 環境現況更新

- **版本號**：v0.9.20
- `START_WEP` 主武器固定 2，副武器補足總等級
- 移除死碼：`.lvbtn` 那組 CSS、`.stat`、`.tag`、`G.best`、`P.heart`、`P.vx/vy`
- 新增 `DIAG_ON` 隱藏開關（連點版本號 5 下）
- 新增 `SFX.musicDebug()` 測試掛鉤
- 新增測試 `tests/py_audit_fixes.py`；`py_range_music.py` 測試4 改用掛鉤、新增測試5

---

## 二十三、v0.9.21 —— 英文版仍然會冒出中文（真人回報的第一個 bug）

### 這是怎麼被發現的

使用者把 v0.9.20 上到 GitHub Pages、用自己的手機玩英文版，第 2 級升級時
截到畫面上出現 **「遠處出現寶箱！快去拿」**。

**這件事本身就是「讓真人玩」的價值示範**：v0.9.19 做完多語系之後，我跑過
`py_i18n.py`（199 key × 11 語言全齊、13 種 locale 偵測、資料表切換）
並在稽核報告 `docs-08` 裡寫下——

> 「英文模式下把主選單／糖果屋／語言選單／重來確認框／暫停畫面／升級卡／
>  結算畫面全部渲染一遍掃描，除了語言選單裡的母語名稱之外**沒有任何中文殘留**」

**這句話是錯的，而且我當時對它有信心。** 錯的原因不是掃描寫壞了，是
**掃描的範圍從一開始就漏掉一整個類別**。

### 根因：掃描只看得到 DOM，看不到 canvas

這個遊戲的文字分成兩類：

| 類別 | 例子 | v0.9.19 的稽核有沒有掃到 |
|---|---|---|
| DOM 元素文字 | 主選單按鈕、糖果屋、升級卡、結算面板 | ✅ 有 |
| **直接畫在 canvas 上的文字** | toast 提示、HUD 指示、分享圖 | ❌ **完全沒掃** |

而漏掉的 7 處全部落在第二類。更關鍵的是：**這 7 處根本沒有經過 `T()`**，
是直接寫死的中文字串——所以就算 L10N 表 100% 完整，它們也永遠是中文。

「L10N 表完整」跟「畫面上沒有中文」是兩個不同的命題，我當時把前者當成後者的證明。

### 找出來的 7 處（外加 1 處死資料）

| 位置 | 原本硬寫的中文 | 觸發時機 |
|---|---|---|
| `spawnChest()` | 遠處出現寶箱！快去拿 | 每關第一個寶箱出現時（**最容易被看到**） |
| 核彈補給生成 | ☢️ 出現核彈補給！快去撿 | 每關一次 |
| 撿到核彈 | 取得核彈！左下角圖示可以引爆 | 撿到核彈時 |
| HUD | 護盾 ×N | 拿到甜甜圈護盾後**持續顯示** |
| HUD | 暴走中！ | 暴走糖觸發時 |
| 結算 + 分享圖 | 存活 M:SS · LV.N · 擊殺 K | **每一局結束都會看到** |
| 結算 + 分享圖 | 果凍覆蓋率 N% | 同上 |
| `ETYPE[].name` | 軟泥／兔子／無人機／自爆 | 死資料，全檔沒有任何地方讀它 → 直接移除 |

前 7 項新增 key（`tChest` / `tNukeDrop` / `tNukeGet` / `hudShield` /
`hudFrenzy` / `shotStats` / `shotGoo`），11 種語言都補齊，**199 → 206 key**。

翻譯用詞刻意對齊既有 key，避免同一個概念在不同畫面用不同字：
Goo 對齊 `hudGoo`（Glibber/Gelée/Gelatina/Gosma/Слизь）、
Kills 對齊 `hudKills`、LV 對齊 `hudLv`（fr/es 用 NIV、it 用 LIV、
pt-BR 用 NÍV、ru 用 УР）、Shield 對齊 `tShieldGain`。

### ⭐ 新工具：`tests/py_cjk_scan.py`

不修完就算了——**把「當初為什麼沒抓到」也一起修掉**。

新測試攔截 `CanvasRenderingContext2D.prototype.fillText / strokeText`，
錄下**畫面上實際被畫出來的每一段文字**，再逐一走過：

- 靜態畫面：主選單＋關卡圖、糖果屋、語言選單、重來確認框
- 13 個會噴 toast 的遊戲事件：寶箱出現／撿到寶箱／核彈出現／撿到核彈／
  引爆核彈／護盾 HUD／暴走 HUD／護盾擋傷／冰凍／Boss 登場／超級大 Boss／
  重生蠟燭／全滿補心
- 暫停、勝利結算、失敗結算、分享圖（`makePolaroid()` 的兩種結果）
- 全 100 關的名稱與說明
- 8 張資料表的所有顯示欄位（UPGRADES / META / BOSS_POOL / BOSS_CHAPTER /
  CHEST_TYPES / WEAPONS / EVOS / ETYPE）

任何中日韓字元都算失敗（語言選單裡「繁體中文/简体中文/日本語/한국어」是白名單，
那是刻意用母語寫的）。

**修正前跑一次會抓到 41 處洩漏、7 種不同字串——先證明測試抓得到，再修。**

另外新增 `tests/py_i18n_new.py` 做逐語言驗收：11 語言 × 7 key 檢查
「有沒有回退成 key 名稱」「`{0}` 佔位符有沒有殘留」「數值有沒有代進去」
「非 CJK 語言不含中日韓字元、CJK 語言確實含該語系字元」，
再加上「執行中切語言、HUD 立刻跟著換（不必重載）」。

### 測試

- 修正前 `py_cjk_scan` 抓到 **41 處洩漏**，修正後 **0 處**
- `py_i18n_new` 11 語言全過
- **11 支測試同時平行跑（最嚴苛條件）全綠**：cjk_scan / i18n_new / i18n /
  meta / audit_fixes / spawn / 100level / laser_boss / range_music /
  ui_fixes / ui_edge

---

## 二十四、v0.9.21（續）—— 翻譯對了，但版面放不下

修完中文洩漏、截圖驗收時看到**更嚴重的一層問題**：英文寶標提示變成
「…ppeared in the distance — g…」，**兩端直接被切出畫面**。

這不是翻譯問題，是**版面問題**：畫面上所有 canvas 文字原本都是固定字級、
固定座標，而那個版面是照**中文字串長度**排的。中文「遠處出現寶箱！快去拿」
10 個字，英文同一句 45 個字元、德文 52 個——長度差 4~5 倍。

### 找到 4 個版面缺陷（全部是「換語言就爆」的同一類）

| # | 問題 | 影響語言 | 嚴重度 |
|---|---|---|---|
| 1 | toast 提示固定 26px、不折行 → 長句被切出畫面兩端 | **英/德/法/西/葡/俄全部** | 🔴 玩家看不到提示內容 |
| 2 | 護盾指示畫在 `hy+56`、關卡名畫在 `hy+54` → **只差 2px，直接疊字** | **全語言（含中文）** | 🔴 但要先抽到甜甜圈護盾才看得到，所以一直沒被發現 |
| 3 | 擊殺數固定畫在 `pad+46`（照中文「LV.99」的寬度抓的）→ 法/西/葡的 `NIV./NÍV.` 較寬，等級三位數時撞上擊殺數 | fr / es / pt-BR | 🟡 高等級才會出現 |
| 4 | 關卡名沒有寬度上限 → 壓到右上角的「果凍%／傷害+%」 | en / de / ru / pt-BR | 🟡 v0.9.20 的 Boss 截圖裡就看得到，當時被我記成「小瑕疵、之後再說」 |

第 2 項要特別記一筆：**它在中文版也是壞的**，跟多語系無關，
只是觸發條件（抽到護盾卡）讓它躲過了所有測試與人工檢查。

### 修法

新增三個 canvas 排版工具，讓版面自己去適應文字長度，而不是反過來要求翻譯遷就版面：

- `fitFont(txt,maxW,weight,base,min)` —— 從基準字級往下縮到放得下
- `wrapText(txt,maxW,weight,sz,maxLines)` —— 有空白的語言折行；CJK 沒空白，交給縮放
- `fitClip(txt,maxW,weight,base,min)` —— 縮到最小仍放不下就截斷加「…」

各處套用：

- **toast**：先縮字級（26 → 最小 15），還不夠就折成 2 行
- **關卡名**：可用寬度**動態量出右上角數值的實際寬度**再扣掉
  （原本先寫死 `W*0.62`，實測俄文「Уровень 40 · Босс главы «Чистилище»…」
  縮到最小字級仍然超過，所以最後必須能截斷）
- **LV／擊殺**：擊殺數的 x 改成 `pad + measureText(LV文字).width + 10`
- **護盾／暴走**：從 `hy+56 / hy+74` 移到 `hy+72 / hy+90`

### ⭐ 新工具：`tests/py_layout_fit.py`

同樣的原則——**把「為什麼沒抓到」也一起修掉**。

攔截 `fillText`，用當下的 `ctx.font` 量出每段文字的真實寬度，
依 `textAlign` 換算左右邊界，然後在 **11 種語言**下逐項檢查：

1. **14 種 toast 提示**都不可以超出畫面（用每種語言**最長的關卡名**當輸入）
2. 左上角 HUD（LV／擊殺／關卡名／護盾／暴走）**兩兩不可以重疊**，也不可以超寬
3. 關卡名不可以壓到右上角的果凍%／速度×

寫這支測試時踩到兩個測試設計問題，一併記下來：

- **世界座標的文字會誤判**：`pop()` 的傷害數字、敵人圖示也走 `fillText`，
  但它們的 x/y 是世界座標（會是負數），拿來判斷「有沒有出界」毫無意義。
  改成只比對「這次 toast 實際要顯示的那段字」。
- **修好之後測試反而失敗**：關卡名被截斷成「前綴…」，原本的完全比對就找不到了。
  改成前綴比對。

**這支測試在修正前會抓到 15 項失敗，修正後全過。**

### 測試（第三輪：最嚴苛條件）

- **13 支測試同時平行跑全綠**：cjk_scan / **layout_fit** / i18n_new / i18n /
  meta / audit_fixes / spawn / 100level / laser_boss / range_music /
  ui_fixes / ui_edge / **ab_2021**
- 第 1 關笨 bot：連續 2 次都是**零失誤通關**
- 幀時間：第 1 關中位 **16.7ms**、卡頓幀 0.5%（與 v0.9.20 相同；
  雖然每幀多了 `measureText`，但那是幾十次字串量測，相對於數百個實體的
  更新與繪製可以忽略）
- zip 解壓後實跑：v0.9.21 / en / 100 關 / 206 key / 無 JS 錯誤

### ⭐ `tests/py_ab_2021.py`：用機制對照取代大量取樣

改完之後第 1 關笨 bot 抽樣一度出現 1/4 零失誤（參考區間 50-55%）。
照方法論第 8 條「先找有沒有影響機制，再看統計」——與其再燒 40 分鐘跑樣本，
不如**直接證明兩版的數值輸入完全相同**。

新測試把 v0.9.20 與 v0.9.21 同時載入，比對：100 關的完整難度參數、
ETYPE 的 13 個數值欄位、tier 倍率表、`START_WEP`、
10 組開局狀態（買滿／沒買永久強化 × 5 個關卡）、永久強化的價格表、升級卡池。

**結果全部 byte-identical** → bot 成績差異必定是隨機雜訊，不存在迴歸機制。
這個手法比「再跑 20 次取平均」快，而且結論更強（是證明，不是統計推斷）。

### 得到的教訓（寫進方法論）

12. ⭐ **「掃過了」要先問「掃描看得見哪些東西」**。DOM 掃描看不到 canvas 上
    的字；資料表完整性檢查看不到「根本沒接上 T() 的硬寫字串」。
    稽核報告寫「沒有殘留」之前，要先能說出這次掃描的**盲區**在哪。
13. ⭐ **修 bug 的同時要修「當初沒抓到它的原因」**，否則同一類問題會再回來。
14. ⭐ **一個真人玩 5 分鐘，抓到了我三輪自動化測試 + 一份完成度稽核都沒抓到的
    缺陷。** 這正是前幾輪一直建議「先讓真人玩」的具體證據。
15. ⭐ **「翻譯齊全」≠「版面放得下」**。多語系有兩個獨立的驗收面向，
    v0.9.19 只做了前者。任何固定字級 + 固定座標的文字，在字串長度可能變 4 倍的
    情況下都是未爆彈。
16. ⭐ **改完 UI 一定要真的看截圖**。這 4 個版面缺陷沒有任何一個是靠讀程式碼
    或跑既有測試發現的，全部是看圖看出來的。
17. ⭐ **懷疑迴歸時，「證明輸入相同」勝過「多跑幾次取平均」**（`py_ab_2021.py`）。

### 環境現況更新

- **版本號**：v0.9.21
- L10N：199 → **206 key** × 11 語言
- 移除 `ETYPE[].name`（死資料）
- 新增測試 `tests/py_cjk_scan.py`、`tests/py_i18n_new.py`、
  `tests/py_layout_fit.py`、`tests/py_ab_2021.py`
- 新增 i18n 來源 `i18n/keys_v0921.py` 與注入器 `i18n/build_v0921.py`
- 新增 canvas 排版工具 `fitFont()` / `wrapText()` / `fitClip()`
- 新增上架素材產生器 `tests/py_cover.py`、`tests/py_shots2.py`
  （itch.io 封面 630×500 + 5 張截圖）
- 測試工具總數：15 → **19 支**

---

## 二十五、v0.9.22 —— 預設語言、Boss 名稱、Boss 外觀

### 🔴 新增的長期指令

**「接下來的修改，都需要在各個語言都進行修改。」**
任何會顯示給玩家看的文字，都必須同時補齊 11 種語言，不能只改英文或只改中文。
這一版新增／覆寫的 14 個 key 全部照做。

---

### 1. 預設語言不是英文（使用者回報）

**根因**：`loadSave()` 在沒有存檔時呼叫 `detectLang()`，跟著手機語系走。
使用者的手機是 zh-TW，所以一打開就是中文版——跟 v0.9.19 的需求
「預設語言為英文」完全相反。**我當時把「自動偵測」當成了「預設英文」的更好版本，
那是我自己加碼的解讀，不是使用者要的。**

**修法**：新增 `const DEFAULT_LANG='en'`，`loadSave()` 一律用它；
只有玩家在主選單 🌐 自己選過語言、存進存檔之後才會優先採用。
`detectLang()` 保留但不再決定預設值（之後可能拿來做「偵測到你的語言，要不要切換？」）。

⚠ 這反轉了 `py_i18n.py` 的測試 2。舊測試斷言「zh-TW → zh-Hant」，
新測試斷言「不管什麼語系一律 en」，另外新增 2b 驗證 `detectLang()` 本身仍然正確。

---

### 2. Boss 名稱重做（13 個 × 11 語言）

舊名是上班族笑話：加班文件怪／未讀訊息怪／已讀不回怪／業績KPI巨獸……
問題有三個：**對兒童客群不好笑、對任何客群都不威風、翻成英文之後更弱**
（"Overtime Paperwork" 完全不像 Boss）。

新命名用「材質 + 頭銜」，頭銜本身就是一條力量階梯——**玩家不用看數值，
光看名字就知道下一隻更兇**：

| | 一般 Boss | |
|---|---|---|
| b1 | Gumdrop Brute | 軟糖蠻兵 |
| b2 | Syrup Warden | 糖漿獄卒 |
| b3 | Jawbreaker Tyrant | 硬糖暴君 |

| 章節 | 英文 | 繁中 |
|---|---|---|
| 1 | Molar Crusher | 臼齒粉碎者 |
| 2 | Sourfang Warlord | 酸牙督戰王 |
| 3 | Licorice Reaper | 甘草收割者 |
| 4 | Frostbite Overlord | 霜噬霸主 |
| 5 | Cinder Devourer | 燼焰吞噬者 |
| 6 | Obsidian Colossus | 黑曜巨像 |
| 7 | Nightmare Sovereign | 夢魘君主 |
| 8 | Abyss Archfiend | 深淵魔君 |
| 9 | Starfall Titan | 隕星泰坦 |
| 10 | **Omega, the Ender** | **終焉·滅世者** |

**每個語言都用自己的頭銜階梯，不是直譯**——所以 fr/es/it/pt 的
Overlord 與 Sovereign 刻意用不同的字（Suzerain / Souverain、
Amo Supremo / Soberano、Dominatore / Sovrano），避免撞名讓階梯斷掉。

銀河圖的章節圖示也跟著換成一條會升級的序列：🦷👹💀🧊🔥🗿👻😈☄️🐉

資料表裡的舊中文名清成空字串（真正的文字一律來自 L10N，跟 WEAPONS/EVOS 一致）。

---

### 3. Boss 外觀特徵系統（六種可混搭）

**問題**：Boss 只是一顆比較大的果凍球加一圈光暈。第 10 關的 Boss 跟第 100 關的
**長得一模一樣**，只有數字不同——玩家對「越打越深、敵人越來越強」零視覺回饋。

**六種可混搭的裝飾**：
`spike` 尖刺／`armor` 盔甲／`horn` 巨角／`crown` 尖冠／`wing` 翼／`aura` 光環

**逐章升級**（章節 Boss，1 件 → 6 件）：

| 章 | 造型 | 章 | 造型 |
|---|---|---|---|
| 1 | 尖刺 | 6 | 尖刺+角+光環 |
| 2 | 盔甲 | 7 | 盔甲+冠+翼 |
| 3 | 尖刺+角 | 8 | 尖刺+盔甲+角+翼 |
| 4 | 盔甲+角 | 9 | 尖刺+盔甲+冠+翼+光環 |
| 5 | 尖刺+盔甲+冠 | 10 | **六種全開** |

一般 Boss 另有一張表，永遠比同章的章節 Boss 樸素一階
（維持「章節 Boss 才是頭目」的階層），且**第 1 章完全樸素**——新手期不要視覺噪音。

**盔甲碎裂**：血量掉到 50% 時盔甲碎裂，噴出 26 顆金屬碎片 + 螢幕震動 + 頓幀 +
音效 + 全語言的「ARMOR SHATTERED!」提示，**並露出底下更多更長的尖刺**
（沒有 spike 特徵的盔甲 Boss 也會長刺——盔甲本來就藏著刺）。

⚠ **刻意做成純視覺、不改任何數值。** 盔甲減傷會改動難度曲線、必須重跑整套平衡測試；
而「階段轉換的爽感」光靠視覺＋震動＋音效就拿得到，風險 0。
由 `py_ab_base.py` 逐項證明所有難度輸入與 v0.9.20 基準 byte-identical。

**外觀做了兩輪修正，兩次都是看圖看出來的**：
1. 翼的第一版是兩條二次貝茲圍成的色塊，遠看像黏在身體兩側的果凍，讀不出是翼
   → 改成上緣大弧 + 下緣三段鋸齒 + 三根翼骨的蝙蝠翼
2. 尖刺第一版用同色系的亮版，在小螢幕上跟身體糊在一起
   → 改成骨色（低飽和高亮度）+ 深色描邊
3. 翼的顏色第一版亮度 24%，深色翼畫在深色背景上等於看不見 → 提到 38%

---

### 4. 關卡數量評估 → `docs-10-關卡數量評估.md`

用 `tests/py_level_audit.py` 實測後的關鍵數字：

- 100 關只有 **4 種敵人組合**，**第 7 關之後 94 關沒有任何新敵人**
- `enemyTier()` 在**第 9 關就封頂**，之後 92 關的雜兵強度分層完全相同
- 相鄰兩關的總體差異：第 2-50 關 **4.20%**，第 51-100 關 **1.03%**
- **第 61-100 關這 40 關的關卡設定完全相同**（時長 323 秒、上限 150、Boss 3 隻、tier 4）
- 全破一次 **476 分鐘（近 8 小時）**，平均每關 286 秒

**結論：有效內容長度大約是 50 關。** 建議改成 **50 關、每 5 關一個章節 Boss**
（剛好用滿 10 種 Boss 造型、獎勵間隔砍半、對上「每 5 關換背景」）。
實作屬於難度改動，排 v0.9.23 並附 A/B 對照與存檔遷移。

---

### 測試（三輪）

- 新增 `tests/py_boss_skin.py`：預設語言 ×7 種手機語系、語言記憶、
  13 名稱 ×11 語言（有值／無舊名／不重複）、逐章造型升級、
  盔甲碎裂只觸發一次、碎裂後長刺、繪製呼叫計數
- 新增 `tests/py_level_audit.py`（關卡數量評估用的實測）
- 新增 `tests/py_boss_gallery.py`（10 章 Boss 造型對照圖，人眼驗收用）
- `py_ab_2021.py` → 改名 `py_ab_base.py`，改成「目前版本 vs v0.9.20 基準」通用比對
- **14 支測試同時平行跑全綠**
- 效能：第 100 關 3 隻六特徵 Boss + 90 小怪 **中位 18.6ms、卡頓幀 0.6%**
  （v0.9.19 記錄的第 100 關是 20.9ms，沒有退化）。
  ⚠ CPU 2 倍節流下 47.8ms —— 那是既有的 O(n²) 分離迴圈，不是這次的裝飾造成的
- 第 1 關笨 bot 2 次：1 次零失誤、1 次掉 2 心，都通關。
  `py_ab_base` 已證明難度輸入與基準 byte-identical，所以是雜訊

### 教訓

18. ⭐ **不要把使用者的需求「升級」成自己覺得更好的版本**。使用者說「預設英文」，
    我做成「自動偵測」，自認為更貼心——結果他的手機打開就是中文，
    需求 3 個版本沒有被滿足，而且我在稽核報告裡還簽核通過了。
19. ⭐ **視覺設計要看圖疊代**。Boss 造型改了三輪，每一輪的問題都只有看圖才看得出來
    （翼讀不出形狀、刺跟身體同色、翼太暗）。
20. ⭐ **拍遊戲外觀圖要先關掉玩家的自動攻擊**——第一版對照圖每一隻 Boss 都是純白的，
    因為武器一直命中、`e.flash` 讓 Boss 永遠處在受擊閃白狀態。
21. ⭐ **呼叫遊戲內部函式前先確認簽名**：`enemyTier()` 不吃參數（讀全域 `G.lvIdx`），
    我第一版傳了參數進去，得到「100 關全部 tier 0」的假結論。

### 環境現況更新

- **版本號**：v0.9.22
- L10N：206 → **207 key** × 11 語言（新增 `tArmorBreak`，覆寫 13 個 Boss 名稱）
- 新增 `DEFAULT_LANG='en'`
- 新增 `BOSS_SKIN_CH` / `MID_SKIN_CH` / `skinHas()` / `drawBossBack()` / `drawBossFront()`
- 新增 i18n 來源 `i18n/keys_v0922.py` 與注入器 `i18n/build_v0922.py`
- 測試工具總數：19 → **22 支**

---

## 二十六、v0.9.23 —— 100 關砍成 50 關，章節 Boss 改成每 5 關

依據 `docs-10-關卡數量評估.md` 的實測結論執行（使用者核可）。

### 核心設計原則：難度範圍完全不縮水

把舊第 100 關的終點**原封不動**搬到新第 50 關。所有「以章節 c 為索引」的參數
（`startWep` / `xpMul` / `atkSlow` / Boss 造型 / 難度頂點）全部不動，
只把「每章 10 關」改成「每章 5 關」。需要重算的只有以關卡編號為索引的三個：
`dur` / `maxE` / `chest`。

**難度頂點重新內插**：舊公式 `peakMul(c)=1+c*0.17`（c=0..9 → 第10/20/…/100 關，
終點 2.53）。新結構的第 1、2 章是手調的第 1-10 關（頂點＝第 10 關＝1.0），
生成的是第 3~10 章（c=2..9），8 章要走完 1.0 → 2.53
⇒ 每章 **+0.19125**（= 0.17 × 9/8），終點完全相同。

`py_50levels.py` 逐項驗證新第 50 關的
`rate/hp/espd/bspd/bossHp/atkSlow/maxE/xpMul/startWep` 全部等於舊第 100 關。

### 新結構

| 章 | 關卡 | 章節 Boss | 造型 |
|---|---|---|---|
| 1 | 1-5 | 第 5 關 Molar Crusher | 尖刺 |
| 2 | 6-10 | 第 10 關 Sourfang Warlord | 盔甲 |
| 3 | 11-15 | 第 15 關 Licorice Reaper | 尖刺+角 |
| 4 | 16-20 | 第 20 關 Frostbite Overlord | 盔甲+角 |
| 5 | 21-25 | 第 25 關 Cinder Devourer | 尖刺+盔甲+冠 |
| 6 | 26-30 | 第 30 關 Obsidian Colossus | 尖刺+角+光環 |
| 7 | 31-35 | 第 35 關 Nightmare Sovereign | 盔甲+冠+翼 |
| 8 | 36-40 | 第 40 關 Abyss Archfiend | 尖刺+盔甲+角+翼 |
| 9 | 41-45 | 第 45 關 Starfall Titan | 尖刺+盔甲+冠+翼+光環 |
| 10 | 46-50 | **第 50 關 Omega, the Ender** | **六種全開** |

手調的第 1-10 關維持原數值，只做兩處刻意改動：
**第 5 關升格為第 1 章的章節 Boss 關**（`dur` 120→140 讓 Boss 有作戰時間、
一般 Boss 由 2 隻減為 1 隻，總 Boss 數維持 2 隻不讓這關突然變成 3 隻）。
`py_ab_base.py` 逐項證明其餘欄位與 v0.9.20 基準 **byte-identical**。

其他調整：
- **單關時長上限 323 → 240 秒**（手機是零碎時間在玩的，5 分半會逼玩家中途放棄）
- **`runCoins` 的 `lvBonus` 係數 0.025 → 0.05**：關卡數砍半後，同樣的遊戲進度
  對應到一半的 `lvIdx`，不加倍的話全破能拿到的糖果幣會少快一半，
  局外成長會追不上被壓縮的難度曲線。經濟防呆仍通過（單場最高 520 = 商店總價的 5.8%）
- **最後一關獨佔 tier 13「終焉/Finality」**，讓 14 個難度層級名稱全部用得上
- 背景色帶本來就是「每 5 關一條」，現在**剛好一章一條**，
  換背景／出現章節 Boss／章節推進三件事同時發生

### 存檔遷移

`{progress, coins, meta, lang}` 加上 `v:2` 版本標記。
舊存檔（未帶 `v`）遷移一次：**新進度 = ceil(已破關數 / 2) + 1**
（新第 N 關 ≈ 舊第 2N 關；無條件進位，寧可多給不要倒扣）。
糖果幣、永久強化、語言偏好完全不動。

`py_50levels.py` 驗證 7 組換算（1→1、2→2、11→6、21→11、51→26、100→51、101→51）、
重載兩次不會越切越小、已是 v2 的存檔不會被再次遷移。

### ⭐ 難度驗收：`tests/py_ab_curve.py`（新工具）

方法論第 3 條：平行分頁數會明顯影響笨bot成績，跨批次不能互比。
所以這支把「舊第 2N 關」與「新第 N 關」**放進同一次 `asyncio.gather`**，
兩邊搶同樣的 CPU，比出來的差異才是關卡造成的。

**滿等滿裝（每組 5 次，只比最終關）**

| 新關卡 | 走完% | 舊對應關卡 | 走完% | 差異 |
|---|---|---|---|---|
| 50 | 26.3% | 100 | 21.5% | **+4.8pt（新版較好過）** |

**笨bot（每組 3 次，全部配對）**

| 新 | 走完% | 舊 | 走完% | 差異 |
|---|---|---|---|---|
| 5 | 53.8% | 10 | 5.3% | +48.5pt |
| 10 | 6.8% | 20 | 3.9% | +2.9pt |
| 25 | 5.0% | 50 | 2.5% | +2.5pt |
| 50 | 3.4% | 100 | 2.6% | +0.8pt |

**每一個配對點，新版都不比舊版難。**

⚠ 過程中踩到一次雜訊陷阱：第一輪滿等滿裝 A/B（4 組配對 × 2 次 = 16 個分頁）
量到新第 50 關 12.7% vs 舊第 100 關 17.9%（−5.2pt，看起來變難了）。
把配對縮到 1 組 × 5 次（10 個分頁）重測，變成 +4.8pt。
**同樣是「同批次」，分頁數不同時雜訊還是很大——樣本數要夠。**

### 關卡差異度：前後對照

| 指標 | v0.9.22（100 關） | v0.9.23（50 關） |
|---|---|---|
| 全破純遊玩時間 | 476 分（近 8 小時） | **170 分（約 3 小時）** |
| 平均每關 | 286 秒 | **204 秒** |
| 相鄰關差異（前半） | 4.20% | **7.92%** |
| 相鄰關差異（後半） | 1.03% | **2.37%** |
| **相鄰差異 < 1% 的關卡數** | **40 關** | **0 關** |

### 測試（三輪）

- **15 支測試同時平行跑全綠**（新增 `py_50levels.py`、`py_ab_curve.py`）
- 第 1 關笨 bot 2 次：1 次零失誤、1 次掉 1 心，都通關。
  `py_ab_base` 證明第 1-4、6-10 關與 v0.9.20 基準 byte-identical → 差異是雜訊
- zip 解壓實跑：v0.9.23 / en / 50 關 / 207 key / 無 JS 錯誤
- 效能：第1關 16.7ms、第5關 16.5ms、第50關（4 隻 Boss + 80 小怪）**17.6ms，卡頓幀 0%**

### 這一版改動的測試（全部是測試端跟上結構改變，不是遊戲缺陷）

`py_100level`（改名意義：Boss 從每 10 關改每 5 關）、`py_audit_fixes`（抽樣關卡
與起始武器對應表）、`py_boss_skin`（章節→關卡換算）、`py_i18n`（tier 取樣點）、
`py_spawn`（關卡索引）、`py_ui_edge`（點節點的關卡）、`py_ui_fixes`（背景帶數／節點數）、
`py_ab_base`（改成只守「不該變的東西」，關卡曲線交給 `py_ab_curve`）、
`py_level_audit`（章節大小改成動態）。

### 教訓

22. ⭐ **同批次 A/B 也會被分頁數干擾**——「同批次」解決的是「不同批次不可比」，
    但沒有解決「樣本太少」。看到不符預期的結果時，先加樣本再下結論。
23. ⭐ **用 `sed` 插入含註解的程式碼很危險**：這次把
    `// 註解 G.hasMoved=true; DIAG.touch=3;` 塞成同一行，
    等於把後面兩個語句一起註解掉。改多行內容要用 Python 精準比對替換。

### 環境現況更新

- **版本號**：v0.9.23
- 關卡：100 → **50**（10 章 × 5 關），章節 Boss 在第 5/10/…/50 關
- 存檔格式加上 `v:2`，附一次性遷移
- 新增 `tests/py_50levels.py`、`tests/py_ab_curve.py`
- 新增 `i18n/build_v0923.py`
- 測試工具總數：22 → **24 支**

---

## 二十七、v0.9.24 —— 版權聲明與 `<html lang>`

使用者問「repo 是 public，之後要賣錢該怎麼調整」，順手先做掉兩件現在就該做的：

### 1. 版權聲明

repo 裡**沒有 LICENSE 檔**。在著作權法下，沒有授權條款＝保留一切權利，
理論上別人本來就不能拿去用；但實務上「原始碼公開、又沒有任何聲明」會讓人
以為可以隨便拿，而且真的要發 DMCA 下架通知時，有明確聲明會順很多。

新增：
- `LICENSE.txt`（中英雙語，明確禁止複製／修改／再散布／上架到任何商店，
  也禁止商業使用「GOO BLASTER」名稱與美術）
- `index.html` 開頭的 HTML 註解版權聲明——**這一份會跟著遊戲檔案走**，
  不管別人是從 GitHub 抓、從 itch.io 抓，還是從 APK 解出來，都會看到

⚠ 我不是律師，這是常見做法不是法律意見。

### 2. `<html lang>` 寫死成 zh-Hant

從 v0.9.19 預設語言改成英文之後，`<html lang="zh-Hant">` 就一直是錯的。
影響：螢幕閱讀器會用中文語音念英文介面、搜尋引擎會把頁面判成中文網頁
（itch.io / Google Play 的 SEO 都吃這個）。

修法：標籤改成 `en`，並在 `applyLanguage()` 裡同步
`document.documentElement.setAttribute('lang', LANG)`。
新增 `tests/py_lang_attr.py` 驗證 11 種語言切換後都正確，
以及原始碼開頭確實有中英文版權聲明。

### 測試

**16 支測試同時平行跑全綠。**

### 環境現況更新

- **版本號**：v0.9.24
- 新增 `LICENSE.txt`、`index.html` 開頭的版權註解
- 新增 `tests/py_lang_attr.py`（測試工具總數 24 → **25 支**）
- ⭐ **交付方式改變**：使用者裝了 GitHub Desktop，且
  `C:\Users\Surface\Documents\projects\goo-blaster` 已連上 session。
  改用 `device_commit_files` 直接把檔案寫進他的 repo 資料夾，
  他在 GitHub Desktop 按 Commit + Push 即可。
  （仍然沒有 `device_bash`，我不能在他的電腦上跑 git 指令；
  雲端這邊也依舊沒有 push 權限。）

---

## 二十八、v0.9.25 —— 真人回報三連發：第5關過不了 / 連續選卡 / 同伴寶箱

**這是上線後第一次真人回饋，三項全部查證完畢。**

---

### 1️⃣ 「第5關一直過不了」——根因不是 Boss 太肉，是卡池把武器稀釋掉了

第 5 關是 v0.9.23 我新升格的章節 Boss 關，**從來沒有被真人玩過就上線了**。

**第一層診斷（Boss 血量結構）**

| 關 | 一般 Boss 血 | 章節 Boss 血 | 倍數 |
|---|---|---|---|
| 5 | 2,352 | **23,296** | 9.9× |
| 10 | 7,140 | 80,240 | 11.2× |
| 50 | 18,064 | 523,002 | 29× |

**第二層診斷（玩家實際 DPS，實測非估算）**

| build | DPS | 打死第5關章節 Boss 要多久 |
|---|---|---|
| Lv.5 · 主武器 Lv.1 | 157 | **149 秒** |
| Lv.8 · 主武器 Lv.3 | 403 | 58 秒 |
| Lv.10 · 主武器 Lv.5 | 1994 | 12 秒 |

主武器 Lv.1 → Lv.5 之間 DPS 差 **12.7 倍**。所以真正的問題是「玩家能不能把武器練起來」。

**第三層診斷（卡池統計）—— 這裡才是真兇**

| | 修正前 |
|---|---|
| 卡池組成 | 16 張裡**只有 3 張武器卡** |
| 抽 3 張時出現「升級主武器」的機率 | **18.6%** |
| 把主武器 Lv.1→Lv.5 平均需要升幾級 | **17.6 級**（還是每次都優先選它的前提） |

實測：笨bot（自動優先選武器卡、不死模式）在第 5 關打到 148 秒、升到 **Lv.13，主武器仍停在 Lv.1**。

**整個傷害模型假設玩家會把武器練起來，但抽卡系統讓那件事變成一場會輸的樂透。**

**修法**：`rollCards()` 第一格保底抽武器卡，並在武器卡之間加權——
進化卡（權重 6）> 升級已持有的武器（3）> 解鎖沒有的新武器（1）。
解鎖新武器刻意排最後：新武器是 Lv.1，對當下戰力的幫助遠小於把手上的練上去。

| | 修正前 | 修正後 |
|---|---|---|
| 主武器出現率 | 18.6% | **66.4%** |
| 任一武器卡出現率 | 49.1% | **100%** |
| 練滿主武器需要升幾級 | 17.6 | **7.3** |
| 主武器 Lv.4 時抽到進化卡 | — | 60% |

**驗收（完全沒有動 Boss 血量）**：同一隻笨bot在第 5 關拿到 **5/4/0 武器 + 水槍進化**，
章節 Boss 從 100% 打到死約 18 秒 → **通關**。

⚠ **刻意只改這一項。** 卡池與 Boss 血量同時改的話，之後就分不出是哪一項起作用。
如果真人再測還是覺得難，下一步才動 Boss 血量。

---

### 2️⃣ 「剛選完卡就馬上又能選」——不是 bug，是畫面沒說

實測 `gainXP(120)` → 一次升 4 級 → `pendingCards=4` → 連續 4 張卡，
最後正常關閉視窗、`G.paused` 正常回到 false。**鏈路完全正確。**

問題純粹是溝通：連續看到四次一模一樣的「LV.5 UP! Pick one」，任何人都會覺得系統壞了。

**修法**：欠超過一張時，標題補上「(第幾張/共幾張)」，例如 `LV.5 UP! Pick one (2/4)`。
只欠一張時維持原樣，不顯示 (1/1)。新增 key `cardTitleN` ×11 語言，
括號樣式依語言調整（CJK 全形、法文括號前留空格）。

---

### 3️⃣ 同伴寶箱 —— 評估結果見 `docs-11-同伴寶箱評估.md`

結論：**建議做，但排在難度修正之後**，且要限制為「1 個寶箱類型、3 個同伴、15 秒」。

---

### 測試

- 新增 `tests/py_lv5_diag.py`（Boss 血量結構 + 實測 DPS + 擊殺秒數）
- 新增 `tests/py_lv5_growth.py`（真的笨bot跑第5關，記錄成長曲線與 Boss 血量變化）
- 新增 `tests/py_card_pool.py`（卡池統計 + pendingCards 行為驗證）
- 新增 `tests/py_cards_fix.py`（保底武器卡 7 項 + 連續選卡提示 11 語言）
- **17 支測試平行全綠**
- 第 1 關基準線：3 次 → 2 次零失誤通關（第 3 關還首次通關）
- 難度重測（笨bot）：第1關 87.4%（**2/2 通關**，以前打不贏）、第5關 58.1%、
  第10/25/50關 5.6/3.7/3.2%（與修正前相同，沒有被拉歪）

### 兩支測試的時序修正（測試端問題，非遊戲缺陷）

17 支平行時 `py_audit_fixes` 的「連點版本號 5 下」偶發失敗：
Python 端每點一次 sleep 30ms，CPU 被搶時會超過遊戲端 1.2 秒的重置門檻。
照方法論第 9 條**從結構上修掉**——五次點擊改成在同一個 `page.evaluate` 裡送出，
完全不依賴 wall-clock。`py_ui_edge` 的 `wait_for_selector` 逾時同理放寬。

### 教訓

25. ⭐ **玩家「打不過」的時候，先量他的輸出能不能長起來，再看敵人的血量。**
    我第一直覺是「Boss 太肉」，差點就去改血量——真正的瓶頸在三層之外的卡池分佈。
26. ⭐ **`0/N 通關` 是紅旗，不是雜訊。** v0.9.23 的 A/B 測試裡每一個關卡都是 0 勝，
    我用「這隻 bot 不會閃子彈」解釋掉了。那個解釋對第 10 關以後成立，
    對第 5 關不成立——而我沒有分開檢查。
27. ⭐ **一次只改一個變數。** 卡池與 Boss 血量同時改就失去了歸因能力。

### 環境現況更新

- **版本號**：v0.9.25
- `rollCards()` 保底武器卡 + 加權
- 新增 `cardTitleN`（L10N 208 key × 11 語言）、`G.cardTotal`
- 測試工具總數：25 → **29 支**

---

## 二十九、v0.9.26 —— 第5關 Boss 彈幕密度（真人回報第二輪）

**使用者回報：**
> 「第五關的boss發射的砲彈多到難以躲藏，大人都很難操作了，小朋友玩這個會信心崩潰」

v0.9.25 修掉了「打不動」（卡池稀釋主武器），但玩家接著撞上第二道牆：**躲不掉**。
這兩件事是分開的，前者是輸出，後者是輸入。

---

### 根因：v0.9.23 把「第10關的 Boss」搬到了第5關，連彈幕型態一起搬過去

v0.9.23 把章節 Boss 從「每 10 關一隻」改成「每 5 關一隻」，
`BOSS_CHAPTER[0]` 原本掛在第 10 關，改完之後掛到第 5 關。
但 `BOSS_CHAPTER[0]` 用的攻擊型態是 `ring`——全遊戲最密的一種：

| 攻擊型態 | 正常一波 | 暴走（血量<50%）一波 |
|---|---|---|
| `cross` | 8 顆 | 12 顆 |
| `spiral` | 6 顆 | 7 顆 |
| **`ring`** | **19 顆** | **25 顆** |

實測修正前各章節 Boss 的彈幕密度（`tests/py_bullet_density.py`）：

| 關 | 型態 | 一波顆數 | 暴走 |
|---|---|---|---|
| **5** | **ring** | **19** | **25** |
| 10 | spiral | 6 | 7 |
| 15 | cross | 8 | 12 |
| 25 | ring | 19 | 25 |

**玩家第一次見到章節 Boss，面對的就是全遊戲最密的彈幕，比第 10、15 關都密。**

還有第二件事一起被搬過來：**雷射鎖定攻擊**。
那段程式的註解自己寫著它「只給超級大 Boss（第6、10關）用，不影響其餘 8 關
一般 Boss 戰既有的難度曲線與教學節奏」——但判斷式寫的是 `if(e.superBoss)`，
而 v0.9.23 之後第 5 關就是 superBoss。**註解描述的設計意圖被程式碼悄悄違背了。**

所以第 5 關的玩家同時要應付：環形 19 發彈幕 ＋ 追蹤散彈 ＋ 直線雷射。

---

### 修法：彈幕顆數依章節縮放，雷射延後到第 6 章

**1. 章節 Boss 的攻擊型態重排（由稀到密穿插）**

```js
// BOSS_CHAPTER 的 atk 欄位
cross, spiral, cross, spiral, ring, cross, ring, spiral, cross, ring
//  5     10     15     20     25    30    35     40     45    50
```

**2. 新增章節縮放係數 `cs`**

```js
const cs = e.superBoss ? Math.min(1, 0.45+(e.chIdx|0)*0.061) : 1;
const sc = (v,min)=>Math.max(min, Math.round(v*cs));
```

- `chIdx` 由 `buildBosses()` → `spawnBoss()` 一路帶進 Boss 實體
- 第 1 章 `cs=0.45`（第5關），第 10 章 `cs=0.999`（第50關）
- **一般 Boss 與一般敵人完全不縮放**（`cs` 恆為 1），第 5 關一般 Boss 仍是 8 顆
- 每種型態都有下限（`cross` 至少 4、`ring` 至少 6），不會縮到 0

**3. 雷射恢復成後期專屬**

```js
if(e.superBoss && (e.chIdx|0)>=5){   // 第 6 章＝第 30 關起
```

---

### 修正結果（實測，攔截 `shootE()` 數實際射出的子彈）

| 關 | 章 | 型態 | 正常一波 | 暴走 | 雷射 |
|---|---|---|---|---|---|
| **5** | ch0 | cross | **4**（原 19） | **5**（原 25） | ❌ |
| 10 | ch1 | spiral | 5 | 5 | ❌ |
| 15 | ch2 | cross | 5 | 7 | ❌ |
| 20 | ch3 | spiral | 5 | 6 | ❌ |
| 25 | ch4 | ring | 13 | 17 | ❌ |
| 30 | ch5 | cross | 6 | 9 | ✅ |
| 35 | ch6 | ring | 16 | 21 | ✅ |
| 40 | ch7 | spiral | 6 | 7 | ✅ |
| 45 | ch8 | cross | 8 | 11 | ✅ |
| **50** | ch9 | ring | **19** | **25** | ✅ |

**第 50 關一顆都沒少**——最終關的難度是設計終點，不能因為前期太難就整條曲線下修。

390px 螢幕上「同時看得到幾顆敵方子彈」（9 秒取樣）：

| | 修正前 | 修正後 |
|---|---|---|
| 第5關（正常）峰值 | 19 | **4** |
| 第5關（暴走）峰值 | 25+ | **7** |
| 第50關峰值 | 22 | 22（不變） |

---

### 已知取捨（誠實記錄）

修正後各關的「一波顆數」不是單調遞增的：25關13顆 → 30關6顆 → 35關16顆。
這是**攻擊型態本身的差異**（ring 環形彈天生比 cross 多），不是縮放造成的，
修正前也一樣。彈速、射擊間隔、Boss 血量三項仍然是單調遞增的，
整體難度曲線沒有回頭；型態穿插反而讓每一章的手感不同、不會膩。

---

### 測試

- 更新 `tests/py_bullet_density.py`：第 4 項改成斷言「雷射只出現在第 30/35/40/45/50 關」
- 更新 `tests/py_laser_boss.py`：雷射測試改用最終章 Boss（`chIdx:9`），
  並新增第 5 項「第5關章節Boss 不得發射雷射」
- 新增 `tests/py_v0926_edge.py`（測試循環 2，邊界）：
  攔截 `shootE()` 數實際彈數、縮放係數單調性、一般 Boss/敵人未受影響、
  atk 重排後 Boss 名稱與圖示仍對應正確章節、第1~4關無章節 Boss——**全數通過**
- 新增 `tests/py_lv5_stress.py`（測試循環 3，最嚴苛）：
  零永久強化 ＋ 慢反應窄視野 bot ＋ Boss 鎖在暴走狀態、只留 Boss 的第5關 A/B，
  對照組用同一份程式碼把 Boss 改回 `atk='ring', chIdx=9`，
  確保血量／速度／間隔完全相同，唯一差異就是彈幕顆數與雷射：

| 第5關 Boss 暴走狀態（5 次重複） | v0.9.25 | v0.9.26 |
|---|---|---|
| 被打中次數（3 顆心） | **3.0**（每次都死） | **0.0** |
| 撐過 30 秒 | **0 / 5** | **5 / 5** |
| 平均存活 | 8.6 秒 | **30.4 秒** |
| 平均同屏子彈 | 12.8 | **4.1** |
- 修好 5 支長期壞掉的舊 JS 測試（`renderLevels` 早已更名為 `renderStage`，
  且路徑仍指向不存在的 `/home/claude/goo-blaster/`）——**它們已經沉默失敗很久了**
- **31 支測試平行全綠**

### 教訓

28. ⭐ **搬動一個資料表的位置，等於搬動它所有的隱含假設。**
    v0.9.23 只是把章節 Boss 從每 10 關改成每 5 關，卻同時把「最密的彈幕」
    和「後期專屬的雷射」一起搬到新手面前。改結構時要問：
    **「這一格資料原本待的位置，替它擋掉了什麼？」**
29. ⭐ **程式碼註解寫著的設計意圖，要有測試去釘住它。**
    `if(e.superBoss)` 上面那段註解明明白白寫著「只給第6、10關用」，
    但沒有任何測試在檢查這件事，所以它被違背了三個版本都沒人發現。
30. ⭐ **測量工具本身也要被驗證。**
    這次的壓力測試連續錯了三版：
    (a) 用 `G.EB.length` 數彈數 → 打到玩家的子彈會消失，少算；
    (b) 開 10 個分頁 + CPU 4x 節流 → 30 秒牆鐘只跑出 3.9 秒遊戲時間，量到的是容器負載；
    (c) bot 貼著 Boss 打 → 死因主要是接觸傷害，量到的不是彈幕。
    **每一版都會給出一個看起來很像結論的數字。**
31. ⭐ **沉默失敗的測試等於沒有測試。** 5 支 JS 測試因為函式改名與路徑失效，
    已經壞了不知道多久，而每次跑「全套測試」時它們的紅字被當成背景雜訊。

### 環境現況更新

- **版本號**：v0.9.26
- `BOSS_CHAPTER` 的 `atk` 重排；新增 `chIdx` 欄位貫穿 `buildBosses`/`spawnBoss`
- 彈幕縮放 `cs`／雷射門檻 `chIdx>=5`
- 測試工具總數：29 → **31 支**
- ⚠ **本版只動彈幕密度，沒有動 Boss 血量、沒有動卡池。** 保留歸因能力。

---

## 三十、v0.9.27 —— 糖果援軍（同伴寶箱）＋ 免費/付費切點

**這一版同時做了兩件事，但它們互不干擾**：援軍只碰寶箱池與遊戲迴圈，
付費切點只碰選單與流程判斷，`EDITION` 預設 `'full'` 時完全不啟用。
歸因能力沒有被破壞（見第 1.4 條）。

---

### 1️⃣ 糖果援軍 Candy Comrades

規格完全照 `docs-11-同伴寶箱評估.md`，一項都沒有加碼：

| 項目 | 值 | 為什麼 |
|---|---|---|
| 同伴數量 | 3 | 390px 螢幕上讀得出來又不會擠爆 |
| 持續時間 | 15 秒 | 夠翻盤、不足以抹平整關 |
| 傷害 | 玩家的 **55%** | 幫忙，不代打 |
| 射速 | 玩家的 **60%** | 控制子彈總量 |
| 武器 | 只複製**主武器**（等級最高那把），**不繼承進化** | 效能與平衡都可控 |
| 移動 | 環繞玩家、阻尼跟隨（半徑 104px） | 玩家永遠知道它們在哪 |
| 稀有度 | 寶箱池 **4.5%／個寶箱** | 一關約 8~10 個寶箱 → 期望 0~1 次 |
| 結束 | 最後 3 秒閃爍 + 消失噴粒子 | 不是突然消失 |

**實作上刻意不複製一份射擊邏輯**：同伴用一個代理物件
`ALLY_PROXY={x,y,atkRange,dmgEff}` 直接呼叫 `WEAPONS[k].fire()`。
複製一份的話，以後改武器就要記得改兩個地方——那是遲早會忘的。

**寶箱池改成整數權重表**。原本是「同一個物件塞 N 份」的陣列，沒辦法表達 0.4 份。
整張表 ×5 之後 heart:5 / nuke,rain,gold,ice,fire:10 / speed,power:15 / ally:4。
**其餘 8 種的相對比例與 v0.9.26 完全相同**（`py_v0927.py` 逐項驗證），沒有偷調平衡。

**看了截圖才發現的問題**：粉紅色的雜兵也存在，同伴光靠「粉紅＋比較小」在手機上
分不出來。加了一圈**青色隊友環**——那是玩家的顏色，敵人永遠不會有。顏色講的是陣營。

---

### 2️⃣ 免費/付費切點 —— 做成編譯期開關，這一版**不改變線上版的任何行為**

```js
const EDITION='full';    // 'full' | 'demo'  ← 上架時只改這一行
const DEMO_LEVELS=15;    // 第 15 關是第 3 章章節 Boss，收在高峰
const STORE_URL='';      // 上架後填入；留空時不顯示購買按鈕
```

**為什麼不直接把線上版切成試玩版**：

1. 現在 GitHub Pages / itch.io 上的完整版突然變成 15 關，已經在玩的人直接失去內容——
   而此刻根本還沒有東西可以買。**那是純粹的損失，沒有對應的收益。**
2. 上架當天要做的事，就只是把 `EDITION` 改成 `'demo'`。機制已經全部做完並驗過。

兩條上架路線都支援：
- **買斷制 App**：`EDITION='full'`，玩家付錢下載，全部關卡直接開放
- **免費+內購**：`EDITION='demo'`，原生外殼付款成功後呼叫 `window.GOO_UNLOCK()`

**存檔**：新增 `prem` 旗標。`full` 版**不寫** `prem=1`——判斷依據是「這個玩家真的
買過嗎」，不是「他曾經開過 full 版」。否則未來把網頁版切成 demo 時，
所有開過完整版的人都會被誤判成付費玩家。

**三個觸發點，按心理學排序**（原則 A）：

1. 點銀河圖上的付費鎖節點
2. 在鎖住的關卡按「開始」
3. ⭐ **通關第 15 關的結算畫面**——「下一關」直接變成「取得完整版」。
   這是整條漏斗轉換率最高的一格：玩家剛通關、腎上腺素還在、正想按下一關。

**付費鎖跟進度鎖的外觀刻意分開**：進度鎖是暗紫色、不能點（「還沒輪到你」）；
付費鎖是金色描邊、可以點（「拿得到，只是要買」）。

文案強調「一次買斷、沒有廣告、沒有轉蛋」——這是給小朋友玩的產品，
那三件事本身就是賣點，不只是合規要求。

**沒有商店連結時不顯示購買按鈕**。一顆按了沒反應的按鈕，比沒有按鈕更傷信任。

---

### 測試（36 支平行連跑三輪全綠）

- 新增 `tests/py_v0927.py`：援軍規格、實測傷害比、寶箱權重比例、
  full/demo 兩種版本的鎖定行為、存檔 prem 讀寫、11 語言解鎖面板版面
- 新增 `tests/py_v0927_perf.py`：第 50 關最壞情況同批次 A/B
  → **46.2 → 44.6 fps（-3.5%）**，同伴確實有在打（敵人數從 147 降到 91）
- 新增 `tests/py_v0927_shots.py`：UI 截圖（規則：UI 改動一定要看圖）
- 新增 `tests/py_v0927_ab_lv1.py`：第 1 關迴歸調查（見下）

### ⭐ 這一版最重要的發現：`py_test9` 的驗收標準本身是錯的

改完之後第 1 關基準線連續兩次「零失誤通關」沒過。照方法論第 8 條先找機制：
v0.9.27 對第 1 關的唯一接觸點是寶箱池多了援軍，而援軍**是幫玩家的**。

於是做同批次 A/B（現版 vs 把援軍權重設成 0 的對照版），各 6 場：

| | 通關 | 零失誤 |
|---|---|---|
| 有援軍（v0.9.27） | **6/6** | 2/6 |
| 無援軍（對照組） | **6/6** | 2/6 |

**完全相同。** 也就是說「零失誤」本來就只有大約 1/3 的機率，
跟程式碼有沒有改一點關係都沒有。

**把一個機率事件當成硬性驗收標準，等於每三次就誤報一次「基準線被打破」**——
而真正的迴歸會被淹沒在這些狼來了裡面。已經改成：
**硬門檻＝一定要通關；零失誤降級成參考指標**。
（專案現況文件其實早就寫著「真正該看的是有沒有通關」，但測試程式沒有照做。）

### 這一版修掉的 6 支測試（全部是測試自己的問題，不是遊戲的問題）

高負載（36 支平行）下遊戲時間嚴重落後牆鐘，所有「睡固定毫秒再檢查」的寫法都會偶發失敗：

| 測試 | 真正的原因 |
|---|---|
| `py_laser_boss` | 用 Python 每 100ms 取樣數雷射週期 → 改成頁面內 16ms 計數 |
| `py_ui_edge` / `py_ui_fixes` | `wait_for_selector` 逾時太短 |
| `py_v0927` | route handler 裡又打了一次同一台 HTTP server，執行緒被搶滿就卡死 |
| `py_v0927_perf` | 平行時整台機器只剩 1 fps，連相對值都沒解析度 → 改成排在平行批次**之後**單獨跑 |
| `py_layout_fit` | 用原文當去重 key，果凍% 每幀數值都在變 → 同一行被當成兩行、誤判疊字 |
| `py_ui_fixes`（核彈） | 見下 |

### ⭐⭐ 核彈那一項：我連續猜錯四次

「拖曳中按核彈鍵有引爆」在高負載下一直失敗。我依序猜了：
(1) 兩根手指的同步問題 → 加同步點，沒用；
(2) 玩家中途死掉 → 加不死 keep-alive，沒用；
(3) 關卡時間到而通關 → 凍結時鐘，沒用；
(4) 升級面板把 `G.paused` 設成 true 吃掉點擊 → 停掉升級，還是沒用。

**第五次我不猜了，改成量。** 裝監聽器測「事件到底有沒有進到按鈕」——
第一版還把監聽器裝在送觸控**之後**，`touches` 恆為 0，那個診斷等於什麼都沒量到。
修正位置後量到 `touches:1`（事件有到），再加一個冒泡階段的監聽器讀 `doNuke()`
執行後的狀態：**`held:false, flash:1`——核彈其實每一次都有炸。**

失敗的是量法：Python 每 150ms 問一次 `nukeFlash`，爆炸閃光整段落在兩次詢問之間；
而 keep-alive 又立刻把 `nukeHeld` 設回 true，把另一個訊號也蓋掉。
改成頁面內 16ms 記錄 `nukeFlash` 的最大值，一次就過。

**遊戲從頭到尾沒有問題。壞的是測試，而且我花了四輪才肯去量。**

### 教訓

32. ⭐ **固定 `wait_for_timeout(N)` 在高負載會偶發失敗 → 一律改輪詢條件。**
33. ⭐ **機率性的指標不可以當硬性驗收標準。** 每三次誤報一次的門檻，
    會訓練所有人（包括我）忽略紅字——那比沒有測試更危險。
34. ⭐ **猜錯兩次就該停下來裝儀器。** 我猜了四次才去量，每一次「修正」都讓
    測試更複雜、離真相更遠。而真相是：被測的功能根本沒壞。
35. ⭐ **診斷程式碼本身也會有 bug。** 監聽器裝錯位置，`touches:0` 這個
    「證據」是假的，還差點讓我做出「事件沒送到」的錯誤結論。
36. ⭐ **效能測試不能跟別的測試搶 CPU。** 平行時容器只剩個位數 FPS，
    絕對值無效、相對值也失去解析度。已在 `run_tests.sh` 排成平行批次之後單獨跑。
37. ⭐ **絕對門檻要對著「量測環境的天花板」校準。** 這個容器上限就是 46 fps，
    把門檻訂在 45 等於在跟容器比，不是在測遊戲。
    ⚠ 而且**容器不是真實手機**——真機效能只能靠實機驗證，這裡量不到。

### 環境現況更新

- **版本號**：v0.9.27
- L10N：208 → **218 個 key** × 11 語言（`i18n/build_v0927.py`）
- 新增 `G.ALLY` / `CHEST_W` 權重表 / `EDITION`・`DEMO_LEVELS`・`PREM_OWNED`
- 存檔新增 `prem` 欄位（`SAVE_VER` 不變，舊存檔讀進來 `prem` 是 undefined → false，
  在 full 版沒有任何影響）
- 測試工具：33 → **37 支**（`run_tests.sh` 一鍵跑 36 支，效能測試排在最後單獨跑）

---

## 三十一、v0.9.28 —— 原生儲存層（上架前的第一風險）

`docs-12-上架路線圖.md` 把這件事列為關鍵路徑的最前端，理由：

> 包成 WebView App 之後，存檔還是走 `localStorage`。iOS 與 Android 都可能在
> 儲存空間吃緊時清掉 WebView 的 localStorage。
> **家長付了錢，小孩玩到第 30 關，某天打開 App——進度、糖果幣、買斷解鎖全沒了。**
> 退款 + 一星評價，而且是我們自己造成的。

而且它擋住整條路徑：沒有原生儲存 → 不敢出 APK → Google Play
那個「12 位測試員 × 連續 14 天」的鐘就一天都還沒開始走。

---

### 架構：hydrate 模式

難處在於 **Capacitor Preferences 是非同步的**，但 `loadSave()` 在開機時是同步呼叫，
後面一整串程式都假設 `PROGRESS`/`COINS` 已經有值。

```
開機
 ├─ loadSave()          同步讀 localStorage → 主選單立刻畫出來（沒有等待畫面）
 ├─ applyLanguage(); renderStage();
 └─ hydrateFromNative() 非同步讀原生儲存 → 合併 → 需要的話重畫選單
```

**純網頁版（沒有 Capacitor）在 `hydrateFromNative()` 第一行就 return**，
行為與 v0.9.27 完全相同——這是最重要的一條，`py_ab_base` 也持續全綠。

### 合併規則：永遠不讓玩家倒退

`pickBetterSave(a,b)`：

| 欄位 | 規則 |
|---|---|
| `progress` | 取高的 |
| `coins` | 取兩邊最大值 |
| `prem`（買斷解鎖） | **OR**——只要有一邊是 true 就保留 |
| 平手時 | 比糖果幣 |

**買斷狀態用 OR 是刻意的**：付過的錢在任何情況下都不可以消失。
寧可有極小的機率多給，也不能少給。

### 存檔寫入：不 await

```js
try{ localStorage.setItem(PROG_KEY, payload); }catch(e){}
try{ NATIVE_STORE.set(PROG_KEY, payload); }catch(e){}   // 刻意不 await
```

`saveGame()` 在遊戲迴圈裡會被頻繁呼叫，不能讓它卡住任何一幀。
實測連續存檔 300 次只花 **5.2ms**。

---

### 測試（`tests/py_native_store.py`，用假的 Preferences 外掛模擬原生環境）

| 情境 | 結果 |
|---|---|
| 純網頁版行為不變 | ✅ |
| 原生外殼：存檔同時寫兩邊、內容一致 | ✅ |
| ⭐ **localStorage 被清掉 → 原生儲存救回進度/幣/買斷** | ✅ |
| 兩份打架 → 進度取高、買斷 OR、幣取大 | ✅ |
| 第一次在原生外殼跑 → 網頁存檔搬進原生（一次性遷移） | ✅ |
| 原生外掛丟例外 → 降級成純 localStorage，不當掉 | ✅ |
| 原生存檔是壞掉的 JSON → 退回用 localStorage | ✅ |
| hydrate 在**遊戲進行中**完成 → 不會把玩家踢回選單 | ✅ |
| 玩家先存了更新的進度 → 不會被舊的原生存檔蓋掉 | ✅ |
| 更早的舊格式（純數字）在原生外殼裡也能遷移 | ✅ |

⚠ **沒有實測過的部分**：真正的 Capacitor 打包與實機。
這個雲端容器沒有 Android SDK / Xcode，`native/` 那份骨架是照官方做法寫的，
**必須在真機上驗過「玩一關 → 關掉 App → 重開 → 進度還在」才算數。**

---

### 同時交付

- `native/`：Capacitor 專案骨架（`package.json` / `capacitor.config.json` / README
  含逐步指令與送審檢查清單）。**要另開一個 repo**，見 AGENTS.md 第 4 節 E 區塊。
- `PRIVACY.md`：隱私政策（中英雙語）。核心內容就一句「不收集任何資料」——
  這是當初為了小朋友做的設計決定帶來的直接好處。

---

### ⭐ 這一版抓到一個藏了六個版本的測試缺陷

`py_boss_skin` 的「血量 55% 時盔甲還在」在全套平行跑時失敗了。
我一開始又想歸因到平行負載——但照 AGENTS.md 第 3 節第 7 條，**猜錯兩次就裝儀器**，
這次直接先量。把打之前/之後的實際血量印出來：

```
打之前 100% → 打之後 10%
```

打「45% 的血」竟然掉了 90%。原因是 `hurtEnemy()` 內部會擲 **5% 的爆擊，傷害 ×2**。

**也就是這一項從 v0.9.22 起就有 5% 的機率會失敗**，跟平行負載完全無關，
只是機率低到之前每一次都剛好躲過。修法是測試裡先 `G.P.crit=0`，
讓「驗 50% 門檻」拿到確定的傷害。

這正好是第 33 條教訓的另一個實例：**機率性的東西不可以拿來當確定性的驗收**。
只是這次反過來——不是驗收標準是機率的，而是**被測的輸入是機率的**。

### 教訓

38. ⭐ **不只驗收標準不能是機率的，「測試的輸入」也不能是機率的。**
    `hurtEnemy()` 內建 5% 爆擊，讓一個看起來完全確定的斷言變成 95% 通過率。
    寫測試時要問：**我餵進去的東西，每次都一樣嗎？**
39. ⭐ **這次我第一時間就裝儀器，沒有再猜四次。** v0.9.27 學到的東西有生效。
40. ⭐ **常數要去程式裡查實際值，不要憑印象打。**
    測試裡我把 `OLD_PROG_KEY` 寫成 `'gooblaster_lv'`（實際是 `'gooblaster_progress_v2'`），
    結果什麼都沒遷移、得到一個假失敗。

### 環境現況更新

- **版本號**：v0.9.28
- 新增 `NATIVE_STORE` / `pickBetterSave` / `applySaveObject` / `hydrateFromNative`
- `loadSave()` 拆成 `loadSave()` + `applySaveObject()`（讓 hydrate 可以重用）
- 新增 `native/`（Capacitor 骨架）與 `PRIVACY.md`
- 測試工具：37 → **38 支**（`run_tests.sh` 一鍵跑 37 支 + 效能測試單獨跑）

---

## 三十二、v0.9.29 —— 存檔備份碼（真人審過商店頁與程式碼給的反饋）

### 起因

itch.io 上一位叫 HKM Industry 的玩家沒有實際玩遊戲（他自己講明了），
但認真看了商店頁的 metadata 跟能看到的公開資訊，給了一段很扎實的技術回饋。
拆成四點逐一核對：

1. **itch.io 篩選欄位沒填**（Input methods / Accessibility features / Average
   session length / Languages）——這是商店頁編輯問題，不是程式問題，留給
   使用者自己去 itch.io 後台填，不動 `index.html`。
2. **卡池稀釋會隨版本新增內容悄悄復發**——**查證後這點是過時的推論**：
   v0.9.25 已經把「主武器至少出現一張」從機率保底改成**結構性保證**
   （`rollCards()` 裡 `wepPool` 跟 `statPool` 分開抽，第一張永遠從 `wepPool`
   抽），這個保證不會隨統計卡數量增加而被稀釋，因為它不是「卡池裡武器卡的
   佔比」而是「輸出的第一格固定來自武器池」。這位玩家是從敘述反推程式邏輯，
   猜錯了；不用改。
3. **touch-action**——查證後**程式碼本來就對**：`canvas{touch-action:none}` 早就
   存在，itch 頁面底下的捲動/雙指縮放不會在拖曳時被誤觸發。不用改。
4. ⭐ **網頁版的儲存持久性沒有解——這點是真的、而且是新發現**。
   v0.9.28 做的 `NATIVE_STORE` 只解決「包成 App 之後」的持久性；itch.io
   把遊戲嵌在第三方 iframe 裡，Safari 的 ITP 之類機制本來就會比較積極地回收
   久沒被直接造訪的網站的 localStorage。這個風險網頁版一直都在，只是
   docs-12 當時只評估了「包成 App 之後」的情境，沒評估「itch.io 網頁版本身」。

### 根因

網頁版存檔完全依賴 `localStorage`，而 `localStorage` 在「玩家隔了一段時間
沒回來」＋「透過第三方 iframe 嵌入」這兩個條件疊加時**不保證還在**。
這件事在我們自己的測試裡永遠不會被抓到，因為測試機每次都是全新分頁、
立刻執行、立刻讀回——不會有「兩週後回來」這個時間維度。
會撞到這個問題的玩家也不會回報 bug，只會默默不玩了——這對「靠回訪轉換
付費」的商業模式（principle B）是最傷、卻最安靜的一種流失。

### 改了什麼

新增「存檔備份碼」面板（主選單新按鈕 💾）：

- `saveCodeEncode()`：把目前存檔（`progress`/`coins`/`meta`/`lang`/`v`/`prem`）
  序列化成 `GOO1-<checksum>-<base64>` 格式的一段文字
- `saveCodeDecode()`：反解，checksum 對不上或格式不對一律回傳失敗（不猜測、
  不部分套用）
- `importSaveCode()`：**走 `pickBetterSave()` 合併，不是覆蓋**——這支函式
  v0.9.28 已經測過，這裡直接重用它的保證：匯入永遠不會讓玩家倒退
- UI：`#codeBox` 面板，仿照既有的 `lockBox`/`langBox` 樣式；文字全部走 `T()`，
  11 種語言一次補齊（`i18n/build_v0929.py`，新增 12 個 key）

### 踩到的一個坑：toast() 在主選單會直接丟例外

第一版寫完後才想到：`toast()` 是畫在 canvas 上的（`G.TXT.push(...)`），
但這個面板活在主選單，這時候 `G` 是 `null`。其他呼叫點（例如 `unlockFull()`）
都用 `if(G && G.TXT) toast(...)` 擋過這個情況，我一開始沒擋，寫成
`toast(G&&G.TXT?T(...):'', ...)`——**只擋了文字內容，沒擋呼叫本身**，
`toast()` 還是會被呼叫、還是會在存取 `G.TXT` 時丟例外。
改成不依賴 `G` 的獨立 DOM 文字列 `#codeStatus`，這個面板的回饋訊息才真的
在主選單時顯示得出來。**這是先讓測試（py_save_code.py 的「無 JS 錯誤」斷言）
抓到的，不是憑印象發現的。**

### 測試

新增 `tests/py_save_code.py`，六組情境：

| 情境 | 驗收標準 |
|---|---|
| 開面板、匯出碼格式 | 碼自己解得回來、內容跟目前狀態一致 |
| round-trip（清掉 localStorage 模擬「進度不見了」→重整→貼碼） | 進度/幣/永久強化正確救回，且重整一次後仍然在（不是只活在記憶體） |
| 匯入「進度較低」的舊碼 | 不會讓玩家倒退；買斷狀態 OR 邏輯保留 |
| 5 種壞碼（空字串/亂打/格式不全/checksum不合/版本前綴不合） | 都顯示錯誤、都沒有動到既有存檔、沒有 JS 例外 |
| 語言切換 | 面板標題與按鈕文字跟著換，不殘留寫死的英文 |
| Clipboard API 被拒絕（模擬 itch iframe 常見情境） | 仍然顯示提示文字，不靜默失敗、不當掉 |

三輪：① 全套 38 支平行跑（含新測試）②③ 針對性邊角案例 + 重跑兩次全套抓偶發，
全部通過，沒有出現偶發性失敗。

### 教訓

41. ⭐ **回饋來自「讀商店頁 + 讀程式碼」而不是「實際玩過」時，要先查證再照做。**
    這次四點裡有兩點（卡池稀釋、touch-action）查證後發現已經解決或本來就對，
    照單全收會浪費工來修不存在的問題。**但另外兩點（itch metadata、儲存持久性）
    是真的**——不能因為兩點猜錯就連帶懷疑其餘的，逐點查證比整體採信或整體
    駁回都準確。
42. ⭐ **`toast()` 假設 `G` 存在，這個假設在主選單畫面不成立。**
    任何新的主選單 UI 要顯示回饋訊息，不能直接呼叫 `toast()`，要嘛照既有寫法
    `if(G && G.TXT) toast(...)`，要嘛（像這次）用獨立的 DOM 元素，
    不要寫成「只擋文字內容、沒擋呼叫本身」這種半吊子防呆——這種寫法在
    程式碼審查時很容易被誤判成「已經處理過 null 情況」，但實際上完全沒擋到。
43. **「靜默流失」是留存風險裡最危險的一種**——不會出現在任何測試或客訴管道，
    只會出現在回訪率的數字裡，而且對方甚至不知道要感謝誰的功能救了他的進度。

### 環境現況更新

- **版本號**：v0.9.29
- 新增 `#codeBox` 面板 / `saveCodeEncode` / `saveCodeDecode` / `importSaveCode` /
  `showCodeBox` / `codeStatusMsg`
- 新增 `tests/py_save_code.py`；`run_tests.sh` 的 `PY` 清單加入 `py_save_code`
- 新增 `i18n/build_v0929.py`（12 個 key × 11 語言）

## 三十三、v0.9.30 —— itch.io 真人玩過的回饋（CoderGenius72）＋ 平板按鈕排版

### 背景

itch.io「Get Feedback」串上，除了上一版查證過的 HKM Industry，這次來了第二位
玩家 **CoderGenius72**——這位是**真的玩到第 8 關才停**（原因是要回去做自己的
遊戲，不是被難度擋住），給的回饋比上一輪更接近實測而不是看程式碼猜測。
同一串裡 HKM 也回了第二則留言。逐點查證後：

| 回報 | 查證結果 | 處理 |
|---|---|---|
| 核彈鍵在鍵盤上沒有快捷鍵，只能點螢幕 | **真的沒有** | 加 `E` 鍵 |
| 升級卡沒顯示傷害增幅 | **比回報的還嚴重**——不是「沒顯示增幅」，是顯示的數字本身錯了，永遠凍結在 Lv.1（見下） | 修 `rollCards()` |
| 第 7 關左右，Boss 血條看起來會突然跳到別隻怪身上 | **真的有 UI bug**（多 Boss 重疊時的血條交接邏輯），底層「同時可能不只一隻 Boss 存活」本身是設計如此、沒有動 | 修 `spawnBoss()` |
| 愛心滿了還是會掉愛心寶箱，感覺浪費 | **查證後發現已經是做好的**——`CHEST_TYPES.heart` 早就會在滿血時自動轉成晶核爆發 | 不需要改，記錄查證過程 |
| 平衡數值（甜甜圈護盾太強／溜溜球太弱／泡泡槍太強／愛心回血太弱／前 4 關傷害加成沒感覺／第 20 關幾乎打不過） | **不採動**——單一玩家單次遊玩的主觀觀感，不是可以直接拿來調數值的證據 | 記錄成待驗證清單，見下方「刻意不動」 |
| （行銷建議）商店頁第一句話該直接放遊戲名稱 | 文案建議，不是程式碼 | 記錄，交給文案/上架工作 |
| （HKM 第二則）itch 頁面說明文字沒提到支援哪些語言 | 頁面設定，不是程式碼 | 併入 v0.9.29 已經留的 itch metadata 待辦 |

同一個對話裡，使用者也直接指出一件我們自己這邊還沒發現的問題：
**加速/核彈按鈕在較大的平板螢幕上被排到畫面左下角，很不明顯、也不好按**。
這一版一併處理。

### 修正 1：武器升級卡的傷害數字凍結在 Lv.1（比回報的還嚴重）

查證 CoderGenius72「卡片沒顯示增幅」這句話時，發現真正的根因比字面上嚴重：
不是「沒顯示」，是**顯示的數字本身錯了**，而且錯法很隱蔽——不管玩家實際練到
第幾級，升級卡上寫的傷害數字永遠是 Lv.1 的數字。

根因：`UPGRADES` 陣列上每張武器卡的 `.d`（描述文字）欄位，是 `applyLanguage()`
在切換語言/開機時，用 `WEAPONS[u.w].desc(目前等級)` 算好、直接寫回這個常駐物件
上的字串。但 grep 全部 `applyLanguage(` 呼叫點後發現：**這個函式只在開機、
或主選單的語言選單裡被呼叫，從來沒有在對局進行中被呼叫過**——而這兩個時間點
`G` 都是 `null`，所以「目前等級」這個參數用的是 `Math.max(1, G&&G.P?G.P.wep[u.w]:1)`，
`G` 是 `null` 時永遠算出 `1`。也就是說，卡片上那個「傷害數字」從第一次開機
就被寫死成 Lv.1 的數值，之後**再也沒有任何程式碼路徑去更新它**——玩家練到
Lv.4 時，遊戲中看到的升級卡片，寫的還是 Lv.1 的傷害。

修法：不要相信 `UPGRADES` 上快取的 `.d`，改成 `rollCards()` 每次真的要出卡片時
現算：`WEAPONS[u.w].desc(lv+1)`（`lv+1` 是「選了這張卡之後」玩家會變成的等級——
這才是卡片語意上該講的數字）。這個改動不影響任何傷害或機率數值，純粹是
「卡片上寫的字要跟現實一致」。

同時發現溜溜球（`yoyo`）武器的描述文字 `w_yoyo_d` 從一開始就沒有傷害數字
（另外兩把武器泡泡槍、塗鴉槍都有），順手一起補上，格式跟 `w_bubble_d`
統一（「· 傷害 {0}」），11 種語言一次補齊（`i18n/build_v0930.py`）。

### 修正 2：多 Boss 重疊時血條被搶走

`G.boss` 是單一個可變參照，HUD 的血條、血量文字都直接讀它。`spawnBoss()`
舊寫法是「只要生出新 Boss 就無條件覆蓋 `G.boss=b`」——這在只有一隻 Boss 的
關卡沒問題，但第 7 關左右的關卡設計本來就允許同時有不只一隻 Boss 存活
（by-design，這次沒有動這個設計），這時候第二隻 Boss 一出場，血條就會
**不管第一隻是死是活，直接被搶走**，玩家會看到自己明明還在打的 Boss，
血條突然變成另一隻的。

修法：`spawnBoss()` 改成「只有在目前沒有血條可顯示、或原本顯示的那隻已經
不在場上時，才交接」：

```js
G.E.push(b);
if(!G.boss || !G.E.includes(G.boss)) G.boss=b;   // 原本是：G.boss=b;（無條件覆蓋）
```

### 修正 3：核彈鍵鍵盤快捷鍵

加 `E`/`e` 鍵觸發 `doNuke()`，跟既有的空白鍵（加速）並列。防呆比照既有的
`KEY` 監聽寫法——擋掉 `TEXTAREA`/`INPUT` 取得焦點時的情況（例如玩家在備份碼
匯入框打字打到 `e`，不能被吃掉當成遊戲操作）。

### 修正 4（使用者中途直接指出）：平板大螢幕上，加速/核彈按鈕被擠在角落

**這是這一版裡使用者中途直接提出的需求，不是來自 itch.io 回饋串**：
「加速與核彈按鈕，在較大的平板螢幕上，被安排在螢幕左下角，很不明顯，
且對遊戲者來說不好按到」。

查證：`resize()` 原本算按鈕半徑 `BTN.r` 的公式只跟**視窗寬度**掛勾、有硬性
上限（`Math.min(64, W*0.13)` 這類寫法），手機直向（寬 390px 上下）算出來
沒問題，但平板（寬 800～1366px）視窗變寬時，按鈕半徑很快撞到上限就不再變
大，而**左下角的留白距離完全沒有跟著視窗尺寸放大**——結果就是大螢幕上
按鈕看起來相對畫面小了一大圈、又死貼在角落，跟使用者的觀察一致。

修法：改成用**視窗較短的那一邊**（`shortSide=Math.min(W,H)`）算比例，
不管直向橫向、手機或平板都用同一套邏輯，且邊界留白也跟著 `shortSide`
等比例放大，不再是固定像素：

```js
const shortSide=Math.min(W,H);
BTN.r=Math.max(40,Math.min(64,shortSide*0.13));
const btnMarginX=Math.max(20,shortSide*0.05), btnMarginY=Math.max(24,shortSide*0.055);
BTN.x=BTN.r+btnMarginX; BTN.y=H-BTN.r-btnMarginY-SAFE_BOT;
```

核彈鍵（`#btnNuke`，DOM 元素而非 canvas 繪製）原本是 CSS 裡寫死的
`left/bottom/width/height`，改成每次 `resize()` 都用 JS 重新算、跟加速鍵
半徑等比例縮放（`nukeR=BTN.r*0.9`），CSS 裡原本的數值降級成「JS 還沒跑之前
的 fallback」，並在 CSS 註解裡寫清楚。

**手機尺寸（390×844）的算出來的數字跟改版前逐位元組相同**（`BTN.r=50.7`、
`BTN.x=70.7`）——這不是巧合，是刻意驗證過的：新公式在手機尺寸下退化回
跟舊公式相同的結果，只有平板等大螢幕才會出現放大跟留白增加的差異。

實測（`/tmp/check_tablet.py`，4 種視窗尺寸）：

| 視窗 | 改版前 BTN.r | 改版後 BTN.r | 改版後離左邊界 |
|---|---|---|---|
| 手機 390×844 | 50.7 | 50.7（不變） | 20.7px |
| 平板 834×1194 | 64（已頂到硬上限，但留白沒放大） | 64 | 41.7px |
| 大平板 1366×1024 | 64 | 64 | 51.2px |

### 這次刻意「查證後不動」的一件事

CoderGenius72 提到「愛心滿了還會掉愛心寶箱」感覺浪費——查證 `CHEST_TYPES`
發現 `'heart'` 這個寶箱類型本來就有處理滿血情境（滿血時自動轉成晶核爆發），
**不是沒做，是已經做好的**。這點不需要改動，只是把查證過程記錄下來，
避免之後有人（人類或另一個模型）看到同一則回饋又重新「修」一次已經修好的
東西。

### 這次刻意不動的平衡數值清單（AGENTS.md 1.4）

CoderGenius72 的平衡類回饋（甜甜圈護盾太強、迴旋武器太弱、泡泡槍太強、
愛心回血太弱、前 4 關的傷害加成沒有存在感、第 20 關幾乎打不過）**這次沒有
照單全收去調數值**。理由：這些全部來自**單一玩家、單次遊玩**的主觀感受，
不是可以拿來調整數值的依據——AGENTS.md 1.4 講的「一次只改一個變數」的
前提，是要先有「基準」才談得上調整；單一玩家的一次體驗連基準都不算，
照這個去調某個數值，下一個真人回報「變太弱了」時，完全沒有辦法判斷
是不是這次調整造成的，因為根本沒有 A/B 對照。這份清單留著，等有 bot-based
的 A/B 驗證再處理，不是不管，是刻意排在需要更扎實證據之後。

### 測試

新增 `tests/py_v0930.py`，六大類：

1. 多 Boss 血條交接（重疊時不能被搶、A 死掉才交接給 B）＋ 單 Boss 關卡回歸
2. 武器升級卡片顯示的數字反映「選了之後」的等級（已持有武器、全新武器兩種情況）
3. 溜溜球武器描述有傷害數字，且真的隨等級變化、符合公式
4. `E` 鍵觸發核彈（含無核彈時安全 no-op、主選單安全 no-op、打字時不誤觸）
5. 備份碼提示只在第一次清第5關出現，之後不重複、清其他關不誤觸
6. 手機/平板/大平板三種視窗下的按鈕縮放，含手機尺寸逐位元組回歸驗證

三輪：①（改動當下）38 支既有測試全綠 ②寫 `py_v0930.py` 針對六大類邊角案例
③把 `py_v0930` 併入 `run_tests.sh`、全部 39 支平行跑（最嚴苛條件）。

**③ 第一次跑就抓到一支自己寫的測試本身有問題**（不是遊戲程式碼的 bug）：
第 2 類「武器升級卡數字」的測試在平行負載下偶發失敗，`desc` 是 `null`。
查下去發現原因不是遊戲壞了，是**測試自己的假設錯了**——`rollCards()` 的
保底機制只保證「至少一張武器卡」，不保證一定是 `bubble`；保底抽到哪一把
武器本身是加權隨機（`Math.random()`）。單次呼叫剛好沒抽到 `bubble` 時，
`list.find(c=>c.w==='bubble')` 合法地找不到卡，`card` 是 `undefined`，
不是遊戲邏輯壞掉。修法是讓測試重抽（`rollCards()` 是純函式、不改
`G.P` 狀態，重複呼叫安全）直到抽到目標武器卡為止，而不是放寬成
「不驗證這件事了」。修完之後單獨跑 3 次、全套平行跑 1 次都穩定通過。

### 教訓

44. ⭐ **`applyLanguage()`「只在開機/切語言時跑」這個事實，本身就是個陷阱：
    任何依賴它算出來、之後就假設「反正會再算一次」的常駐狀態，
    在對局進行中永遠不會更新。** 這次的 bug 不是邏輯寫錯，是「什麼時候
    算」跟「什麼時候該顯示」錯位——`.d` 欄位在開機當下算是對的，
    但它被當成「顯示用的快取」一路用到玩家升級之後，沒人重算。
    以後任何「常駐物件上快取一個依賴當下遊戲狀態算出來的欄位」的寫法，
    都要先問一句：**這個快取什麼時候會失效、由誰負責重算？**
    如果答案是「不會，也沒人負責」，那就是這次同一種陷阱。
45. ⭐ **真人玩過（CoderGenius72玩到第8關）的回饋，比只看商店頁跟程式碼
    給的回饋（上一輪 HKM）更接近事實，但依然要逐點查證，不能整體照單全收
    也不能整體駁回。** 這次 6 點裡：2 點真的存在且比回報的更嚴重（傷害數字）
    或就是字面意思（血條交接）、1 點是已經做好的、1 點是快捷鍵缺失（小修）、
    2 點是需要更多證據才能動的平衡數值猜測。同一份回饋裡不同的點，
    證據強度完全不一樣，不能用同一個標準處理。
    46. **測試本身也可能踩到「輸入是機率的」這個坑**（AGENTS.md 第3節鐵則 9
    早就寫過一次，這次是同一種陷阱的變體）：上次是遊戲邏輯本身藏了 5% 爆擊，
    這次是測試呼叫的函式（`rollCards()`）本身回傳結果就是加權隨機的，
    測試卻拿單次結果當成「一定找得到」的斷言基礎。平行跑（CPU 節流、
    執行時機更分散）不會改變 `Math.random()` 的機率分布，但**平行跑的測試數量
    夠多，會把低機率事件的發生次數從「幾乎不會遇到」變成「遲早會遇到」**——
    這也是為什麼「只跑過一次全綠」不能當作測試穩定的證據，AGENTS.md 3.5 條
    「跨批次數字不能互比」背後的同一個道理，也適用在「測試本身有沒有踩到隨機性」
    這件事上。
47. **「查證後發現已經做好」也是有價值的產出**，不是浪費工——沒有這一步，
    很容易在下一版又被同一則回饋觸發重複勞動，甚至改壞原本正確的邏輯。
    查證過程要留紀錄（這份文件），不是查完就丟。

### 環境現況更新

- **版本號**：v0.9.30
- `spawnBoss()`：修正多 Boss 重疊時血條被無條件覆蓋的問題
- `rollCards()`：武器升級卡片的傷害數字改成即時現算（`lv+1`），不再凍結在 Lv.1
- `WEAPONS.yoyo.desc()` / `w_yoyo_d`：補上原本缺少的傷害數字顯示
- 新增 `E` 鍵核彈快捷鍵
- 新增存檔備份碼一次性主動提示（`codeNudge`，第一次清第5關時顯示一次）
- `resize()`：加速/核彈按鈕改成依視窗較短邊等比例縮放，修正平板大螢幕上
  按鈕過小、擠在角落的問題；手機尺寸逐位元組回歸不變
- 新增 `tests/py_v0930.py`；`run_tests.sh` 的 `PY` 清單加入 `py_v0930`
- 新增 `i18n/build_v0930.py`（改寫既有 key `w_yoyo_d` × 11 語言）、
  `i18n/build_v0930b.py`（新增 key `codeNudge` × 11 語言）
- 多語系 key 數：230 → **231**
- 測試工具：38 → **39 支**（新增 `py_v0930`；實測 `run_tests.sh` 全套平行跑
  結果為 39 支全綠，以這個數字為準）

## 三十四、v0.9.31 —— 開場操作提示改成裝置中立文字，順手抓到一個潛伏的排版 bug

### 背景：兩位 itch.io 玩家的留言合起來才拼出全貌

先是玩家 **Barely Playable Games** 給了一則泛泛的操作回饋：喜歡整體呈現，
但「移動控制」是最大的問題——不確定是不是自己誤解操作方式，但常常搞不清楚
自己是不是真的往想要的方向移動、還是感覺在「跟操作對抗」。這則回饋本身
沒有具體重現步驟，比較像主觀感受。

查證方式跟過去處理單一模糊回饋一樣：先去讀搖桿的程式碼（`moveJoy()` /
`startJoy()` / `syncJoy()`），確認是標準的「虛擬搖桿」設計——按下的地方是
圓心，拖曳方向決定移動方向，畫面上會即時畫出那個搖桿圈跟指示點。接著寫了
一支用瀏覽器層級真實觸控事件（CDP `Input.dispatchTouchEvent`，不是合成的
`TouchEvent`）的驗證腳本，分別拖上下左右四個方向，比對「拖曳向量」跟
「角色實際位移」——結果完全正確，沒有反向、沒有 X/Y 軸互換；又測了拖超過
62px 半徑、錨點跟著手指跑之後手指停住不動的情況，方向也沒有抖動反向。
**結論：不是 bug，程式碼本身沒問題。**

接著另一位玩家 **HKM Industry**（上一版 v0.9.29／v0.9.30 都提供過查證後
證實有用的技術回饋）在同一串留言裡提出一個更精準、可證偽的假說：這個遊戲
其實有兩套輸入系統——手機觸控、桌機 WASD/方向鍵——如果「拖曳搖桿」在桌機上
也是透過滑鼠事件在跑，那滑鼠點下拖曳會在按下的地方種一個錨點，跟「手指貼在
玻璃上」的觸控體感完全不同；尤其是拖超過半徑、錨點跟著移動的「重新置中」，
在觸控上很順手，但滑鼠游標本身有自己的絕對位置，重新置中反而會製造出
「我明明看著游標往這邊移動，角色卻沒有照著走」的錯覺。並直接問了一個
關鍵問題：**桌機的拖曳搖桿，是不是真的也吃滑鼠事件？**

查程式碼確認：**是的**——`cv.addEventListener('mousedown', ...)` 一樣呼叫
`startJoy(-1, e.clientX, e.clientY)`，走的是跟觸控完全相同的搖桿邏輯。
又寫了一支非觸控（`has_touch` 關閉）情境的驗證腳本，用真實滑鼠事件
（`page.mouse.down/move/up`）重現：確認滑鼠拖曳的確會啟動同一套搖桿、
方向正確；同時測了「按著 WASD 移動時，滑鼠只是點一下沒有拖曳」的情境——
搖桿會啟動但 `mag` 維持 0，鍵盤移動不會被打斷（程式碼裡 `if(IN.active &&
IN.mag>0.06)` 這個門檻本身就防住了「單純點一下滑鼠」誤觸）。

HKM 猜錯的一個細節：他以為「畫不出東西的虛擬搖桿等於隱形」，但程式碼裡
`if(IN.active){...}` 這段一直都會在按下的當下畫出搖桿圈跟指示點——這個
部分不需要修。**但他猜對的核心是真的：桌機滑鼠拖曳的確跟觸控共用同一套
「相對方向」搖桿，而遊戲裡開場提示的英文原文是「Touch anywhere and drag
to move」——這句話本身就在暗示「這是觸控裝置專用，桌機玩家不用管」，
桌機玩家如果因此跳過這句提示，之後不小心用滑鼠拖曳觸發了這套「相對方向」
邏輯，卻沒有心理準備，體感自然就是「在跟操作對抗」。這正好能解釋 Barely
Playable Games 的困惑，即使我們沒辦法向他百分之百求證他當時用的是哪種
輸入方式。**

### 這次做的事：把 hintMove 改成裝置中立的說法

查了全部 11 種語言的 `hintMove`，發現不是英文特例——每一種語言都用了
「螢幕/手指」「touch/toucher/tocar/schermo/tela/экрана」這類明確指向
觸控裝置的詞。改法不是加字（加字反而會讓文字更長、更容易超出畫面，見下），
而是**拿掉裝置專屬詞**，換成中性的「按住／點下拖曳」講法——英文從
「Touch anywhere and drag to move」改成「Press anywhere and drag to
move」，其餘語言同理，長度盡量跟原文接近（`i18n/build_v0931.py`，
改既有 key，不是新增）。

沒有動任何搖桿/移動的程式碼——問題出在「玩家不知道滑鼠拖曳也算數」，
不是輸入邏輯本身有錯，所以修的是文字，不是機制。這跟 v0.9.30 處理
CoderGenius72 的平衡數值回饋是同一種紀律：**單一玩家的主觀感受，
配上可驗證的技術推理，才值得動手；但動的手要跟證據對應的大小一樣大**——
這裡證據指向的是「文字沒講清楚」，不是「搖桿演算法錯了」，所以只改文字。

### 意外抓到的東西：這個提示區塊的排版從來沒被測過

改 `hintMove` 之後照 AGENTS.md 第 3 節寫驗證腳本，比對 11 種語言在
390px 寬手機畫面下會不會超出邊界——結果發現**不只是我改的那一行**，
`hintDash` / `hintHearts` / `hintChest` 這三行**本來就有嚴重超框**，
德文、法文、西班牙文、葡萄牙文、俄文最多寬出畫面 30% 以上（例如
`pt-BR` 的 `hintDash` 全寬達 482px，畫面只有 390px）。

根因：`py_layout_fit.py`（v0.9.21 就存在、專門抓「翻譯放不放得下」的測試）
在每次進場景後**立刻把 `G.hasMoved` 設成 `true`**，而這四行提示的顯示條件
正是 `!G.hasMoved && G.t<22`——測試為了避免這個一直閃爍的提示干擾其他斷言，
順手跳過了它，副作用是這個區塊從 v0.9.21 到現在**從來沒被測過**，超框
問題可能從很早的版本就存在，只是沒人（包含這幾位認真留言的玩家）specifically
拿桌機瀏覽器去看過開場那 2 秒。

修法：這四行改用專案裡既有的 `fitFont()`（跟「大字」特效同一套機制——
先縮字級，縮到指定下限為止）動態量測，不再假設任何語言的翻譯長度，
`hintMove` 17px 縮到最小 12px、其餘三行 13px 縮到最小 10px。同時把
`py_layout_fit.py` 補上第 4 段測試，直接測這四行在 11 種語言下都不超框——
不再讓 `G.hasMoved=true` 把這個區塊永久排除在測試之外。

### 測試

三輪：①改完 `hintMove` 用一支臨時腳本量 11 種語言的實際渲染寬度，
第一次量就抓到 5 種語言超框（含 4 行裡的 3 行是既有問題、不是我改的那行）
②套用 `fitFont` 之後同一支腳本重跑，11 種語言全部通過，`syntax_check.js`
過、既有 `py_layout_fit.py` 三段全綠 ③把新測試併進 `py_layout_fit.py`
第 4 段、`run_tests.sh` 全部 35 支（34 支平行 + `py_v0927_perf` 單獨跑）
在最嚴苛條件（35 支一起搶 CPU）下全綠；另外重跑一次滑鼠方向驗證腳本
（四個方向 + 錨點重新置中）跟真實滑鼠事件腳本，確認搖桿邏輯本身在改動
前後行為完全一致（沒有因為這次改動而改到任何移動相關程式碼）。

### 教訓

48. ⭐ **一句「聽起來很有道理」的技術假說，跟一段「已經驗證過」的事實，
    是兩件事，要分開處理。** HKM 這次的假說（桌機滑鼠也吃搖桿事件）
    講得很精準，但在查過 `mousedown` 的程式碼、跑過真實滑鼠事件的驗證
    腳本之前，它仍然只是假說——**猜得準不代表可以省略驗證這一步**，
    這次剛好猜對了，但下次可能猜錯，驗證的成本遠低於猜錯就動手改的風險。
49. ⭐ **一個對方猜錯的細節（「搖桿沒畫出來」），不代表整個假說都錯。**
    逐點查證的紀律不是「只要有一點錯就整個駁回」，也不是「只要有一點對
    就整個照單全收」——這次是「畫面回饋」那點錯了、「桌機也吃滑鼠拖曳」
    那點對了，兩點分開處理，回覆對方的時候也要分開講清楚，不能含糊帶過。
50. ⭐⭐ **測試刻意跳過某個區塊，跟「這個區塊沒問題」完全是兩回事。**
    `py_layout_fit.py` 為了避免開場提示的閃爍動畫干擾其他斷言，把
    `G.hasMoved` 設成 `true` 跳過它——這個決定本身沒有錯，但代價是
    這個區塊從此完全沒有安全網，超框問題可以潛伏好幾個版本沒人發現。
    **「測試涵蓋率」不是「有沒有測試檔案」，是「這個測試檔案實際跑過
    了哪些程式碼路徑」——`py_layout_fit.py` 這個名字聽起來像是「排版
    測試都在這裡」，但實際上漏了一整個常駐區塊。** 以後新增任何「只在
    特定條件下顯示」的文字，要主動問一句：現有測試會不會為了方便而跳過
    這個條件？
51. 這次改動的起點是一則沒有具體重現步驟的模糊回饋（Barely Playable
    Games），最後真正定位問題、給出可驗證假說的是另一位玩家（HKM
    Industry）的追加留言——**同一件事，不同的人會給出不同解析度的
    資訊，晚到的、更精確的線索不代表可以無視先到的、模糊的線索**：
    沒有 Barely Playable Games 先提出「感覺在對抗」，HKM 也不會知道
    要往「桌機輸入模型」這個方向去想。兩則留言合起來才有這次的修正。

### 環境現況更新

- **版本號**：v0.9.31
- `hintMove`：開場移動提示文字改成裝置中立說法（不再限定「觸控/手指/螢幕」），
  11 種語言同步（`i18n/build_v0931.py`，改既有 key，key 數不變）
- 開場提示四行（`hintMove`/`hintDash`/`hintHearts`/`hintChest`）改用
  `fitFont()` 動態縮字，修正好幾種語言本來就存在、但從未被測過的超框問題
- `tests/py_layout_fit.py`：新增第 4 段，測開場提示四行在 11 種語言下
  都不超框；不再讓 `G.hasMoved=true` 把這個區塊排除在測試之外
- 搖桿/移動邏輯本身**沒有改動**，用真實觸控事件與真實滑鼠事件雙重驗證
  過方向正確、沒有反向、沒有因為這次改動而改變行為
- 多語系 key 數：231（不變，這次是改既有 key 的值）
- 測試工具：39 支（不變，`py_layout_fit.py` 是加測項不是加檔案）

---

## 三十五、v0.9.32 —— 戰報拍立得上的主角沒有臉

### 起因：第一次真機（Android App）實測

2026-09-05，`npx cap add android` → `npx cap sync` → Android Studio 建置成功，
**第一次把遊戲裝進真正的 Android 手機**（Google Pixel 10 Pro）而不是網頁版。
使用者實玩後按了「存戰報」，回報：

> 存戰報會下載 png 的過關圖片，但是主角的表情並沒有在截圖上，只是一個光球，
> 這樣的效果不好，應該把表情強制放上。

### 順帶推翻的一個推測（記下來，避免以後又猜錯）

在使用者實測之前，我推測「`<a download>` 搭配 `data:` URL 在 Android WebView
裡通常無效，所以存戰報按鈕在 App 版很可能按了完全沒反應」。

**實測結果：推測錯誤，下載完全正常。** Capacitor 的 WebView 有處理這條路徑。
教訓是老的那一條：**沒實測過的推測就是推測，不要因為它「聽起來很合理」
就當成結論寫進待辦清單**——這次幸好只是多花了使用者按一次按鈕的成本。

### 根因

`makePolaroid()` 的「照片區」畫的主角只有這兩行：

```js
x.fillStyle='#6ff0e0'; x.shadowColor='#4ff0ff'; x.shadowBlur=30;
x.beginPath(); x.arc(px+pw/2,py+ph/2,42,0,6.283); x.fill();
```

一顆 42px 的純色圓 —— **沒有白色外框、沒有眼睛、沒有高光**，
跟 `drawPlayer()` 畫出來的角色完全不是同一個東西。

這不是「漏畫」，是這個函式從寫出來那天就這樣。之所以拖到現在才被發現：
**整套 52 支測試沒有任何一支碰過 `makePolaroid()`**
（`py_cjk_scan.py` 只是剛好在原始碼字串裡掃到 `btnSave` 這個字而已）。

而這張圖底下就印著 `#GooBlaster`、整個設計目的就是給玩家分享到社群——
**主角認不出來等於白白丟掉品牌辨識度**，是行銷面的實質損失，不只是美觀問題。

### 這一版只動了什麼

新增 `drawHeroPortrait(x, cx, cy, R, win)`，比例完全照抄 `drawPlayer()`：

| 元素 | `drawPlayer()` | `drawHeroPortrait()` |
|---|---|---|
| 本體色 | `#6ff0e0` | 同 |
| 外框 | `#e8fffb`，線寬 `R*0.147`（2.5/17） | 同 |
| 眼睛位置 | `±R*0.32`, `-R*0.1` | 同 |
| 眼睛半徑 | `R*0.19` | 同 |
| 左上高光 | `-R*0.28`, `-R*0.4`, `R*0.19` | 同 |
| 半徑 | 17（遊戲內） | **78**（讓它在 492×430 的照片區裡真的像張人物照） |

**唯一的差別是表情會依勝負變化**：過關 = 往上彎的笑眼 `^ ^`，失敗 = `× ×`。

兩個刻意的取捨：

1. **只換眼睛形狀，不加嘴巴。** 遊戲裡的主角本來就沒有嘴，加了會變成另一隻
   角色，反而傷害辨識度。角色的三個辨識特徵（果凍藍綠 `#6ff0e0`、白色外框、
   左上高光）在勝負兩種狀態下都保留。
2. **高光改畫在眼睛「之前」。** 遊戲內是高光蓋在眼睛上（刻意的果凍反光感），
   但在這張圖上表情才是主角，高光壓在笑眼上會把弧線吃掉一截。這是這兩個
   函式唯一的繪製順序差異，已寫在程式碼註解裡。

⚠ **純視覺，只影響這張分享圖。** 沒有動 `drawPlayer()`、沒有動任何數值、
沒有動任何字串（所以不涉及 11 語言，key 數維持 231）。

### 測試：補上從來沒有過的那一支

新增 `tests/py_polaroid.py`，並加進 `run_tests.sh` 的預設批次
（**加測試檔卻沒加進 run_tests.sh 等於沒加**——這是第 3 節第 3 條
「沉默失敗的測試等於沒有測試」的變體）。

驗收標準全部用**真的讀畫素**，不是讀原始碼：

1. 角色圓內必須有深色畫素（臉真的畫上去了）
2. 深色畫素必須左右各一群（是兩隻眼睛，不是隨便一塊黑）
3. 本體色與白色外框都必須存在（角色本體還在）
4. 過關與失敗的臉部畫素必須不同（表情真的有隨勝負改變）

實測數字：過關圖臉部 717 個畫素（左 359／右 358），失敗圖 1472 個
（左 736／右 736）——左右對稱代表兩隻眼睛都畫對了。

### 寫這支測試時自己踩到、又自己抓回來的坑

第一版的「勝負兩張圖必須不一樣」寫成**比對整張圖的位元組**，跑出來
「524584 個位元組不同」，看起來很漂亮地通過了。

**但那個斷言等於沒有斷言**——照片區背景那 130 顆泡泡的顏色、位置、大小
是 `makePolaroid()` 每次呼叫都重抽的隨機值，所以就算表情完全沒改，
兩張圖也一定會有幾十萬位元組不同。這正是第 3 節第 9 條
「**測試的輸入不能是機率的**」。

改成只比對**角色圓形範圍內**的深色畫素座標集合——那一塊由
`drawHeroPortrait()` 畫出來，完全沒有隨機成分。同理，圓「外」的深色畫素
（背景泡泡）一律不採計。

順帶把 HTTP server 的埠改成「被佔用就往上找」，避免上一輪留下的
TIME_WAIT socket 讓整支測試以 `Address already in use` 假紅字收場。

### 教訓

52. **「沒有測試碰過」的程式碼區域，就是 bug 的溫床。** `makePolaroid()`
    從寫出來到現在跨了十幾個版本，沒有任何一支測試進去過，結果一個
    「主角沒有臉」這麼明顯的問題活到了第一次真機實測才被人眼抓到。
    寫完新功能要問的不是「這段程式對不對」，而是「**有哪支測試會在它壞掉時變紅**」。
53. **推測要標成推測，而且要儘早拿去撞現實。** 這次同一輪裡我提了一個
    推測（WebView 下載會失效），使用者按一次按鈕就推翻了。標成推測沒有錯，
    錯的是差點把它排進待辦清單當成「要修的 bug」。
54. **視覺功能一定要真的把圖產出來用眼睛看。** 這次的像素測試會通過，
    但「78px 夠不夠大」「笑眼弧度好不好看」「會不會被 CLEAR! 字擋到」
    這幾件事，只有把 PNG 存出來實際看過才知道。

### 環境現況更新

- **版本號**：v0.9.32
- 新增 `drawHeroPortrait()`；`makePolaroid()` 改呼叫它取代原本的純色圓
- `drawPlayer()`、所有數值、所有字串**完全未動**（多語系 key 數維持 231）
- 測試工具：**40 支**（新增 `tests/py_polaroid.py`，已加進 `run_tests.sh`）

---

## 三十六、v0.9.33 —— 🍬 軟糖再生的無限回血

### 起因：使用者真機試玩，直覺抓對問題但抓錯卡片

2026-09-05 第一次把遊戲裝進真機之後，使用者回報：

> OK繃每 3 秒回覆一顆愛心，應該要限制在多少的秒數內有這個作用（等於最多能
> 恢復幾顆愛心），因為如果吃到這個道具就延續一整局都有這個回覆效果，
> 那就有點無敵狀態了。

**查證後：他講的那張卡不是這個效果。** 🩹 是「急救棒棒糖」（`panic`），
效果是「受傷後 3 秒內移速 +75%，無敵時間也更長」——**完全不回血**，
「3 秒」是移速加成的持續時間。

**但他擔心的「吃到就整局無敵」是真的存在的，只是在另一張卡上**：
🍬 **軟糖再生**（`regen`）——每 40 秒（Lv.2 是 25 秒）回一顆愛心，
**完全沒有次數上限**。

這是很值得記下來的一種回饋型態：**玩家的體感是對的，歸因是錯的。**
如果只照字面去查 🩹 那張卡，會得到「使用者記錯了，沒問題」的結論而結案，
真正的漏洞就留在遊戲裡了。

### 根因與實測數字

愛心上限從 v0.9.6 起就固定是 3 顆，但關卡長度一路加到 240 秒——
**這張卡的價值會隨著關卡變長而線性膨脹，卻沒有任何東西擋著。**

用一份「只拿掉 `P.regenN<cap` 這個條件、其餘完全相同」的對照版實測
（Lv.2、每輪把愛心打回 1 顆確保永遠有回血空間）：

| 關卡 | 長度 | 改之前實際回血 | 改之後 |
|---|---|---|---|
| 第 5 關 | 140 秒 | **6 顆** | 4 顆 |
| 第 25 關 | 215 秒 | **9 顆** | 4 顆 |
| 第 50 關 | 240 秒 | **10 顆** | 4 顆 |

第 50 關那個 10 顆，是在一個「血量上限只有 3 顆」的角色身上——
等於**實際血量變成 13 顆、翻了 4.3 倍**，而且關卡越長越誇張。
一張升級卡就把整條難度曲線壓平了。

### 這一版只動了什麼

`update()` 裡的 regen 分支加上每關次數上限，並新增 `P.regenN` 計數器：

```js
const cap = P.regen>=2 ? 4 : 2;                 // 整關最多回幾顆
if(P.regenT>=iv){ P.regenT=0;
  if(P.hearts<P.maxHearts && P.regenN<cap){
    P.hearts++; P.regenN++; ... } }
```

三個刻意的設計取捨：

1. **用「顆數」而不是「幾秒內有效」當上限。** 使用者原話問的是「限制在多少
   秒數內」，但對玩家來說「最多回 2 顆」一眼就懂，「前 80 秒內有效」還要
   自己換算，卡片描述也寫不清楚。
2. **只有真的補到血才扣扣打。** 滿血時計時器照樣歸零，但不消耗次數——
   否則玩家躲得好、一路滿血空轉掉幾次額度，真的受傷時反而沒得回，
   會變成「越會閃的人懲罰越重」的反效果。
3. **上限是「每關」不是「終身」。** `P.regenN` 隨 `newGame()` 重建歸零。

即使砍到 Lv.1 只剩 2 顆，對一個上限 3 顆心的角色來說仍然接近多一條命，
這張卡不會變成廢卡——它只是不再是無限。

⚠ 卡片描述改了，所以 **11 種語言同步更新**（`i18n/build_v0933.py`，
改既有 key `u_regen_d` / `u_regen_d2`，key 數維持 231）。
`py_i18n`／`py_cjk_scan`／`py_layout_fit` 三支都跑過，新的（比較長的）
描述在 11 種語言下都沒有超框。

### 測試

新增 `tests/py_regen_cap.py`（第 41 支），已加進 `run_tests.sh`。
四項驗收全部驅動真的 `update()` 迴圈：

1. Lv.1 上限 2 顆、Lv.2 上限 4 顆
2. 滿血空轉不扣扣打——空轉 150 秒後才受傷，仍然回得滿 4 顆
3. 重開一關後額度歸零重給
4. 沒拿這張卡時完全不回血

### 寫這支測試時踩到的坑

第一版把愛心設成 1 顆之後就一路跑到底，結果 Lv.2 只回了 2 顆就停住，
測試報「上限應該是 4」的紅字。**那不是程式的 bug，是測試設計錯了**——
愛心上限是 3，從 1 顆開始最多就只可能回 2 顆，撞到的是**愛心上限**、
不是 regen 的次數上限，用那種跑法永遠測不到 cap=4。

改成模擬真實玩家的節奏：每一輪都先把愛心打回 1 顆再跑一個週期，
確保永遠有回血空間，`regenN` 停在哪裡才真的反映次數上限。

另外 `G.waves = []` 會讓 `updateSpawn()` 讀 `w.rate` 時噴
`Cannot read properties of undefined`——改成把每一波的 `rate` 歸零、
`G.L.maxE = 0`，效果一樣但不破壞資料結構。

### 教訓

55. ⭐ **玩家的體感是資料，玩家的歸因是假設。** 這次使用者精準指出了
    「有一張卡讓人變得太無敵」這個**現象**，但把它歸給了錯誤的卡片。
    照字面去查 🩹 會得到「使用者記錯了」而結案——**要查的是他描述的效果，
    不是他點名的物件**。這跟 v0.9.31 那次（兩位玩家的模糊回饋合起來才
    拼出真相）是同一類：回饋的價值在現象，不在玩家自己的技術解釋。
56. **會隨關卡長度膨脹的效果，一定要有絕對上限。** 這個漏洞的成因是
    v0.9.6（愛心上限固定 3 顆）與後來關卡長度一路加到 240 秒這兩件事
    **各自都合理、湊在一起才出問題**。往後只要寫出「每 N 秒發生一次」的
    好處，就要立刻問：**關卡最長的時候這會變成多少？**
57. **測試紅字要先問「是程式錯還是測試錯」。** 這次的紅字完全是測試設計
    問題（撞到愛心上限而不是次數上限）。如果當時直接把預期值從 4 改成 2
    「讓它變綠」，就會把真正的上限測試永遠鎖死在測不到的狀態。

### 環境現況更新

- **版本號**：v0.9.33
- `P.regenN`（新欄位，每關歸零）＋ regen 分支的 `cap` 判斷
- 多語系：`u_regen_d` / `u_regen_d2` 改文字，11 語言同步，key 數維持 **231**
- 測試工具：**41 支**（新增 `tests/py_regen_cap.py`，已加進 `run_tests.sh`）
- ⚠ 其他所有數值、卡片、關卡表**完全未動**（遵守 1.4 一次只改一個變數）

---

## 三十七、v0.9.34 —— 開機後主畫面永遠停在第 1 關

### 起因：真機「關掉再重開」才會踩到

2026-09-05 使用者在 Android 實機上測「進度存不存得住」，回報：

> 1. 重開後糖果幣依舊在。
> 2. 重開後主畫面的關卡都會重新跑到「重玩第一關」的選項，而不是停留在最新關卡

**「金幣在、但關卡回到第 1 關」這個組合本身就是最好的線索**——存檔明明讀成功了
（金幣是從同一份存檔來的），所以問題不在儲存層，在畫面狀態。

### 根因

`SEL_IDX`（銀河圖上目前選取/預覽的關卡）宣告是 `let SEL_IDX=0`，
而唯一把它設成 `PROGRESS-1` 的地方是 `showMenu()`：

```js
function showMenu(){
  clampProgress();
  SEL_IDX=Math.min(PROGRESS-1,LEVELS.length-1);   // ← 只有這裡
  renderStage();
```

**但開機路徑根本不會經過 `showMenu()`**，開機是：

```js
applyLanguage();      // 這裡順便重畫了商店按鈕，所以金幣是對的
renderStage();        // ← 直接畫，SEL_IDX 還是初始值 0
hydrateFromNative();  // 非同步；resolve 後也只呼叫 renderStage()
```

金幣之所以正確，是因為 `applyLanguage()` 會重建商店按鈕的 innerHTML、
順手把 `COINS` 印進去（第 2314 行）。**兩個資料來自同一份存檔，
一個有跟上、一個沒有，純粹是因為更新它們的程式碼在不同地方。**

### 這不是 v0.9.28 造成的，範圍也比回報的大

⚠ **網頁版一樣有這個 bug**，而且存在很久了。之所以沒人發現：網頁版玩家
通常是「玩完一關 → 回選單」（那條路徑有經過 `showMenu()`），很少真的
把分頁關掉再重開。是包成 App、養成「關掉再打開」的習慣之後才浮出來。

而 **App 重裝**的情境更糟：`localStorage` 是空的、進度只在原生儲存裡，
`hydrateFromNative()` 非同步讀完後也只呼叫 `renderStage()`——
玩家會看到「金幣回來了但關卡停在第 1 關」，比完全沒存檔更令人困惑。

### 這一版只動了什麼

把那一行抽成 `syncSelToFrontier()`，並補在**三個**需要它的地方
（原本只有一個）：

| 呼叫點 | 為什麼需要 |
|---|---|
| 開機（`renderStage()` 之前） | 冷開機不經過 showMenu()，本次修的主要問題 |
| `hydrateFromNative()` 合併完成後 | App 重裝時 PROGRESS 可能比 localStorage 那份高 |
| `showMenu()` | 原本就有，改成呼叫共用函式 |

順便在原生合併完成後也更新一次金幣顯示——那條路徑原本同樣沒更新，
只是剛好被開機時的 `applyLanguage()` 蓋掉才看起來正常。

⚠ 沒有動任何數值、任何字串（不涉及 11 語言，key 數維持 231）。

### 測試

新增 `tests/py_boot_sel.py`（第 42 支），已加進 `run_tests.sh`。
**而且先證明了這支測試真的抓得到 bug**：拿修正前的對照版跑，
它精準複現了使用者回報的現象——

```
PROGRESS=8　SEL_IDX=0（預期 7）　金幣顯示「4321」　畫面「Stage 1 / 50」
```

修正後同一支測試全綠（SEL_IDX=7、畫面「Stage 8 / 50」、金幣 4321）。

涵蓋：純網頁冷開機（progress = 1 / 2 / 8 / 50 / 51 五種）、
畫面顯示的關卡編號有沒有跟上、以及**用假的 Capacitor Preferences
模擬 App 重裝**（localStorage 清空、進度只在原生儲存）。

### 寫測試時自己誤報一次

第一版斷言「畫面上一定要出現關卡編號」，結果 `progress=51`（全 50 關破完）
時紅字——因為 `renderStage()` 在全破且選在最後一關時會**刻意**顯示
`ALL CLEAR` 而不是「Stage 50 / 50」。**那是正確行為，是我的斷言沒考慮到。**
已在測試裡把這個例外寫清楚，避免下一個人以為是 bug。

### 教訓

58. ⭐ **「同一份資料，一半對一半錯」直接指向「更新它們的程式碼不在同一處」。**
    金幣對、關卡錯，不可能是存檔壞掉——存檔壞了兩個都會錯。這個線索一開始
    就把範圍從「原生儲存層」縮到「畫面狀態同步」，省掉整個錯誤的偵查方向。
59. **同一個狀態有三個入口，就不該讓那行程式碼只寫在其中一個入口裡。**
    抽成 `syncSelToFrontier()` 不是為了好看，是因為「開機 / 非同步合併 / 回選單」
    三條路都會到主選單，漏任何一條就會有一種情境是壞的。
60. **包成 App 會改變玩家的使用習慣，因而暴露出網頁版一直都有的 bug。**
    網頁版沒人「關掉再重開」，App 每次都是。往後真機測試要特別針對
    「冷啟動」「重裝」「切到背景再回來」這些網頁版少見的路徑。

### 環境現況更新

- **版本號**：v0.9.34
- 新增 `syncSelToFrontier()`；開機路徑與 `hydrateFromNative()` 都補上呼叫
- 多語系 key 數：231（不變，這一版沒有動字串）
- 測試工具：**42 支**（新增 `tests/py_boot_sel.py`，已加進 `run_tests.sh`）
- 另新增診斷工具 `tests/py_boss_pressure.py`（**不進預設批次**，量 Boss 火力曲線）

---

## 附：Boss 壓力曲線量測（2026-09-05，尚未有對應修正）

使用者回報「第 6 關之後難度變得太高，玩到第 8 關即使買了很多強化仍難以過關，
尤其 Boss 子彈又多又快又密集」。itch.io 玩家 CoderGenius72 先前也是卡在第 8 關
——**兩個獨立的人撞到同一面牆**。

笨 bot 量不到這一段（第 3 關左右就陣亡），所以改用 `py_boss_pressure.py`
隔離量測「同一時間畫面上的敵方子彈數」：

| 關 | Boss 數 | 總血量 | 攻擊間隔 | 子彈峰值 | 平均同屏 |
|---|---|---|---|---|---|
| 3 | 2 | 5,184 | 3.52 / 2.72 | 30 | 7.4 |
| 4 | 2 | 6,624 | 3.19 / 2.46 | 26 | 6.7 |
| 5 | 2 | 25,648 | 2.97 / 1.76 | 25 | 7.6 |
| 6 | 2 | 9,792 | 2.68 / 2.07 | 26 | 7.0 |
| **7** | **3** | **30,720** | **2.46 / 1.90 / 1.68** | **84** | **22.8** |
| 8 | 3 | 36,480 | 2.20 / 1.70 / 1.50 | 84 | 24.2 |
| 9 | 3 | 48,000 | 2.02 / 1.56 / 1.38 | 101 | 24.2 |
| 10 | 4 | 145,520 | 1.80 / 1.39 / 1.23 / 1.04 | 126 | 30.0 |

**第 6 關 → 第 7 關：子彈峰值 26 → 84，一關之內 +223%。**
第 3~6 關整整四關持平在 25~30，然後突然乘以 3.2 倍。

根因是結構性的：`nboss` 由 2 變 3，同時攻擊間隔全面縮短。
而且 `genExtraLevels()` 裡第 11~50 關**全部硬寫 `nboss: 3`**——
第 7 關就是永久進入「三 Boss 制」的那道門。

⚠ **這一版刻意不動它**（遵守 1.4 一次只改一個變數）。
使用者另外提議的「被打到後 3 秒無敵」也記在這裡：該機制**已經存在**，
目前是 `P.iframe = P.panic ? 2.1 : 1.45`（秒），提議的是延長它。
兩件事分開處理，且應**先修懸崖**——延長無敵是全域的鈍器，
會連本來就正常的第 1~6 關與第 50 關一起變簡單，先動它會蓋掉懸崖的基準。

---

## 三十八、v0.9.35 —— 第 7 關那道 3.2 倍的難度懸崖

### 起因：兩個互不相識的真人撞到同一面牆

- itch.io 玩家 CoderGenius72（v0.9.30 那次回饋）玩到**第 8 關**卡住
- 專案負責人 2026-09-05 真機試玩：「第 6 關之後難度變得太高，我玩到**第 8 關**，
  也使用糖果幣做了很多增強，依舊難以過關，尤其是 boss 發射的子彈又多又快又密集」

笨 bot 量不到這一段（第 3 關左右就陣亡），所以先做了一支診斷工具
`tests/py_boss_pressure.py`，把 Boss 火力單獨隔離出來量「同一時間畫面上的敵方子彈數」。

### 根因：v0.9.26 那個修正的前提，在第 7 關悄悄失效了

v0.9.26 曾經修過一模一樣的問題（第 5 關章節 Boss 彈幕太密），
它的註解結尾寫著：

> 一般 Boss 不縮放（cross 8 顆本來就讀得懂），只縮章節 Boss。

**這個推論在當時是對的，但它在第 7 關就不成立了。**

`buildBosses()` 依 `nboss` 從 `BOSS_POOL` 取前 N 隻：

| nboss | 取到的 Boss | 攻擊型態 | 一波顆數 |
|---|---|---|---|
| 2（第 3、4、6 關） | [0], [1] | cross ＋ spiral | 8 ＋ 6 = **14** |
| 3（**第 7 關起**） | [0], [1], **[2]** | cross ＋ spiral ＋ **ring** | 8 ＋ 6 ＋ **19** = **33** |

`BOSS_POOL[2]` 的攻擊型態正是 **ring（一波 19 顆、暴走 25 顆）**——
跟 v0.9.26 當初判定「太密、必須縮」的章節 Boss 是同一種東西。
第 7 關是 `nboss` 由 2 變 3 的那一關，也就是**一般 Boss 第一次帶著 ring 出場**。
而 `genExtraLevels()` 第 11~50 關全部硬寫 `nboss: 3`，所以這道門一開就不會關。

### 量到的數字（改之前）

```
關 3   2 Boss   峰值 30   平均同屏 7.4
關 4   2 Boss   峰值 26   平均同屏 6.7
關 5   2 Boss   峰值 25   平均同屏 7.6
關 6   2 Boss   峰值 26   平均同屏 7.0
關 7   3 Boss   峰值 84   平均同屏 22.8   ← +223%
關 8   3 Boss   峰值 84   平均同屏 24.2
```

**第 3~6 關整整四關持平在 25~30，第 7 關一次乘以 3.2 倍。**

### 先修測量工具：它的輸入原本是隨機的

第一版工具跑出「第 3 關 30 → 改完變 26」，但第 3 關**沒有 ring Boss、
照理不該有任何變化**。連跑三次同一份程式碼才發現：`spawnBoss()` 用
`rnd(0,6.283)` 決定出場方位角，各隻 Boss 的相對位置會影響彈幕重疊程度，
第 3/4 關的峰值在 26~30 之間跳動（±15%）。

這正是 AGENTS.md 第 3 節第 9 條：**餵進去的東西每次都要一樣**。
改成把所有 Boss 重排成「以玩家為圓心、固定角度的等距圓陣」、
開火相位也對齊，之後同一份程式碼連跑兩次結果**逐字相同**。
（絕對值因此跟第一版不同，但那不重要——它現在是一把穩定的尺。）

### 這一版只動了什麼

沿用 v0.9.26 已經驗證過的同一套 `cs` 縮放機制，把它擴大到「一般 Boss 的 ring」：

```js
const cs = e.superBoss ? Math.min(1, 0.45+(e.chIdx|0)*0.061)
         : (e.atk==='ring' ? Math.min(1, 0.38+(e.chIdx|0)*0.125) : 1);
```

並在 `buildBosses()` 裡把 `chIdx: L.ch||0` 也給一般 Boss（原本只有章節 Boss 有）。

刻意的邊界：

- **cross（8 顆）與 spiral（6 顆）完全不縮** —— 維持 v0.9.26 的判斷，那兩種本來就讀得懂
- **章節 Boss 的公式一個字都沒改**
- ring 從第 7 關（ch1）的約一半強度起跳，**第 26 關（ch5）回到滿值**
  —— 難度還是往上，只是變成斜坡
- `chIdx` 只餵給這個縮放；雷射的判斷是 `e.superBoss && chIdx>=5`，
  一般 Boss 的 `superBoss` 是 false，不會因為多了這個欄位跑出雷射

### 量到的前後數字（同一支確定性工具，同一批）

| 關卡 | 改之前峰值 | 改之後峰值 | 變化 |
|---|---|---|---|
| 5 | 17 | 17 | **0（未受影響）** |
| 6 | 18 | 18 | **0（未受影響）** |
| 7 | 76 | **50** | −34% |
| 8 | 77 | 50 | −35% |
| 9 | 79 | 54 | −32% |
| 10 | 106 | 70 | −34% |
| 11 | 105 | 79 | −25% |
| 12 | 103 | 71 | −31% |

平均同屏子彈（第 7 關）：12.1 → **7.9**（−35%）。

**懸崖高度：第 6→7 關由 +322% 降到 +178%。**

第 5、6 關前後完全相同，證明改動確實只碰到帶 ring 的一般 Boss。

### 沒有做完的事（誠實說明）

**+178% 仍然是全遊戲最大的一階。** 剩下的落差是結構性的——第 7 關本來就
多出一整隻 Boss，而且三隻的攻擊間隔（2.46/1.90/1.68 秒）都比第 6 關
（2.68/2.07 秒）短。要再往下壓，下一個槓桿是「延後 `nboss:3` 的登場關卡」
或「放慢第 7~9 關的攻擊間隔」。

**刻意不在這一版一起做**（AGENTS.md 1.4）：一次動兩個變數的話，
之後玩家回報「變好玩了/還是太難」時，就分不出是哪一項起的作用。
先讓這個 −34% 上真機被玩過，才有下一步的基準。

使用者另外提議的「被打到後 3 秒無敵」也**刻意留到後面**：
該機制已經存在（`P.iframe = P.panic ? 2.1 : 1.45`），延長它是全域鈍器，
會連本來就正常的第 1~6 關與第 50 關一起變簡單——先動它會直接蓋掉
這一版的基準，之後就永遠分不清楚是誰的功勞。

### 教訓

61. ⭐ **「這個判斷在當時是對的」跟「這個判斷現在還對」是兩件事。**
    v0.9.26 寫下「一般 Boss 不縮放，cross 8 顆本來就讀得懂」時，
    當時能看到的一般 Boss 確實只有 cross 與 spiral。但 `BOSS_POOL` 是依
    `nboss` 取前 N 個的，`nboss` 一旦變 3，第三個位置就把 ring 帶進來了——
    **註解裡的前提沒有被任何測試釘住，所以它失效的時候沒有人會知道。**
    往後在註解裡寫「因為 X 所以不處理 Y」時，要問：**X 會不會變？
    如果會，有沒有測試在 X 變了的時候變紅？**
62. **兩個互不相識的玩家給出同一個關卡編號，那就不是個人技術問題。**
    CoderGenius72 與專案負責人都停在第 7~8 關。單一玩家的難度抱怨要存疑
    （v0.9.30 就刻意押後過），但**獨立來源的收斂是很強的訊號**，
    值得為它專門做一支量測工具。
63. **量測工具本身要先被證明是穩定的，才能拿它的數字下結論。**
    這次差一點就用「第 3 關 30→26」去解釋一個根本沒碰到第 3 關的改動。
    同一份程式碼連跑三次，是最便宜的驗證方式。

### 環境現況更新

- **版本號**：v0.9.35
- `cs` 縮放擴大到一般 Boss 的 ring；`buildBosses()` 給一般 Boss 補上 `chIdx`
- 多語系 key 數：231（不變，這一版沒有動字串）
- 測試工具：42 支（`tests/py_boss_pressure.py` 是診斷工具，**不進預設批次**）

---

## 三十九、v0.9.36 —— 掉血的時候一直抽到「滿血才有用」的卡

### 起因

真人回報（2026-09-06）：

> 如果不是滿血狀態，那滿血攻擊力+45% 的升級道具，建議出現頻率低一點，
> 我剛剛非滿血升了兩級，結果兩次升級都有這個道具選擇。

### 根因：卡池從來不看玩家的狀態

`rollCards()` 的結構是「第一格保底抽武器卡（v0.9.25 加的，有加權），
**剩下兩格完全均勻亂抽**」：

```js
while(out.length<3&&cp.length){ out.push(cp.splice(Math.floor(Math.random()*cp.length),1)[0]); }
```

沒有任何權重，也不看 `P.hearts`。而 🔥 **滿心怒氣**（`rage`）的效果是
「愛心全滿時傷害 +45%，**被打掉就失效**」——玩家掉血時抽到它，這一格
等於白卡，而**掉血正是玩家最需要有用選項的那一刻**。

用 `tests/py_card_hp_weight.py` 對修正前的版本抽 4000 次量到：

```
滿血 3/3    → rage 出現 510/4000（12.8%）
非滿血 1/3  → rage 出現 520/4000（13.0%）
```

**兩者幾乎完全一樣**，用數字證實了「卡池完全無視血量」。
連續兩次都看到它的機率約 1.6%（12.8% 的平方）——使用者只是運氣差，
但「該不該在這個狀態出現」本身就是設計問題，跟運氣無關。

### 這一版只動了什麼

後兩格從均勻亂抽改成加權抽，權重目前只有一條規則：

```js
const RAGE_LOW_HP_W = 0.25;               // 非滿血時出現頻率降為四分之一
const stW = c => (!c.w && c.id==='rage' && P.hearts < P.maxHearts) ? RAGE_LOW_HP_W : 1;
```

兩個刻意的取捨：

1. **降權不是排除（不設成 0）。** 愛心糖寶箱與 🍬 軟糖再生都能把血補回全滿，
   rage 在掉血狀態下不是廢卡、只是期望值低。設成 0 會讓「先補血再吃怒氣」
   這條真實可行的打法變成不可能。
2. **這一版只處理 rage 這一張。** 使用者更早之前還提過「血量低的時候救命卡
   出現機率高一點」——那是**加權，不是降權**，是另一個方向的調整。
   同一版做兩件事的話，之後回報「卡池變好了/變怪了」就分不出是哪一項
   起的作用（1.4）。機制已經鋪好了，下一版加權重規則只是多一行。

⚠ 沒有動任何數值、任何字串（不涉及 11 語言，key 數維持 231）。
v0.9.25 的「第一格保底武器卡」邏輯完全沒碰。

### 量到的前後數字

| | 改之前 | 改之後 |
|---|---|---|
| 滿血 3/3 抽到 rage | 12.8% | 13.3% |
| 非滿血 1/3 抽到 rage | **13.0%** | **3.3%** |
| 比值 | 0.98 倍（等於沒差） | **4.06 倍** |

非滿血時仍抽得到（131/4000），符合「降權不是排除」的設計。

### 測試

新增 `tests/py_card_hp_weight.py`（第 43 支），已加進 `run_tests.sh`，
並**先用修正前的對照版證明它抓得到**（對照版紅字：「非滿血的 rage
出現率沒有比較低」）。

除了權重本身，這支測試同時釘住三件不該被這次改動破壞的事：

- 每一抽第一張一定是武器卡（v0.9.25 的保底）
- 一次抽出來的三張不重複
- 每次都抽得滿 3 張

⚠ 這是**機率性指標**，所以門檻用「比值落在 2~8 倍」這種寬區間，
而不是精確機率——精確門檻在機率性指標上必然會週期性誤報，
那會訓練所有人忽略紅字（第 3 節第 6 條）。

### 教訓

64. **「隨機」不等於「公平」，尤其當選項的價值取決於玩家當下的狀態。**
    均勻亂抽在紙面上很中立，但玩家掉血時抽到「滿血才有用」的卡，
    體感是遊戲在懲罰他。卡池應該問的不是「每張卡機率一樣嗎」，
    而是「**這張卡在玩家現在的處境下，有沒有可能是有用的**」。
65. **玩家報「兩次都出現」時，要分開回答兩個問題**：一是「這是不是異常」
    （不是，12.8% 抽兩次連中約 1.6%，運氣而已），二是「這個設計對不對」
    （不對，跟運氣無關）。只回答第一個會變成「你只是運氣差」，
    把一個真的設計問題駁回。

### 環境現況更新

- **版本號**：v0.9.36
- `rollCards()` 後兩格改成加權抽；目前唯一的規則是非滿血時 rage 降權至 0.25
- 多語系 key 數：231（不變，這一版沒有動字串）
- 測試工具：**43 支**（新增 `tests/py_card_hp_weight.py`，已加進 `run_tests.sh`）

### 附帶修掉一個我自己製造的假紅字（v0.9.36 同版）

連續三輪全套測試都看到 `py_layout_fit` 紅字，每次單獨重跑又是綠的，
訊息是 `OSError: [Errno 98] Address already in use`。前兩次我都當成
「上一輪殘留的 TIME_WAIT socket」帶過——**那個解釋是錯的**。

真正的原因：**`tests/py_boot_sel.py`（我在 v0.9.34 新增的）跟
`tests/py_layout_fit.py` 都寫死用 8803 埠。** 兩支平行跑時必定有一支
拿不到埠。是我加測試的時候撞到既有測試的埠，自己製造了這個假紅字。

已把 `py_layout_fit.py` 改成跟新測試同一套「埠被佔用就往上找」的寫法，
並**用當初會出事的情境驗證**（兩支同時啟動）：兩支都 exit=0。

⚠ 這正是第 3 節第 3 條「沉默失敗的測試等於沒有測試」的雙胞胎版本：
**每次都紅、但每次都被解釋掉的測試，跟沒有測試一樣。**
第一次看到就該查到底，而不是連續兩輪用「應該是環境問題」帶過。
