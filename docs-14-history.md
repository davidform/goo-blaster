# docs-14 開發史（v0.9.37 起）

> **這一份接續 `docs-04-append.md`。**
> `docs-04-append.md` 已在 v0.9.36（第三十九節）**就地凍結**，不再追加。
>
> 凍結的原因是工具面的，不是內容面的：這個開發環境**沒有 `device_bash`，
> `device_commit_files` 只能整檔覆寫**。也就是說每次要把新的一節交付到專案負責人
> 的電腦，就得把 214KB 的 `docs-04-append.md` 整份重新輸出一次（約 7 萬 output
> token），只為了追加 5KB 的新章節。一個版本的開發史不值那個代價。
>
> 舊版本的根因分析、教訓、決策理由**全部仍然有效**，要查 v0.9.36 以前的事
> 一律翻 `docs-04-append.md`（用搜尋，不要整份讀進對話）。

---

## 四十、v0.9.37 —— 重生蠟燭的「復活」有了演出

### 起因（專案負責人 2026-09-06 原話）

> 「復活再重生，應該加個復活的效果，而不是直接又把愛心加滿。」

### 查證：機制沒壞，缺的是演出

先確認他講的是不是 bug。翻 `hurtPlayer()` 裡 `G.revives>0` 那個分支，
**機制從 v0.9.17 上線以來就是完整的**：

- 愛心歸零時原地滿血復活
- 給 3.2 秒無敵（比一般受擊的 1.45 秒長很多）
- 清空場上所有敵彈

所以這不是功能缺失，是**表達缺失**。玩家在那一瞬間看到的只有：愛心從 0 突然變回 3、
一圈粒子、一行小字。他既不知道剛剛發生了什麼（是哪個東西救了我？），
也不知道自己現在還在無敵（那 3.2 秒是這張永久強化最值錢的部分，
但畫面上唯一的提示是一圈**跟普通受擊完全一樣**的綠環）。

花了 400 或 1200 顆糖果幣買來的東西，玩家感受不到它發生過——這是體感價值的浪費，
對一支靠「卡關就去刷幣變強」驅動留存的遊戲來說，是直接的商業損失。

### 這一版只動了什麼

**只加畫面與聲音，一個數值都沒動。** 復活分支裡的
`P.hearts=P.maxHearts`、`P.iframe=3.2`、`G.EB.length=0` 三行**逐字未改**。

這是刻意的：v0.9.35 剛量到第 7 關的懸崖由 +322% 降到 +178%，
**那個基準還沒上真機被玩過**，這一版不能蓋掉它（AGENTS.md 1.4）。

（唯一被改掉的既有欄位是 `G.hurt`——那是**畫面欄位**，只有 `drawHUD()` 讀它
畫全螢幕紅色覆蓋與暗角，不參與任何傷害／無敵／掉落判定。理由見下面第 3 點。）

| 層次 | 加了什麼 | 回答玩家的哪個問題 |
|---|---|---|
| 全螢幕金色閃光 `G.reviveFlash` | 0.45 秒衰減完，外加一層由中心向外的暖色暈 | 「剛剛發生了大事」 |
| 燭光柱 + **向內收縮**的三圈金環 | 復活後前 1.2 秒 | 「發生的是什麼事」 |
| 向上飄的 🕯️ 符號 | 同上，放大淡出 | 「是哪一項永久強化救了我」 |
| 金色虛線環 `G.reviveT` | 整段無敵期間，最後 0.6 秒閃爍變快 | 「我現在還無敵嗎、還剩多久」 |
| 專屬音效 `SFX.revive()` | 熄滅 → 留白 0.36 秒 → 重新點燃 | 「這跟武器進化不是同一件事」 |
| 頓幀 `hitstop` .18 → .28、`shake` 16 → 20 | — | 給這一刻應有的重量 |
| **拿掉**受擊紅閃（`G.hurt=1` → `0`） | — | 「你被救回來了」不該同時閃「你受傷了」 |

三個刻意的設計選擇，都是為了**不跟既有的視覺語言撞車**：

1. **金環是向內收縮的**，而核彈的衝擊波是向外擴散的。核彈是「我把場面炸開」，
   復活是「生命被收回身上」——兩件事如果用同一組視覺語彙，玩家會分不出來。
2. **全螢幕閃光的顏色是金色**。受擊是紅、核彈是白橘、復活是金，三種「畫面整片變色」
   的事件各有各的顏色，玩家用餘光就分得出剛剛發生的是哪一件，不必回頭看愛心。
   暖色暈的方向也跟受擊的暗角相反（受擊是邊緣壓黑逼近，復活是邊緣發亮擴散），
   讓「快死了」跟「被救回來」在畫面的同一個位置給出相反的訊號。
3. **復活那一刻不再閃紅**。原本這裡沿用受擊的 `G.hurt=1`（全螢幕紅閃 + 邊緣暗角）。
   截圖出來才看清楚問題：紅閃的語意是「你受傷了」，金閃的語意是「你被救回來了」，
   兩個相反的訊號疊在同一幀上，畫面變成橘紅色，金色想建立的區隔直接被洗掉。
   `G.hurt` 只有 `drawHUD()` 讀它畫覆蓋色，不參與任何判定，拿掉不動難度。
   ⚠ 這一條是**看了截圖才發現的**——參數（環的起手半徑、線寬、閃光強度）也是
   靠三張截圖（復活瞬間 / 0.55 秒 / 2.85 秒）來回調兩輪才定案的。
   視覺改動不能只靠讀程式碼判斷「應該會好看」。
4. **音效不是借用 `evo()`**（原本借的就是它）。那段是純上行的歡快音階，
   聽起來像「我變強了」；復活的情緒是「差點死掉、又被救回來」。
   新的 `SFX.revive()` 中間刻意留了 0.36 秒的空白——**沒有聲音的那一下**，
   才是「心跳停了一拍」的來源。

### 一個容易被下一個人做錯的細節：兩個時鐘

`reviveFlash` 與 `reviveT` **故意用不同的時鐘**，這件事寫在程式碼註解裡，
也用測試釘住了：

- `reviveFlash` 在 `loop()` 裡用**真實時間**衰減——跟 `G.hurt`、`G.nukeFlash` 一致。
  因為復活當下有 0.28 秒頓幀（`hitstop`），如果閃光也走遊戲時間，
  它會跟著一起凍住，看起來像卡住而不是演出。
- `reviveT` 在 `update()` 裡跟 `P.iframe` 用**同一個 dt**（會被慢動作縮放）。
  因為它畫的是「你還無敵多久」。一旦改成真實時間，玩家擦彈觸發慢動作時，
  光環就會比無敵**先**消失——那是一個會說謊的 UI，比沒有還糟。

`py_revive_fx.py` 第 6、7 條就是在守這件事：推進遊戲迴圈之後（正常速度與慢動作各一次），
`reviveT` 與 `P.iframe` 的差必須小於一格（1/60 秒）。
下次有人「順手把兩個時鐘統一」的時候，這支測試會攔下來。

### 順手收掉的一個狀態殘值

`hurtPlayer()` 走到真正死亡那一支時，會把 `reviveT`／`reviveMax`／`reviveFlash` 歸零。
畫面本來就不會再畫它（`drawReviveFX()` 只在遊戲進行中被呼叫），
但留著一個「還在復活中」的殘值遲早會誤導下一個接手的人——
**狀態的生命週期要自己收尾，不要靠畫面那一層幫忙擋。**

### 新增測試：`tests/py_revive_fx.py`（第 44 支）

⚠ **根本問題是：復活這條路徑從 v0.9.17 到 v0.9.36，整套 43 支測試沒有任何一支走過它。**
連「有蠟燭時愛心歸零不該結束遊戲」這種最基本的行為都沒被釘住。
所以這支測試把「機制」跟「演出」一起收：機制那四條是**回歸防護網**
（這一版沒動它們，是為了以後有人動的時候會被攔下來），演出那四條是新功能的驗收。

**寫這支測試踩到的兩個坑，都值得記下來（AGENTS.md 第 3 節第 2 條：量測工具本身也要被驗證）：**

1. **第一版用「整張畫面的平均 R-B」判斷有沒有金色閃光——對照版（v0.9.36）照樣通過。**
   因為復活本來就會噴金色粒子、放金色衝擊波，整張平均的 R-B 在對照版也跳了 +67。
   改成量**四個角落**：全螢幕閃光的定義就是連離玩家最遠的角落都會變色，
   粒子與衝擊波做不到這件事。
2. **第二版改成「同一個狀態畫兩幀、中間只切換 `reviveFlash`」，對照版量到 22.6 的假差值。**
   原因有兩個，都不是這一版造成的，而是這個引擎本來的性質：
   ① 復活會 `shake(20)`，而畫面震動是**每幀重算偏移**的；
   ② 畫面用的是半透明覆蓋的拖尾畫法，**光是連續呼叫 `draw()`，畫面就會一幀比一幀淡**
   （實測同一個遊戲狀態連畫 6 幀，角落 R-B 從 76 一路掉到 32）。
   → 最終做法：先清掉粒子／衝擊波／飄字、震動歸零、連畫 8 幀讓畫面**收斂**，
   並且**先驗證收斂**（連續兩幀的差必須 < 3）才開始比較。
   收斂後量到的差是：對照版 **0.0**、v0.9.37 **57.4**。

### 驗收數字

| 項目 | v0.9.36（對照版） | v0.9.37 |
|---|---|---|
| 角落金色程度（只開/關全螢幕閃光這一個變因） | **0.0** | **+57.4** |
| `reviveT` 與 `P.iframe` 的差（正常速度推進 1 秒） | 欄位不存在 | **0.0000s** |
| `reviveT` 與 `P.iframe` 的差（慢動作推進 1 秒） | 欄位不存在 | **0.0000s** |
| 復活的愛心／無敵／清彈 | 3/3、3.2s、12→0 | 3/3、3.2s、12→0（**完全相同**） |
| 整套測試 | 44 支 + 1 支效能 | 全綠 |

### 順手修掉的兩支「假紅字」測試（都不是這一版造成的）

整套測試在這台 **2 核心**容器上是 44 支 Chromium 平行跑，本來就很容易出現
載頁面逾時。但這一版第一輪跑出來的紅字裡，有兩支是**真的有問題**，不是負載雜訊：

1. **`py_boss_skin` 與 v0.9.36 新增的 `py_card_hp_weight` 都寫死用 8807 埠**，
   平行跑必撞（`Address already in use`）。這跟 v0.9.36 修掉的
   「`py_layout_fit` 與 `py_boot_sel` 都寫死 8803」是**同一個病**——
   新增測試時複製了舊檔卻沒改埠號。已把 `py_boss_skin` 改成「埠被佔用就往上找」。
   ⚠ **往後新增測試檔，第一件事就是確認埠號沒有跟現有的 43 支撞。**
2. **`py_save_code` 用固定 `wait_for_timeout(800)` 等頁面載完**，
   連續兩輪紅字（`Page.click("#btnCode")` 逾時 60 秒），單獨跑一定綠。
   這正是 AGENTS.md 第 3 節第 1 條講的那件事。已改成輪詢
   「`saveCodeDecode` 已定義且 `#btnCode` 存在」。

### 給下一場對話的環境筆記（省時間用）

- `device_stage_files`（讀取）在這個環境**仍然是 HTTP 401 全壞**，只能寫不能讀。
  但 **repo 是公開的**，所以要拿到完整的 `tests/`（44 支）最快的方法是
  `git clone https://github.com/davidform/goo-blaster.git`——
  雲端容器有網路，clone 出來的 `index.html` 跟使用者電腦上那份**逐位元組相同**
  （已用 md5 對過），比想辦法從對話裡搬檔案快得多。
- 跑測試前要先準備兩樣東西，否則會有兩支測試以「環境」而不是「程式碼」的理由紅字：
  1. `py_ab_base.py` 讀 `/tmp/index.v0920.bak`（v0.9.20 的基準檔）。
     用 `git log` 找到 `BUILD='v0.9.20'` 那個 commit，
     `git show <commit>:index.html > /tmp/index.v0920.bak` 即可。
  2. 好幾支測試把路徑寫死成 `/home/claude/work/goo-blaster/index.html` 與
     `/home/claude/goo/game`——工作副本就放在 `/home/claude/work/goo-blaster`
     最省事，不要放別的地方。
- `pkill -f "tests/py_"` 會把執行它的那個 shell 自己也殺掉（exit 144），
  之後接的指令不會執行。要清殘留程序就單獨跑一行，不要跟 `run_tests.sh` 串在一起。

### 沒有做的事（刻意）

- **沒有改復活的機制。** 專案負責人的原話可以有兩種讀法：(a) 加演出、
  (b) 換一套復活規則（例如復活只給 1 顆心 + 更長無敵）。
  (b) 是數值改動，會直接蓋掉 v0.9.35 留下的第 7 關基準，**必須等真機回饋回來之後再單獨一版**。
  這一版做的是 (a)。
- **沒有動 11 種語言的字典。** 復活提示沿用既有的 `tRevive`，
  沒有新增任何 key（維持 231 個），所以 `py_i18n`／`py_cjk_scan`／`py_layout_fit`
  這一版本來就不該有變化——也確實沒有。
